# CLAUDE.md — הנחיות עבודה לפרויקט ultranet-platform

קובץ זה נקרא אוטומטית בתחילת כל שיחה. הוא מכיל את המוסכמות והכללים לעבודה על
הפרויקט. **האיפיון המלא של המערכת נמצא ב-[`SPEC.md`](SPEC.md)** — שם
מתועדים המודולים, מודל הנתונים, ההרשאות והאינטגרציות.

## מה זו המערכת (בקצרה)

**הנהלת החשבונות של עסק "אולטרנט", ולא יותר.** העסק מנהל חדרי מחשבים, השכרות
(מחשבים ניידים + סטיקים) ומשרד שיתופי בכ-35 סניפים — אבל המערכת הזו מתארת רק את
הכסף שלהם, במודל שלוש שכבות. הניהול התפעולי היומיומי (מכשירים, לקוחות, קריאות
שירות, מלאי) ממשיך לרוץ ב-`app.html` הישן, על אותו Firestore בדיוק.

**שלושה מסכים בסך הכל:** `/dashboard/accounting` (המודל), `/dashboard/settlement`
(כמה כל סניף צריך להעביר + הדו"ח החודשי), `/dashboard/my-expenses` (מה מנהל סניף
מזין). ולבעלים גם `/dashboard/users`.

**ה-DB הוא Firestore הקיים — אין מיגרציה, אין שינוי סכימה.** ראה `SPEC.md`.

## סטאק

- **מונורפו**: pnpm workspaces + Turborepo
- **`apps/web`**: Next.js 14 (App Router) + NextAuth + Tailwind — האפליקציה היחידה
- **`packages/shared-types`**: טיפוסי TS משותפים שמשקפים את סכימת ה-Firestore
- **DB**: Firebase / Firestore (דרך `firebase-admin`), פרויקט `ultranet-e94aa`

## פקודות

```bash
pnpm install
cp apps/web/.env.example apps/web/.env.local   # למלא פרטי Firebase Admin
pnpm dev          # מריץ את כל האפליקציות (turbo)
pnpm build
pnpm lint
pnpm typecheck    # tsc --noEmit
```

## מוסכמות קוד

- **TypeScript בלבד**, `strict`. אין להשתמש ב-`any` חדש ללא סיבה.
- **טיפוסי דומיין** חיים ב-`packages/shared-types` ומשקפים 1:1 את שדות ה-Firestore.
  אין לשנות שם שדה/קולקשן בלי לוודא התאמה ל-`app.html` ולדאטה הקיים.
- **שמות קולקשנים** תמיד בתחילית `n_` (למשל `n_branches`, `n_rentals`).
- **Server Actions** (`actions.ts`) לכל מוטציה; קומפוננטות `page.tsx` הן Server
  Components כברירת מחדל, אינטראקטיביות ב-`*-client.tsx` / `use client`.
- **הרשאות**: כל route/מודול מוגן דרך `requireModuleAccess(key)` או `requireOwner()`
  מ-`apps/web/lib/perms.ts`. אין לחשוף מודול בלי בדיקת הרשאה בצד השרת. שכבות 1 ו-2
  כולן `requireOwner()` — גבול השכבה הוא גבול ההרשאה.
- **תשלומים**: לעולם לא לשמור PAN מלא. רק `cardLast4` / `cardExpiry` (לא רגיש) או
  `gatewayToken` מטוקניזציה של Nedarim Plus.
- **טקסטים ב-UI בעברית** (RTL). שמות קבצים ומזהי קוד באנגלית.

## אל תוסיף מסך (חשוב)

המערכת הזו הייתה עמוסה במסכים, ורובם היו דרכים שונות להזין את אותו שקל — וזו בדיוק
הסיבה שהמספרים לא הסתדרו. **תנועה נכנסת פעם אחת ל-`n_tx`**, וכל השאר הוא שאילתה עליה
(`lib/tx-data.ts`, `lib/business-ledger.ts`). לפני שמוסיפים מסך, טבלה או קולקשן — יש
לוודא שהמספר לא נגזר כבר ממה שקיים. מסך הזנה מקביל הוא לא עוד דרך להזין; הוא דרך לספור
פעמיים.

## כלל תחזוקת תיעוד (חשוב)

בכל שינוי משמעותי — פיצ'ר חדש, מודול חדש, שינוי במודל הנתונים
(`packages/shared-types`), אינטגרציה חדשה, או שינוי בהרשאות — **עדכן את
`SPEC.md`** באותו commit של השינוי. התיעוד הוא חלק מההגדרה של "גמור",
לא משימה נפרדת. אם הוספת קולקשן/שדה חדש ל-Firestore — הוסף אותו לטבלת מודל
הנתונים ב-SPEC. שמור את ה-SPEC מסונכרן עם הקוד.

## תהליך עבודה (git)

- כל שיחה מפתחת ב-branch ייעודי משלה; לא דוחפים ל-`main` ישירות.
- **מיזוג אוטומטי**: בסיום משימה (אחרי שהקוד נבדק ועובד) ממזגים את ה-branch
  ל-`main` באופן אוטומטי, ללא צורך לעצור ולבקש אישור מפורש בכל פעם. אין
  צורך לשאול את בעל הפרויקט לפני מיזוג — זו הסכמה עומדת עבור הפרויקט הזה
  כולו.
- הודעות commit ברורות ותיאוריות.
