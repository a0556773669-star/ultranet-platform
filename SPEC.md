# איפיון מערכת — ultranet-platform

> מסמך חי. יש לעדכן אותו בכל שינוי פיצ'ר, מודול, מודל נתונים או אינטגרציה
> (ראה כלל תחזוקת התיעוד ב-[`CLAUDE.md`](CLAUDE.md)).
>
> עודכן לאחרונה: 2026-09-06

## תוכן עניינים
1. [סקירה כללית](#1-סקירה-כללית)
2. [ארכיטקטורה](#2-ארכיטקטורה)
3. [סטאק טכנולוגי](#3-סטאק-טכנולוגי)
4. [הרצה והגדרת סביבה](#4-הרצה-והגדרת-סביבה)
5. [תפקידים והרשאות](#5-תפקידים-והרשאות)
6. [אימות (Authentication)](#6-אימות-authentication)
7. [מודל הנתונים (Firestore)](#7-מודל-הנתונים-firestore)
8. [מודולים ופיצ'רים](#8-מודולים-ופיצרים)
9. [אינטגרציות חיצוניות](#9-אינטגרציות-חיצוניות)
10. [עקרונות מפתח והחלטות ארכיטקטורה](#10-עקרונות-מפתח-והחלטות-ארכיטקטורה)
11. [מפת קבצים מרכזיים](#11-מפת-קבצים-מרכזיים)
12. [פרק יד׳ — מה מנהל הסניף מזין בעצמו](#12-פרק-יד-—-מה-מנהל-הסניף-מזין-בעצמו)

---

## 1. סקירה כללית

**אולטרנט (ultranet)** היא מערכת ניהול מאוחדת לעסק המנהל מספר קווי פעילות בכמה
סניפים:

- **חדרי מחשבים** — ניהול מכשירים, מדפסות, קריאות שירות (tickets), משימות ומלאי.
- **השכרות** — השכרת מחשבים ניידים וסטיקים סלולריים ללקוחות, כולל תמחור, גבייה
  והנהלת חשבונות ברמת סניף.
- **משרד שיתופי (coworking)** — ניהול עמדות ולקוחות עם תשלום חודשי.
- **הנהלת חשבונות** — הכנסות/הוצאות, מסלולי גבייה, והתחשבנות שותפים.
- **חנות AI** — עמוד ציבורי (`/shop`, ללא התחברות) עם עוזר קנייה חכם למחשבים בהתאמה אישית:
  לקוח מתאר למה הוא צריך מחשב, מקבל שאלות המשך ממוקדות, ובסוף שלוש הצעות תצורה (מינימלי/מומלץ/מוגזם)
  עם הסבר, אפשרות להוסיף ציוד נלווה, ולסיום קבלת הצעת מחיר להורדה + פנייה שנשמרת לצוות אולטרנט.

המערכת מרובת-סניפים ומרובת-משתמשים, עם הרשאות לפי תפקיד (owner / partner /
employee) ולפי מודול. זהו **הדור הבא של `app.html`** — אפליקציית עמוד יחיד ישנה
שעדיין רצה במקביל על אותו מסד נתונים.

---

## 2. ארכיטקטורה

מונורפו מנוהל ב-**pnpm workspaces + Turborepo**:

```
ultranet-platform/
├── apps/
│   ├── web/            Next.js 14 (App Router) — האפליקציה הראשית
│   └── api/            NestJS — שירות API (על אותו Firestore)
├── packages/
│   └── shared-types/   טיפוסי TS משותפים = סכימת ה-Firestore
├── turbo.json          פייפליין build/dev/lint/typecheck
├── pnpm-workspace.yaml
└── tsconfig.base.json
```

- **`apps/web`** — הלקוח והשרת של המשתמש. משתמש ב-App Router: `page.tsx` הן
  Server Components, מוטציות דרך Server Actions (`actions.ts`), חלקים
  אינטראקטיביים בקומפוננטות `*-client.tsx`. גישה ל-Firestore דרך
  `firebase-admin` (צד שרת בלבד).
- **`apps/api`** — שירות NestJS נפרד שקורא מאותו פרויקט Firestore
  (`ultranet-e94aa`). כרגע כולל מודול `branches` לדוגמה + מודול Firebase.
- **`packages/shared-types`** — מקור אמת יחיד לטיפוסי הדומיין; משקף שם-לשם את
  קולקשני ושדות ה-Firestore.

---

## 3. סטאק טכנולוגי

| שכבה | טכנולוגיה |
|------|-----------|
| שפה | TypeScript (strict), Node ≥ 20 |
| Frontend/SSR | Next.js 14 (App Router), React 18 |
| עיצוב | Tailwind CSS (RTL, ממשק בעברית) |
| אימות | NextAuth 4 (Credentials + Email-code + Google) |
| API נוסף | NestJS |
| DB | Firebase / Firestore (`firebase-admin`) |
| מייל | Resend, EmailJS (`@emailjs/browser`) |
| תשלומים | Nedarim Plus (טוקניזציה + חיוב) |
| קבצים/אקסל | `xlsx` (ייצוא/ייבוא לקוחות) |
| Build | Turborepo, pnpm 9 |

---

## 4. הרצה והגדרת סביבה

```bash
pnpm install
cp apps/web/.env.example apps/web/.env.local
pnpm dev        # turbo run dev — מריץ web + api
```

משתני סביבה (`apps/web/.env.example`):

| משתנה | תיאור |
|-------|-------|
| `FIREBASE_PROJECT_ID` | `ultranet-e94aa` |
| `FIREBASE_CLIENT_EMAIL` | חשבון שירות Firebase Admin |
| `FIREBASE_PRIVATE_KEY` | מפתח פרטי של חשבון השירות |
| `NEXTAUTH_SECRET` | סוד NextAuth |
| `NEXTAUTH_URL` | כתובת הבסיס (localhost:3000 בפיתוח) |
| `GOOGLE_CLIENT_ID` / `GOOGLE_CLIENT_SECRET` | אופציונלי — כניסת Google |

ל-`apps/api` קובץ `.env.example` נפרד עם פרטי Firebase.

---

## 5. תפקידים והרשאות

מוגדר ב-`apps/web/lib/perms.ts` ו-`packages/shared-types` (`AppUser`).

**תפקידים (`UserRole`):** `owner` · `partner` · `employee`.

**מפתחות הרשאה (`PermKey`):**
`branches` · `computers` · `rentals` · `coworking` · `accounting` · `tasks` · `charging` · `shop` · `duxus`.

**כללי גישה:**
- `owner` — עובר תמיד את כל הבדיקות (branchId = `"all"`).
- `partner` / `employee` — עוברים רק אם `n_users.perms[key] === true`.
- `requireModuleAccess(key)` — שער כניסה למודול; מפנה ל-`/dashboard` בהיעדר
  הרשאה, ול-`/login` בהיעדר session.
- `requireOwner()` — למסכים ברמת בעלים בלבד (למשל הנהלת חשבונות מרכזית).
- `viewClientBranchIds` — שיתוף ראייה של לקוחות בין סניפים (cross-branch).
- `charging` — הרשאה נפרדת המתירה חיוב כרטיסים (gate ל-UI וגם בצד שרת).

הניווט (`lib/nav-items.ts`) מסנן פריטים לפי הרשאה עם `visibleFor(role, perms, item)`.

**הספר הראשי הוא גבול ההרשאה:** השותף לא "מסונן" מההנה"ח הראשית — פשוט אין לו אליה גישה.
אין שדה להסתיר, אין מסך לחסום, ואין באג הרשאות שממתין לקרות: **כל** מסכי
`/dashboard/accounting` דורשים `requireOwner()`. השותף רואה את הסניף שלו (`/rentals/accounting`,
`/expenses/[id]`, שאליו הוא מגיע בהפניה אוטומטית מ-`/rentals/expenses` לפי ה-session ולא לפי
הטופס). החריגה היחידה, וההכרחית: השותף רואה **כמה** מחשבים וסטיקים יש אצלו, כי הוא חייב
לתפעל אותם — אבל לא כמה הם שווים.

אותו גבול חל גם **בתוך** מסך משותף: ב-`/rentals/accounting` השותף לא מקבל את טבלת ה-`owner*`
של הבעלים מסוננת לשורה אחת, אלא מסך אחר לגמרי — שש משבצות עם החלק שלו וחשבון פתוח. ראו
`PartnerAccountingView` בסעיף 10.

---

## 6. אימות (Authentication)

מוגדר ב-`apps/web/lib/auth.ts` (NextAuth, אסטרטגיית JWT). שלושה providers:

1. **מייל + סיסמה** — נבדק מול `n_users` (השדה `pass`, plaintext legacy — תואם
   ל-`app.html`; יש להחליף לאימות תקין לפני חשיפה חיצונית).
2. **קוד באימייל** (`email-code`) — מייל חייב להופיע ב-`n_approved_emails`; קוד
   חד-פעמי נשמר ב-`n_login_codes` עם תפוגה, ונמחק אחרי שימוש.
3. **Google** — מצליח רק אם כתובת ה-Gmail כבר קיימת ב-`n_users`.

ה-JWT מסנכרן מחדש `role` / `branchId` / `perms` / `viewClientBranchIds`
מ-Firestore כל ~2 דקות, כך ששינויי הרשאה נכנסים לתוקף בלי צורך בכניסה מחדש.
**הערה חשובה למימוש**: `getServerSession()` מריץ את ה-callback אבל אין לו תשובה לכתוב
אליה, ולכן ה-`branchSyncedAt` המעודכן נזרק ולא נשמר בעוגייה — חלון ה-2 דקות שעל הטוקן
לעולם לא נסגר בזמן רינדור. לכן הקריאה ל-`n_users` עצמה מקושרת לפי אימייל באותו חלון
(`loadUserForSync` ב-`lib/auth.ts`); בלי זה כל קריאת session בצד שרת הייתה שאילתה נפרדת,
ובכל דף יש לפחות שתיים (layout ואז `requireModuleAccess`) ועוד אחת לכל Server Action.
פעולות עריכת משתמש קוראות ל-`invalidateUserSyncCache(email)` כדי ששינוי הרשאה ייכנס לתוקף
מיד ולא בסוף החלון. בנוסף קיים אימות מכשיר (`/verify-device`, `lib/device-trust.ts`).

---

## 7. מודל הנתונים (Firestore)

כל השמות בתחילית `n_`. הטיפוסים ב-`packages/shared-types/src/index.ts`.
**אין לשנות שמות שדות/קולקשנים ללא התאמה ל-`app.html` ולדאטה הקיים.**

> **קולקשני שכבות 1–2 (`n_tx`, `n_purchases`, `n_items`, `n_item_moves`, `n_cost_rates`) —
> הדאטה נשאר, המסכים נמחקו (2026-09).** `lib/tx.ts` / `lib/tx-data.ts` / `lib/assets*.ts`
> ממשיכים לשרת את חישוב ההשקעה ב-`/dashboard/computer-rooms-accounting`, אבל מסכי ההזנה
> שלהם (`entries`, `purchases`, `inventory`, `sales`) הוסרו יחד עם מודל שלוש השכבות. אין
> מיגרציה ואין מחיקת דאטה.
>
> **`n_tx` אינו נקרא היום ע"י אף מסך הנה"ח.** `lib/branch-accounting-data.ts` — המקור של
> `/rentals/accounting` ושל ההנה"ח הראשית — קורא `n_fixed_expenses` ו-`n_var_expenses` בלבד.
> לכן `n_tx` הוא כרגע קולקשן היסטורי: יש בו מסמכים, אבל הם לא מגיעים לשום דוח. ראו פרק יד׳.

> **`countsToMain` — שדה רוחבי.** מופיע על `FixedExpense`, `VariableExpense`,
> `MultiBranchExpense`, `RecurringVariableExpense`, `AccountingExpense` ו-`CoworkingPayment`.
> הוא ורק הוא קובע אם השורה נכנסת לסה"כ של ההנה"ח הראשית. `undefined` = `false`.
> ראו `COUNTS_TO_MAIN_DOC` בטיפוסים ואת סעיף ההנה"ח למטה.
>
> **החריג היחיד: `Branch.setupCountsToMain`** (עלות הקמה של סניף) — שם `undefined` = `true`.
> זו אינה שורה שמישהו הזין אלא שדה אחד לסניף שגלוי בטופס, ולכן אין סיכון ששורות ותיקות
> ייסחפו לספר בדיעבד; הבעלים ביקש שעלויות ההקמה ייכנסו לראשי, והצ'קבוקס משמש להחריג סניף
> ולא לצרף אותו. הכלל חי ב-`setupCostCountsToMain()` ב-`apps/web/lib/counts-to-main.ts`
> ואסור לקרוא את השדה ישירות.

| קולקשן | טיפוס | תיאור |
|--------|-------|-------|
| `n_branches` | `Branch` | סניפים; סוג (`computers`/`rentals`/`coworking`), אחוזי בעלים/שותף, סניף-אב, מסלול גבייה, דגלי גבייה/קבלות, `phone` (רשות - טלפון ליצירת קשר), `openedAt` (רשות - תאריך פתיחת הסניף; ממנו ואילך מחושבות ההכנסות וההוצאות, ראו "תאריך פתיחה" בסעיף ההנה"ח), `notStarted` (רשות - סימון ידני "עדיין לא התחיל לפעול" שגובר על כל נתון שכבר הוזן), `rentalPricing` (רשות - מחירון ברירת המחדל של סניף השכרות, ראו `/pricing`), `deleted`/`deletedAt` (רשות - soft-delete טכני, ראו סעיף 10 `/branches`), `closedAt` (רשות - **תאריך הסגירה העסקי**, שאינו `deletedAt`: סניף שנסגר ב-15 ביולי וסומן כמחוק רק בספטמבר חייב להפסיק לצבור מיולי; ראו פרק טו׳ בסעיף 8), `expensePolicy` (רשות - מי משלם כל קטגוריה חוזרת בסניף; ממנו נגזר `paidBy` של כל הוצאה שמנהל הסניף מזין, ראו "מה מנהל הסניף מזין" בסעיף 8), `setupCost` (רשות - סך עלות ההקמה של חדר מחשבים), `setupItems` (רשות - `SetupCostItem[]`, פירוט עלות ההקמה: `label` + `amount` לכל שורה; כשהוא קיים `setupCost` הוא בדיוק סכום השורות), `setupCountsToMain` (רשות - האם עלות ההקמה נספרת בהנה"ח הראשית; **ברירת המחדל הפוכה מ-`countsToMain` הרגיל: `undefined` = כן נספר**, ראו סעיף ההנה"ח הראשית) |
| `n_users` | `AppUser` | משתמשים; תפקיד, סניף, `perms`, `viewClientBranchIds` |
| `n_fixed_expenses` | `FixedExpense` | הוצאות קבועות לסניף; `branchIds` (רשות) - רק כש-`branchId` הוא סנטינל משותף (`shared-rentals`/`shared-computers`), רשימת הסניפים שההוצאה מתחלקת ביניהם. חסר/ריק = כל סניפי המודול, כולל סניפים שייפתחו (ההתנהגות ההיסטורית). `ownerPct` (רשות) - **רק על סנטינל משותף**: כמה אחוז מההוצאה על הבעלים, והסניפים מתחלקים בשאר שווה בשווה. השדה הוא גם המתג: קיים = ההוצאה נכנסת לספר של כל סניף שהיא חלה עליו; חסר = נשארת בספר המשותף בלבד ו-`owedBy` הוא שקובע. ראו `lib/expense-shared-scope.ts` |
| `n_var_expenses` | `VariableExpense` | הוצאות משתנות (לפי חודש); `branchIds` ו-`ownerPct` (רשות) - כמו ב-`FixedExpense` |
| `n_branch_income` | `BranchIncome` | הכנסות סניף; שורות הכנסה ידניות owner-only בדף `/dashboard/rentals/expenses/[id]` - מתחשבנות כמו השכרה רגילה בחישוב ההשכרות (`computeBranchFinancials`), וגם שורות ידניות נפרדות בדשבורד "השקעה מול רווח" של חדרי מחשבים (שם לא מחושבות בשום מקום) - בשני המקרים לעולם לא נכתב ל-`n_ah_income`, לא משפיע על הנה"ח הראשית. שדה `source` (`"manual"` \| `"import"`, ריק = ידני) מסמן בחדרי מחשבים שורה שנוצרה מייבוא קובץ אקסל, ומאפשר ייבוא חוזר אידמפוטנטי וניקוי גורף בלי לגעת בשורות שהוקלדו |
| `n_tasks` | `Task` | משימות (דחיפות, חזרתיות, בוצע) |
| `n_sub_locations` | `SubLocation` | תתי-מיקומים בסניף |
| `n_devices` | `Device` | מכשירים/מחשבים בחדרי מחשבים |
| `n_laptops` | `Laptop` | מחשבים ניידים להשכרה + תמחור (יום/שבוע/חודש, וריאנטים); `hasPartner`/`partnerName`/`partnerPct` - שותפות פר-מחשב (ראו סעיף 8); `itemId` (רשות) - הפריט ב-`n_items` שמייצג את המחשב הפיזי ונושא את עלותו האמיתית |
| `n_sticks` | `Stick` | סטיקים סלולריים + תמחור מדורג (`day1`/`day2`/`day3plus`) ומדרגות `weekPrice`/`monthPrice` (רשות) להשכרת סטיק בלבד; `linkedLaptopId` מקשר לסטיק שמשוייך למחשב ניתן להשכרה - מסונכרן אוטומטית מ-`n_laptops` (ראו סעיף 8, `/laptops`); `itemId` (רשות) - הפריט ב-`n_items` שמייצג את הסטיק הפיזי |
| `n_rental_clients` | `RentalClient` | לקוחות השכרה; פרטי כרטיס לא-רגישים, `gatewayToken`, `collectionRouteId` (העסק שהטוקן שויך אליו) |
| `n_rentals` | `Rental` | השכרות; פריט, תאריכים, מחיר מחושב/סופי, סטטוס, תשלום |
| `n_ops_stock_items` | `OpsStockItem` | **הגדרת פריט מלאי** במודול התפעול, נקבעת ע"י הבעלים: `name`, `targetQty` (+`unit`) = הכמות שאמורה להיות תמיד בסניף, `scope` (`all`/`branches`) + `branchIds`, `order`, `active`. הסניף לא מזין כמויות — רק מסמן מולה |
| `n_ops_stock_checks` | `OpsStockCheck` | **בדיקת המלאי של סניף בחודש אחד**. מזהה דטרמיניסטי `${branchId}__${YYYY-MM}` (upsert). `marks` = מפה מ-`OpsStockItem.id` ל-`{status: "ok"|"missing", note?, at, by}`. **כאן נמצא ה"איפוס בכל ראשון לחודש"**: אין job שמוחק כלום — חודש חדש הוא מסמך שעוד לא קיים, ולכן הסימונים מתחילים ריקים וההיסטוריה נשמרת |
| `n_ops_tasks` | `OpsTask` | **הגדרת משימה** תפעולית: `name`, `details` (רשות), `freq` (`weekly`/`monthly`), `scope` + `branchIds`, `order`, `active` |
| `n_ops_task_checks` | `OpsTaskCheck` | **ביצועי המשימות של סניף בתקופה אחת**. מזהה דטרמיניסטי `${branchId}__${periodKey}`, כאשר `periodKey` הוא `YYYY-Www` (ISO) למשימות שבועיות ו-`YYYY-MM` לחודשיות — ולכן לסניף יש בו-זמנית מסמך שבועי ומסמך חודשי. `done` = מפה מ-`OpsTask.id` ל-`{at, by}`. גם כאן האיפוס הוא מעבר תקופה, לא מחיקה |
| `n_printers` | `Printer` | מדפסות (טונר, נייר, IP) |
| `n_cw_stations` | `CoworkingStation` | עמדות במשרד שיתופי |
| `n_cw_clients` | `CoworkingClient` | לקוחות משרד שיתופי + היסטוריית תשלומים |
| `n_recurring_var_expenses` | `RecurringVariableExpense` | **הוצאה קבועה משתנה** — חשמל, משכורת מזכירה, מע"מ: שורה אחת + מערך `amounts` של `{month, amount, updatedAt}`. חודש בלי סכום = "עוד לא עודכן" (לא אפס) וזה מה שהתזכורת החודשית מחפשת. `frequency` (`monthly`/`bimonthly`/`quarterly`/`yearly`, ברירת מחדל חודשי) קובע באילו חודשים נדרש תשלום, ו-`spread` אם לפרוס תשלום רב-חודשי על חודשי המחזור בדוחות. `scope` (`computers`/`rentals`/`coworking`/`main`) קובע באיזה מסך הכרטיס מופיע |
| `n_expense_types` | `RecurringPurchaseType` | **סוג רכישה חוזרת** — נייר למדפסת, שקיות אשפה, פחיות. שם + `module` (תיעוד בלבד — הסוגים גלובליים) : הוא לא מחזיק סכומים ולא משנה איך ההוצאה נספרת. `expenseTypeId` על `n_var_expenses` / `n_multi_branch_expenses` / `n_ah_expenses` מחבר את כל הקניות של אותו מוצר בכל העסק, והסיכום מוצג ליד ההוצאות עצמן — אין מסך נפרד |
| `n_partner_payouts` | `PartnerPayout` | תשלום שהועבר פר חודש למי שיש לו אחוז מהברוטו — שותף-מחשבים חיצוני או הסדר פר-סניף (`lib/revenue-shares.ts`); מזהה דטרמיניסטי `${slug(partnerName)}_${month}` (upsert). מה שלא נרשם כאן פשוט מצטבר ביתרה — ראו `lib/partner-payouts.ts` |
| `n_ah_income` | `AccountingIncome` | הכנסות בהנה"ח מרכזית (בעלים); 3 סוגים בלבד - `laptops`/`credit`/`cash`, ראו סעיף 8. שורת `cash` נקראת גם (לתצוגה בלבד, בלי כתיבה חזרה) לדשבורד המעקב של חדר המחשבים שמזוהה ב-`branchId` שלה |
| `n_ah_expenses` | `AccountingExpense` | הוצאות בהנה"ח מרכזית — רכישה/הוצאה **חד-פעמית** של העסק עצמו |
| `n_ah_fixed_expenses` | `AccountingFixedExpense` | **הוצאה קבועה של העסק עצמו** — שכירות משרד, רואה חשבון, מנוי תוכנה: אותו סכום כל חודש, שורה אחת. `name`, `amount` (הסכום ה**חודשי**), `startDate` (נספרת מהחודש הזה והלאה, החודש הראשון במלואו), `endDate` (רשות — החודש האחרון שנספר), `business`, `category`, `countsToMain`. המקבילה של `n_fixed_expenses` לספר הראשי, בלי `branchId`: ההוצאה של העסק ולא של סניף, ולכן גם לא יכולה להפוך ל"שארית" כשסניף נמחק. הצבירה ב-`apps/web/lib/main-fixed-expenses.ts`, והספר הראשי מקבל **שורה לכל חודש** |
| `n_collection_routes` | `CollectionRoute` | מסלולי גבייה (ספק תשלום, קבלות, יעד הפקדה, עמלות, `defaultForNewCards`) |
| `n_shop_catalog` | `ShopComputerConfig` | תצורות מחשב אמיתיות למכירה בחנות ה-AI (`/shop`) - שם, דרגה (`minimal`/`recommended`/`extreme`), תחומי שימוש (`useCases`), מפרט, מחיר; ממולא ידנית ע"י הבעלים ב-`/dashboard/shop/catalog` |
| `n_shop_addons` | `ShopAddon` | ציוד נלווה שניתן להוסיף להצעת המחיר בחנות ה-AI (תיק/עכבר/מקלדת/דיסק און קי/אחר) |
| `n_shop_leads` | `ShopLead` | פניות שהתקבלו מהצ'אטבוט הציבורי - תיאור, תשובות, ההמלצות שהוצגו, תצורה/ציוד נבחרים, פרטי קשר, סטטוס |
| `n_branch_transfers` | `BranchTransfer` | התחשבנות חודשית שותף↔בעלים; `transferredAmount` (רשות) - הסכום שבפועל נרשם כהועבר לאותו חודש, באותה מוסכמת סימן כמו `netToOwner` (חיובי = לבעלים, שלילי = מהבעלים) - מאפשר לרשום העברה חלקית/מאוחרת שלא תואמת בדיוק את הסכום המחושב. רשומות ישנות בלי השדה הזה (רק `transferred: true`) מטופלות כמסולקות במלואן, ראו `lib/branch-ledger.ts`. `linkedAhIncomeId` (רשות) - מזהה רשומת `n_ah_income` שנוצרה/מתעדכנת אוטומטית כש-`transferredAmount` חיובי, ראו `lib/branch-income-ledger.ts` בסעיף 9. `receiptIssued` (רשות) - סימון עצמאי "הוצאנו קבלה", לא קשור לסכום |
| `n_approved_emails` | — | מיילים מאושרים לכניסת קוד |
| `n_login_codes` | — | קודי כניסה חד-פעמיים עם תפוגה |
| `n_procedures` | `Procedure` | נהלים ברורים במודול "משימות ונהלים" (`/dashboard/duxus/procedures`) - כותרת, תיאור קצר (`summary`), תוכן HTML עשיר (עורך זהה להדרכות), קטגוריה חופשית, `version` (מספר גרסה ידני), `status` (`draft`/`active`/`superseded`/`archived` - דאטה ישן בלי השדה נחשב `active`), `ownerName`, `supersedesId` (שרשרת גרסאות - גרסה חדשה מסמנת את הקודמת `superseded` ולעולם לא מוחקת אותה), ו-`attachmentName`/`attachmentDataUrl` (קובץ PDF/Word כ-data URL בתוך המסמך, אותו דגם כמו קבצי ההדרכות) |
| `n_quarters` | `Quarter` | רבעוני העבודה (`/dashboard/duxus/rocks`). **מזהה המסמך הוא ה-`quarterKey`** שמופיע על `Rock`/`Milestone`: `label` (תווית חופשית, למשל "ראש חודש אלול - ראש חודש כסלו"), `status` (`active`/`archived` - מאורכב = קריאה בלבד, נאכף בשרת), `startDate`/`endDate` (רשות, לתצוגה), `order` (חותמת זמן למיון ציר הזמן), `activeMonthKey`/`activeWeekKey` (החודש/השבוע ה"פתוחים" - מה שמוצג במסכי החודש והשבוע; ריק = עוד לא נפתחו), `rolledFromKey` (הרבעון שממנו גולגל). מפתחות לועזיים ישנים ("2026-Q3") ממשיכים לעבוד בלי מיגרציה - מוצגים כרבעון פעיל וירטואלי, והמסמך נוצר בפועל רק כשמבצעים עליו פעולה |
| `n_rocks` | `Rock` | סלעים רבעוניים ותתי-סלעים (`/dashboard/duxus/rocks`) - `quarterKey` (מזהה מסמך ב-`n_quarters`), `parentRockId` (ריק = סלע עצמו, מוגדר = תת-סלע, עומק אחד), `status` (`active`/`done`/`dropped`; **`done` תמיד מחושב מאבני הדרך ולעולם לא מסומן ידנית** - `dropped` הוא ההחלטה הידנית היחידה, ודורשת סיבה), `dueDate` (רשות), `deletedAt` (מחיקה לוגית - סלע עם ילדים או היסטוריה לא נמחק פיזית), `rolledFromId`, אחראי |
| `n_milestones` | `Milestone` | אבני הדרך (המשימות בפועל) תחת סלע/תת-סלע, וגם המשימות השוטפות. **הסטטוס נשמר כאן פעם אחת בלבד** - `status` (`not_started`/`in_progress`/`waiting`/`done`/`cancelled`) הוא המקור היחיד לאמת, ו-`done` הבוליאני הוא שיקוף שלו שנשמר כדי ששאילתות ישנות ימשיכו לעבוד (לעולם לא לעדכן אותו בנפרד). דאטה ישן בלי `status` נגזר מ-`done` בזמן קריאה. עוד: `quarterKey` (מוכפל מהסלע-אב לשאילתת שוויון יחידה), `description`, `priority`, `dueDate`, `notes`, `waitReason`/`waitUntil` (חובה בהמתנה), `cancelReason` (חובה בביטול), `doneAt`/`completedBy`, `reopenCount`, `carryOverCount` (כמה פעמים שובצה מחדש), `source` (`rock`/`adhoc`), `origin` (`quarter`/`month`/`week` - איפה נוספה, כדי להפריד "תוכנן מראש" מ"נוסף תוך כדי"), `deletedAt` (מחיקה לוגית), `updatedAt` (נעילה אופטימית). **השיוך לתקופות אינו כאן** אלא ב-`n_period_assignments`; `stage`/`monthKey`/`weekKey` נשארו כשדות legacy לקריאה בלבד |
| `n_period_assignments` | `PeriodAssignment` | **השיוך של אבן דרך לתקופת עבודה - לא עותק שלה.** מזהה המסמך דטרמיניסטי: `${milestoneId}__${periodType}__${periodKey}`, וכך אילוץ "אבן דרך אחת לכל תקופה" נאכף ברמת בסיס הנתונים ולא רק בממשק. שדות: `milestoneId`, `quarterKey` (**הרבעון שבו נעשה השיוך**, לא בהכרח רבעון הבית של אבן הדרך - כך שאילתת שוויון אחת מחזירה את כל שיוכי הלוח), `periodType` (`quarter`/`month`/`week`), `periodKey`, `assignedAt`/`assignedBy`, `outcome` (`open` כל עוד התקופה פתוחה; בסגירת התקופה ננעל ל-`done`/`missed`/`cancelled`), `closedAt` |
| `n_task_activity` | `TaskActivity` | יומן הפעולות של המודול - כל פעולה משמעותית עם מי, מתי, ומה השתנה: `entityType`, `entityId`, `action` (`create`/`update`/`assign`/`unassign`/`status`/`complete`/`reopen`/`cancel`/`move`/`delete`/`restore`), `field`/`oldValue`/`newValue`, `milestoneId`/`quarterKey` (להקשר ולשליפה מהירה), `note` (סיבה), `userName`, `at`. אינו ניתן לעריכה דרך הממשק |
| `n_task_settings` | `TaskSettings` | מסמך יחיד במזהה `default`: `weekStartDay`, `warningWeekday` (מתי מופיעה האזהרה הכתומה) ו-`recommendedRocksPerQuarter`. שבוע העבודה ומועד האזהרה הם **הגדרה ולא קבוע בקוד** |
| `n_rock_reviews` | `RockReview` | סיכום ישיבת רבעון/חודש/שבוע - מזהה דטרמיניסטי `${period}_${periodKey}` (upsert, רשומה אחת לתקופה). בנוסף ל-`notes`: `participants`, `meetingDate` ו-`locked` (ישיבה נעולה = סיכום סופי, עד שנפתחת מחדש במפורש) |
| `n_cost_rates` | `CostRate` | תעריפון עלויות התפעול (`/dashboard/accounting/rates`) - מחשב/תיק/סטיק/סינון וגלישה: `unitCost`, `kind` (`once`/`monthly`), `owedBy` (ברירת מחדל של חלוקת החוב), `qtySource` (מאיפה נגזרת הכמות בסניף). כשהקולקשן ריק מוצגות ברירות המחדל מ-`lib/cost-rates.ts` בזיכרון בלבד עד ששומרים אותן במפורש. **שורות `kind: "once"` (ציוד) הוצאו מהתעריפון** ומסוננות ב-`isRetiredRate` — העלות האמיתית מגיעה מ-`n_items.unitCost`; ראו "מה יצא מהתעריפון" בסעיף 8 |
| `n_branch_cost_settings` | `BranchCostSetting` | דריסה של שורת תעריפון לסניף מסוים - מזהה דטרמיניסטי `${branchId}__${rateKey}` (upsert). כל שדה רשות: `qty`/`unitCost`/`owedBy`/`paidBy`/`enabled` - מה שלא מוגדר נופל חזרה לערך שבתעריפון |
| `n_multi_branch_expenses` | `MultiBranchExpense` | **קולקשן סגור לכתיבה (2026-09).** הוצאה חד-פעמית אחת שמתחלקת בין כמה סניפי השכרות: `amount` (הסכום המלא), `ownerPct` (כמה אחוז ממנה על הבעלים), `branchIds` (בין מי מתחלק השאר, שווה בשווה), `paidBy`, `date`/`month`, `linkedAhExpenseId`. הוא נולד כי `owedBy` יודע לבטא רק בעלים/שותף/50-50 ולא אחוז חופשי — ומאז ש-`ownerPct` יושב על ההוצאה עצמה בספר המשותף אותה שאלה נענית שם, גם בהוצאה קבועה (מה שכאן מעולם לא היה אפשרי), ולכן **טופס היצירה הוסר ואין דרך לכתוב לכאן שורה חדשה**. השורות הקיימות ממשיכות להיפרס לשורת הוצאה פר-סניף בזמן קריאה (`expandExpenseLines`, `lib/branch-accounting-data.ts`) ומוצגות למחיקה בספר המשותף (`legacy-multi-branch.tsx`) |
| `n_ad_areas` | `AdArea` | אזור פרסום - קמפיין אחד המשותף לכמה סניפים באותה עיר (`/dashboard/accounting/ads`): `monthlyCost`, `ownerPct` (חלק הבעלים בקמפיין), `branchIds`/`branchCount` (בין כמה סניפים מתחלק השאר), `startMonth`/`endMonth`, `paidBy`. שורת הפרסום האוטומטית היחידה בסניף (בתעריפון אין שורת פרסום) - אלא אם הסניף הזין פרסום ידנית |
| `n_tx` | `Transaction` | **שכבה 1 — כל שקל שזז, פעם אחת.** היעד של כל תנועה חדשה, ומחליף כמסלול הכתיבה את `n_ah_income`/`n_ah_expenses`/`n_var_expenses`/`n_fixed_expenses`/`n_multi_branch_expenses`/`n_ad_areas`. שדות: `amount` (תמיד הסכום המלא לפני חלוקה), `nature` (`operating`/`capital`/`transfer` — קובע לאיזו שכבה התנועה הולכת), `node` (`business` + `branchId`, כשהערכים `shared`/`hq` הם צמתי-אב לגיטימיים), `ownerShare` (חלק הבעלים **בשקלים**, ולכן אחוז חופשי לא צריך קולקשן משלו), `allocations[]` (פיצול בין סניפים; חייב להסתכם ל-`amount`), `recurring` (`from`/`to` — נפרס לשורות חודשיות בזמן קריאה; **שינוי סכום סוגר תקופה ופותח גרסה חדשה, לעולם לא עורך במקום**), `paidBy`, `purchaseId` (רק ב-`capital`), `doc`, `enteredBy` (מי הזין — מוגדר כשמנהל סניף הזין בעצמו), `reviewedAt`/`flags` (רשימת הסקירה של הבעלים, ראו פרק יד׳). **המסמכים הישנים לא עברו מיגרציה** — הם מוקרנים לאותה צורה בזמן קריאה, ראו `lib/tx-data.ts` |
| `n_purchases` | `Purchase` | **שכבה 2 — חשבונית הספק.** `date`/`supplier`/`invoiceNo`/`total`, `txId` (התנועה ההונית שנוצרה איתה), `lines[]` (`kind`/`label`/`qty`/`unitCost`). אילוץ שנאכף בכתיבה ונבדק שוב במסך בדיקת השלמות: `Σ(qty × unitCost) === total === tx.amount`. יצירת רכישה יוצרת תנועה הונית אחת ופריט לכל יחידה — ולעולם לא הוצאה של סניף |
| `n_items` | `Item` | **שכבה 2 — פריט פיזי בודד.** `kind` (`laptop`/`stick`/`bag`/`sim`/`other`), `unitCost` (**מה שהיחידה הזו באמת עלתה בחשבונית שלה** — השדה שכל המודל נשען עליו), `purchaseId`, `acquiredAt`, `location` (מזהה סניף · `warehouse` · `sold`; שדה חובה — פריט נמצא בדיוק במקום אחד), `status` (`active`/`repair`/`sold`/`writeoff`/`lost` — מצבים סופיים, **פריט אף פעם לא נמחק**), `serial`, `linkedLaptopId`/`linkedStickId`. **שדות היציאה** (פרק יג׳): `soldAt`, `soldPrice` (חלקו של הפריט בתמורה), `saleTxId`, ו-`lastBranchId` — **חובה בכל יציאה**: אחרי מכירה `location` כבר לא מצביע על סניף, ובלי לזכור מאיפה הפריט יצא אי אפשר לייחס את התמורה והחזר ההון של הסניף שגוי לנצח |
| `n_item_moves` | `ItemMove` | **שכבה 2 — פריט עבר ממקום למקום, ומקור האמת למיקום.** `itemId`/`from`/`to`/`date`/`reason` (`allocation`/`return`/`transfer`/`repair`/`writeoff`/`sale`/`branch_closed`/`initial`). **אין כאן שדה סכום, בכוונה, לעולם** — העלות נוסעת עם הפריט, ולכן משלוח לא יכול לייצר כסף. `Item.location` הוא **מטמון בלבד** של התנועה האחרונה: אסור לכתוב אליו בלי לכתוב תנועה מתאימה באותו batch, אחרת כל מספר היסטורי הופך לניחוש (פרק טו׳) |

> הערה: `RentalClient` שומר `cardLast4` (תצוגה בלבד), `cardExpiry` (MM/YY,
> לא רגיש) ו-`gatewayToken` — לעולם לא PAN מלא. שדה נוסף: `referralSource`
> (רשות, טקסט חופשי) — מאיפה הלקוח הגיע אלינו, למעקב שיווקי.
>
> **`collectionRouteId`** (רשות) — ה-`n_collection_routes` שהטוקן נוצר תחתיו, כלומר
> לאיזה עסק/מוסד נדרים פלוס הכרטיס השמור שייך. טוקן תקף רק אצל המוסד שיצר אותו ולכן
> אי אפשר "להעביר" אותו בין עסקים — רק ליצור טוקן חדש (הזנת כרטיס מחדש) תחת מוסד אחר.
> לקוחות שנשמרו לפני שהשדה הזה נוסף אין להם ערך (`undefined`) וממשיכים להיפתר לפי
> מסלול הסניף כמו קודם (ראו `resolveNedarimCreds` בסעיף 9) — כך שכרטיסים ישנים ממשיכים
> להיגבות אוטומטית דרך אותו עסק כמו תמיד. כרטיסים **חדשים** נשמרים כברירת מחדל תחת
> המסלול המסומן `defaultForNewCards: true`, עם אפשרות לבחור מסלול אחר מתוך רשימת
> המסלולים המחוברים בזמן שמירת הכרטיס (`client-card-section.tsx`,
> `complete-cards-queue.tsx`). בכל מסך חיוב (`token-charge-button.tsx`,
> `client-charge-section.tsx`, `active-rental-row.tsx`) מוצג באופן קבוע לאיזה עסק
> החיוב מתבצע ("שים לב: זה גובה לעסק X"), ובדף עריכת לקוח עם כרטיס ישן (לא במסלול
> ברירת המחדל) מוצגת הודעת אזהרה שמציעה להעביר את הלקוח (כלומר לבקש ממנו להזין כרטיס
> מחדש) לעסק הנוכחי כברירת מחדל.

---

## 8. מודולים ופיצ'רים

הבסיס תחת `apps/web/app/dashboard/`. פריטי הניווט הראשיים
(`lib/nav-items.ts`): בית · חדרי מחשבים · השכרות · משרד שיתופי · הנה"ח · משימות
ונהלים · חנות AI · הדרכות.

### חדרי מחשבים (`/dashboard/computer-rooms`) — perm: branches/computers/tasks
מכשירים (`n_devices`), מדפסות, ניהול סניפים (`/branches`), **תפעול** (`/operations`),
הוצאות (`/expenses`) והשקעה-מול-רווח (`/computer-rooms-accounting`).

#### תפעול חדרי מחשבים (`/dashboard/operations`) — perm: `computers`

מודול אחד שהחליף ארבעה מודלים ישנים ומיותרים (מלאי, משימות, פניות, עדכונים).
`/tickets` ו-`/news` בוטלו לגמרי; `/orders` יצא מכאן ויוקם מחדש כמודול עצמאי
בניווט הראשי. הקולקשנים הישנים (`inventory`, `tasks`, `tickets`, `news`
ב-Firestore, בלי תחילית `n_`) לא נמחקו — הם פשוט כבר לא נקראים.

שלוש לשוניות (`operations-tabs.tsx`):

- **עדכון מלאי** (`/operations/stock`) — **לא הזנת כמויות**. הבעלים מגדיר מראש
  בהגדרות מה הפריטים וכמה אמורים להיות מכל אחד, והסניף מסמן מול כל פריט **V**
  (יש) או **X** (חסר) בלבד, עם שדה הערה חופשי כשסומן X. הרשימה **מתאפסת בכל
  ראשון לחודש** — לא ע"י job, אלא מפני שהסימונים יושבים על מסמך לפי
  `${branchId}__${YYYY-MM}`, וחודש חדש מתחיל ממסמך ריק.
- **משימות** (`/operations/tasks`) — משימות **שבועיות** ו**חודשיות** בשתי רשימות
  נפרדות, סימון V על ביצוע. שבועית מתאפסת בכל שבוע ISO, חודשית בכל ראשון לחודש,
  באותו מנגנון מפתח-תקופה.
- **הגדרות תפעול** (`/operations/settings`, **owner בלבד**, `requireOwner()`) —
  יצירה/עריכה/מחיקה של פריטי המלאי ושל המשימות. לכל הגדרה יש `scope`:
  **"לכל הסניפים"** או **"סניפים נבחרים"**. זו הנקודה שמונעת עבודה כפולה —
  פריט או משימה "לכולם" נוצרים פעם אחת ומופיעים בכל הסניפים, בלי לשכפל רשומה
  לכל סניף.

**הפרדת סניפים.** `getOpsAccess()` (`lib/operations.ts`) מחזירה את הסניפים
שהמשתמש רשאי לראות: בעלים — כל סניפי `branchType === "computers"`; כל אחד אחר —
`n_users.branchId` שלו בלבד. כל Server Action שמסמן משהו קורא
`assertBranchAllowed()`, כך שההפרדה נאכפת בשרת ולא רק בתצוגה. (נכון להיום עדיין
לא נפתחו למנהלי הסניפים הרשאות בפועל — התשתית מוכנה מראש.)

בעלים רואה בנוסף שני סיכומים חוצי-סניפים: **"מצב הסניפים החודש"** במסך המלאי
(מה סומן X בכל סניף וכמה פריטים טרם סומנו) ו**"התקדמות הסניפים"** במסך
המשימות. חוסרי המלאי של החודש הנוכחי מוצגים גם בכרטיס בדף הבית.

**פיצול לקוח/שרת.** `lib/operations.ts` מייבא `firebase-admin`, ולכן כל העזרים
הטהורים (מפתחות תקופה, `formatPeriod`, `appliesToBranch`, הטיפוס `OpsBranch`)
יושבים ב-`lib/operations-shared.ts`. קומפוננטות `"use client"` מייבאות **משם
בלבד** — ייבוא מ-`lib/operations.ts` גורר את `firebase-admin` לבאנדל הדפדפן ושובר
את ה-build.

**מה מתוכנן ועוד לא מומש:** דיוור שבועי אוטומטי לסניפים, וקו טלפוני ממומשק
שיאפשר עדכון מלאי בלי להיכנס לאתר.

- **`/expenses`** (perm: `computers`) — הוצאות קבועות/חד-פעמיות פר סניף חדר מחשבים
  (`n_fixed_expenses` / `n_var_expenses`, אותו מודל בדיוק כמו ב-`/dashboard/rentals/expenses`).
  לכל הוצאה (קבועה או חד-פעמית) יש כפתור **עריכה** הפותח מודל לעריכת כל השדות - שם/תיאור,
  סכום, תאריך, קטגוריה, ו"מי שילם בפועל"/"על מי החוב" עבור סניפי שותף
  (`updateFixedExpenseAction` / `updateVariableExpenseAction` ב-`actions.ts`). `canManage`
  (עריכה/מחיקה/סיום) פתוח ל-owner **וגם** לשותף בסניף שלו עצמו (`isOwner || isPartner` ב-
  `[id]/page.tsx`) - לא רק ל-owner כמו קודם; ה-Server Actions עצמם אוכפים זאת דרך
  `requireBranchAccess(branchId)` וגם מוודאים ש-`id` שהתקבל שייך בפועל לאותו `branchId`
  (`loadOwnedFixedExpense`/`loadOwnedVariableExpense`) כדי שרשות פעולה על הסניף שלך לא תאפשר
  למחוק/לערוך הוצאה של סניף אחר.
  עריכת הוצאה חד-פעמית כבר לא צריכה לתחזק שום עותק בספר האישי — חלק הבעלים נגזר מהתנועה עצמה
  (ראו "מודל הקריאה המאוחד"). הוצאה ישנה שעדיין נושאת `linkedAhExpenseId` מוחקת אותו יחד איתה,
  כדי שלא יישאר רפאים. פעולות עריכה/מחיקה/סיום (`expense-action-buttons.tsx`,
  `edit-expense-modals.tsx`) קוראות ל-Server Action ישירות מהלקוח (לא דרך `<form action>` פשוט),
  ומריצות `router.refresh()` + הודעת "בוצע בהצלחה" (`lib/toast.tsx`) בסיום - כדי למנוע את
  התופעה שבה `redirect()` לאותו נתיב לא רענן בפועל את הרשימה בלי רענון ידני. בנוסף קיים
  "סניף" מדומה **`shared-computers`** (`SHARED_COMPUTERS_BRANCH_ID`, מיוצא גם בתור
  `SHARED_EXPENSE_BRANCH_ID` מ-`apps/web/lib/computer-room-accounting.ts` לתאימות; המקבילה
  בניידים היא `SHARED_RENTALS_BRANCH_ID`, שתיהן מוגדרות ב-`apps/web/lib/expense-shared-scope.ts`)
  עבור הוצאות המשותפות לסניפי חדרי המחשבים יחד (למשל פרסום/רישיונות משותפים) - עלותן
  מתחלקת שווה בשווה בין הסניפים שההוצאה חלה עליהם בחישוב "הוצאות עד היום" בדשבורד
  ההשקעה-מול-רווח (`/computer-rooms-accounting`, ראו להלן), אך **לא** מתחלקת כשהיא מתחשבנת
  בהנה"ח הראשית (שם היא נספרת פעם אחת במלואה, ראו סעיף 10). מסך הבחירה (`/dashboard/expenses`)
  מציג גם קישור ייעודי להוצאות המשותפות. שדות "מי שילם בפועל"/"על מי החוב" (`paidBy`/`owedBy`) מוצגים עם
  שמות אמיתיים - לא "אני"/"השותף" - דרך `apps/web/lib/owner-name.ts`; זה כולל גם את הסניף
  המדומה המשותף, שקודם לא איפשר לבחור כלל מי שילם.

  **שינויים (2026-09):**
  - לכל הוצאה (קבועה, חד-פעמית, על כמה סניפים) יש כעת צ'קבוקס **`countsToMain`** —
    `CountsToMainField` — שקובע אם היא נכנסת לספר הראשי. אותה קוביה בדיוק מופיעה בכל טופס
    הוצאה בעסק. הוצאה משותפת (`shared-computers`) שסומנה נספרת בראשי פעם אחת במלואה; לא
    סומנה — היא מתחלקת בין סניפי חדרי המחשבים בדשבורד ההשקעה-מול-רווח בלבד.
  - **מסך בחירת הסניף הוא טבלה, לא קוביות.** כרטיס "הוצאות משותפות" הגדול שהיה בראש הוסר
    (הקישור נשאר כטקסט ליד הכותרת), ורשימת הסניפים הפכה ל-`BranchExpenseTable`
    (`apps/web/components/expenses/branch-expense-table.tsx`) עם שתי עמודות שלא היו קודם:
    **סה"כ עד היום** ו**מזה לראשי**. אותה כמות מידע בשליש מהגובה.
  - **הוצאות על כמה סניפים** — המודל תומך גם בחדרי מחשבים (`MultiBranchExpense.module` הורחב
    ל-`"rentals" | "computers" | "coworking"`, והטופס והפעולות מקבלים את המודול כפרמטר), אבל
    **בעמוד האינדקס של הוצאות חדרי מחשבים הבלוק התחתון הוסר** — גם טופס ההוצאה על כמה סניפים
    וגם רשימת ההוצאות שנרשמו כך. נשארה דרך אחת בלבד להוצאה שחלה על יותר מסניף אחד: הספר
    המשותף שבראש המסך (`shared-computers`), שבו אפשר לבחור לכל הוצאה על אילו סניפים היא חלה.
    רשומות `n_multi_branch_expenses` קיימות עם `module: "computers"` ממשיכות להתפרס לספרי
    הסניפים כרגיל (`expandExpenseLines`) — רק נקודת הקלט ב-UI ירדה. הטופס עצמו
    (`multi-branch-form.tsx`) נשאר בשימוש במודול ההשכרות.
  - כרטיס **הוצאות קבועות משתנות** (`scope: "computers"`) מופיע בעמוד כל סניף.
  - **בחירת הסניפים של הוצאה משותפת + איתור ההוצאות המשותפות.** עד כה הוצאה בספר המשותף
    (`shared-computers`) התחלקה תמיד בין **כל** סניפי המודול, כך שסניף שנפתח היום ירש מיד
    הוצאה קבועה שנרשמה הרבה לפניו ולא נגעה אליו, ולא הייתה דרך להגיע מהסניף אל ההוצאה
    שיצרה את המספר. מה שהשתנה:
    - `FixedExpense.branchIds` / `VariableExpense.branchIds` (רשות): רשימת הסניפים שהוצאה
      משותפת מתחלקת ביניהם. **שדה חסר או ריק = כל הסניפים, כולל עתידיים** - ההתנהגות
      ההיסטורית, ולכן שום מסמך קיים לא משנה משמעות. הלוגיקה כולה ב-
      `apps/web/lib/expense-shared-scope.ts` (`sharedExpenseBranchIds`,
      `sharedExpenseAppliesTo`, `sharedExpenseScopeLabel`, `sharedExpenseBranchIdsFromForm`),
      והרשימה תמיד מוצלבת מול הסניפים הקיימים כדי שסניף מחוק לא ייכנס למחלק.
    - הטופס: `SharedBranchScopeField`
      (`apps/web/components/expenses/shared-branch-scope-field.tsx`) - "כל הסניפים" מול
      "סניפים נבחרים" - מופיע בטופסי ההוספה ובמודלי העריכה של הספר המשותף בלבד. הטופס שולח
      `branchScope` + `branchIds`, ו-Server Action כותב את השדה **רק** כש-`branchId` הוא
      הסנטינל המשותף; חזרה ל"כל הסניפים" מוחקת את השדה (`FieldValue.delete()`).
    - החישוב (`lib/computer-room-accounting.ts`): כל הוצאה משותפת מתחלקת ב**מחלק שלה**
      (`SharedExpenseEntry`) ולא בסך כל הסניפים, והפירוט בדשבורד ההשקעה-מול-רווח מציג
      **שורה נפרדת לכל הוצאה משותפת** במקום שורת "חלק הסניף בהוצאות המשותפות" אחת.
    - איתור: הספר המשותף מופיע כשורה בטבלת `BranchExpenseTable` עם המספרים שלו
      (`sharedRow`, נספר גם בשורת הסה"כ), בעמוד כל סניף יש כרטיס **"הוצאות משותפות שחלות גם
      על הסניף הזה"** (`shared-expenses-for-branch.tsx`) עם המחלק והחלק של הסניף, ומדשבורד
      ההנה"ח יש קישור לספר המשותף (הקישור שם הצביע קודם על `/dashboard/expenses/shared` -
      מזהה שלא קיים - ותוקן לסנטינל האמיתי).
- **`/computer-rooms-accounting`** (perm: `computers`) — דשבורד "השקעה מול רווח" פר סניף חדר
  מחשבים: **עלות הקמה** (`Branch.setupCost`), **הוצאות עד היום כולל הקמה** (הקמה + הוצאות
  הסניף + חלקו בהוצאות המשותפות), **הכנסות עד היום**, ו**רווח מוחזק** (הכנסות פחות הוצאות).
  **מסך הבחירה של owner הוא טבלה** ולא קובייה לכל סניף: השאלה שם היא השוואה בין הסניפים
  ("מי מחזיר ומי לא"), והקוביות הכריחו לקרוא כל סניף בנפרד ולזכור את המספר של הקודם. ארבע
  קוביות הסה"כ נשארו מעליה.
  **ההשקעה נקראת משכבת הנכסים** כשקיימות לסניף רכישות (Σ `unitCost` של הפריטים שבו), ונופלת
  ל-`Branch.setupCost` רק כשאין; המסך מסמן מאיזה מקור הגיע המספר (`setupFromAssets`) ומוסיף
  `payback` — כמה מההשקעה כבר חזרה. עלות ההקמה מוצגת **כאן בלבד**: היא לא נכנסת לספר הראשי
  אלא אם ההוצאה שיצרה אותה סומנה `countsToMain` ככל הוצאה אחרת.
  **פירוט "ממה מורכבות ההוצאות עד היום"** מוצג במסך הסניף (`stats.expenseLines`): שורה לכל
  הוצאה קבועה עם החשבון שהביא אליה (`X לחודש × N חודשים`), שורה לכל הוצאה חד-פעמית, ושורה
  לחלק הסניף בהוצאות המשותפות עם המחלק המפורש. זו הייתה השאלה הנפוצה ביותר על המסך: הסכום
  הוא חישוב חי ולא נתון שמור, והוצאה קבועה מכפילה את עצמה בכל חודש שעובר בלי שנוגעים בה.
  **סניפים מחוקים מסוננים גם כאן** — קודם הם נספרו במחלק של ההוצאות המשותפות בזמן שמסך
  ההוצאות סינן אותם, ואותה הוצאה משותפת הוצגה בשני המסכים בשני סכומים שונים.
  **ההכנסות כאן מגיעות משני מקורות** (`RoomIncomeLine.source` ב-`lib/computer-room-accounting.ts`),
  ושתיהן מוצגות באותה רשימה ונספרות ב"הכנסות עד היום" של המסך:
  - `manual` — שורה שמוזנת כאן (`n_branch_income`, `saveMonthlyBranchIncomeAction`): **מעקב
    סטטוס בלבד**, לא נכתבת ל-`n_ah_income` ולכן לא מתחשבנת בהנה"ח הראשית ולא בדף הבית.
  - `main-cash` — **מזומן שנמשך מקופת הסניף ונרשם בהנה"ח הראשית** (`n_ah_income` מסוג `cash`
    עם ה-`branchId` של הקופה). הכיוון הוא **קריאה בלבד**: המסך הזה קורא את השורה ומציג אותה,
    ולעולם לא כותב חזרה. אותו שקל ממשיך להיספר פעם אחת בלבד — בספר הראשי — וכאן הוא רק
    *מוצג*, כדי שסניף שכל הכנסתו מזומן לא ייראה כאילו לא הכניס כלום. השורה מסומנת "מזומן
    מהקופה · מההנה"ח הראשית", **אין עליה כפתור מחיקה** (מוחקים אותה במקום שבו נרשמה, עם
    קישור לספר הראשי ל-owner), והכרטיס למעלה מפרק את הסכום לשני המקורות כשקיימים שניהם.

  טופס ההזנה **עדיין לא מציע אמצעי תשלום ולא "מזומן"** — זה בדיוק מה שמונע את הכפילות:
  אין צורך להקליד כאן מזומן, הוא מגיע לבדו. `createIncomeAction`/`deleteIncomeAction`
  (`/dashboard/accounting`) מרעננות גם את `/dashboard/computer-rooms-accounting` ואת עמוד
  הסניף, כדי שהשורה תופיע מיד ולא בטעינה הבאה שתעקוף את המטמון.

  **טבלת "הכנסות לפי חודש"** (`summarizeByMonth` בעמוד הסניף) מציגה שורה אחת מסכמת לכל חודש,
  משני המקורות יחד, מהחודש האחרון לראשון — זו התמונה שמחפשים במסך הזה ("איפה הסניף אוחז"),
  והרשימה המפורטת שורה-שורה נשארת מתחתיה.

  **מבנה עמוד הסניף** (`[id]/page.tsx`) — תמונת מצב למעלה, פירוט רק לפי דרישה:
  - **ארבע קוביות בגובה אחיד** ובלי פירוט בתוכן. פירוט עלות ההקמה עבר לחלון קטן שנפתח
    בלחיצה על הקובייה (`setup-cost-card.tsx`, `stats.setupBreakdown`): שורות
    `Branch.setupItems` כשהמספר הגיע מהשדה, או הפריטים שנקנו לפי סוג (`ITEM_KIND_LABEL`,
    כמות ו-Σ `unitCost`) כשהוא נקרא משכבת הנכסים — הפירוט תמיד מאותו מקור שממנו הגיע המספר
    עצמו. קודם הוא ישב בתוך הקובייה, וסניף עם עשר שורות הקמה מתח את כל שורת הקוביות לגובה
    שדחף את שאר המסך מטה.
  - **גרף 12 החודשים** (`monthly-flow-chart.tsx` מעל `stats.monthlyFlow`) — עמודת הוצאות
    ועמודת הכנסות לכל חודש, עם ניווט אחורה/קדימה בקפיצות של הטווח הנבחר (6 / 12 / 24 חודשים)
    וכיבוי-הדלקה של כל סדרה (רווח כבוי כברירת מחדל). ציר הזמן מצויר `dir="ltr"` — הישן בשמאל
    והחדש בימין — גם במסך RTL. **עלות ההקמה לא נכנסת לגרף** בכוונה: היא נקודה אחת בזמן, והיא
    הייתה מוחצת את כל שאר העמודים לגובה אפס.
  - **שלושה טורים מתקפלים** מתחת לגרף, סגורים כברירת מחדל: "ממה מורכבות ההוצאות", "הכנסות לפי
    חודש" ו"שורות הכנסה". כל אחד נפתח בתוך השליש שלו (`items-start` ב-grid) ולא דוחף את
    שכניו, וכולם מוצגים כטבלאות. הפעולות נשארו בדיוק כשהיו — מחיקת שורה ידנית ל-owner,
    קישור לספר הראשי על שורת מזומן, וקישורי ניהול ההוצאות.
  - **טופס ההזנה והייבוא הם כפתור אחד בראש העמוד**, ליד "בחירת סניף אחר"
    (`income-entry-panel.tsx`), שפותח חלון עם הטופס וכלי הייבוא. קודם הם היו פרוסים בתחתית
    העמוד, מתחת לכל הפירוט, למרות שזו פעולה שעושים אחת לחודש.

  **טופס ההזנה הוא חודש אחד לכל הסניפים** (`saveMonthlyBranchIncomeAction`): בוחרים חודש
  למעלה, ומתחתיו שורה לכל סניף עם הסכום שלו. זו הצורה שבה המספרים באמת מגיעים — פעם בחודש,
  לכל הסניפים יחד; הטופס שקדם לו היה שורה בודדת (תאריך + תיאור + סכום) לסניף אחד בכל פעם,
  וכדי לרשום חודש שלם היה צריך לעבור סניף-סניף ולפתוח אותו מחדש בכל אחד. לכן אותו כפתור יושב
  גם במסך בחירת הסניף — שם owner מתחיל ממילא. `branchMonthlyIncomeRows()` מזין אותו:
  - כל שדה **מגיע מלא במה שכבר רשום** לאותו סניף באותו חודש, ולצידו (לתצוגה בלבד) המזומן
    מהקופה של אותו חודש — כדי שלא יוקלד שוב.
  - **שדה ריק = לא נוגעים בסניף הזה**, מספר **מחליף** את שורות המעקב של אותו סניף+חודש
    (אידמפוטנטי: הקלדה חוזרת מתקנת ולא מכפילה, והמסך מראה מספר אחד לחודש), ו-**0 מוחק** את
    החודש. המזומן מהספר הראשי לא נמחק ולא נכתב מכאן לעולם.
  - ההרשאה נבדקת **לכל סניף בנפרד ב-Server Action** (`requireBranchAccess` +
    `requireComputerRoomBranch`), ולא רק ברשימה שהמסך שלח: owner מזין לכל הסניפים, שותף רק
    לסניף שלו. גם היעד לחזרה (`back`) מסונן לנתיבי המודול בלבד.

  **ייבוא הכנסות חודשיות מאקסל (כלי זמני למילוי היסטוריה)** —
  `importBranchIncomeAction` + `lib/branch-income-excel.ts`, עם תבנית להורדה ב-
  `/api/computer-rooms-accounting/[id]/income-template`:
  - הקובץ הוא שתי עמודות בלבד: **חודש** (`10/24`) ולצידו **סכום**, שורה אחת לכל חודש.
    התבנית נכתבת RTL (`wb.Workbook.Views[0].RTL` — ההגדרה היא ברמת החוברת; `ws["!views"]`
    המקביל פשוט לא נכתב לקובץ), ולכן עמודת החודש היא באמת הימנית.
  - **עמודת החודש מוגדרת מראש כטקסט** (תאים ריקים עם `z: "@"`): בלי זה אקסל היה הופך `10/24`
    ל-24 באוקטובר של השנה הנוכחית, והשנה 2024 הייתה אובדת בשקט. הפרסר בכל זאת מקבל תא-תאריך
    אמיתי, אבל מחזיר עליו **אזהרה** שמוצגת למשתמש ("נקרא כ-10/2024 — ודא שזה החודש הנכון").
  - הפרסר סובלני בכוונה: מזהה את עמודות החודש והסכום לפי הכותרות *או* לפי התוכן (כולל סדר
    עמודות הפוך וקובץ בלי כותרות כלל), מקבל `10/24` / `10/2024` / `2024-10`, סכום עם פסיקים
    או ₪, מדלג על שורות ריקות, מחבר שתי שורות של אותו חודש לשורה אחת, ומחזיר לכל שורה שדולגה
    הודעה עם מספר השורה.
  - **הייבוא אידמפוטנטי לפי חודש**: לפני כתיבת חודש נמחקות שורות ה-`source === "import"`
    הקודמות של אותו חודש בלבד. העלאה חוזרת מתקנת ולא מכפילה, ושורה שהוקלדה ידנית לאותו חודש
    שורדת. `clearImportedBranchIncomeAction` (owner) מוחק בבת אחת את כל שורות הייבוא של
    הסניף, גם כאן בלי לגעת בשורות שהוקלדו.
  - **גם הייבוא הוא מעקב פנימי בלבד** — הוא כותב ל-`n_branch_income` ולעולם לא ל-`n_ah_income`,
    בדיוק כמו הטופס הידני שלצידו.
  - **כיבוי הכלי**: `BRANCH_INCOME_IMPORT=off` בסביבה מסתיר את הכפתורים *וגם* מסרב ב-Server
    Action וב-API של התבנית, כדי שלא יישאר נתיב פתוח אחרי שהכפתור נעלם מהמסך. זו התוספת
    היחידה במסך שנועדה להיעלם אחרי שמילוי ההיסטוריה יסתיים.

  owner רואה סקירת כל הסניפים ונכנס לכל סניף בנפרד; partner/עובד עם הרשאת `computers` רואה
  ישירות את הסניף שלו. הזנת הכנסות חודשיות וייבוא מאקסל מותרים ל-owner ול-partner של אותו
  סניף בלבד (אותה בדיקה בדיוק ב-Action, ב-API של התבנית ובמסך); מחיקת שורות — owner בלבד.
- **`/branches` + `/branches/[id]` + `/branches/new`** (owner בלבד לעריכה) — טופס הסניף
  (`branch-form.tsx`, `branches/actions.ts`). **עלות ההקמה אינה שדה מספר בודד**: הקמת חדר היא
  מחשבים + ריהוט + חשמל + שילוט, ולכן `SetupCostField` (`branches/setup-cost-field.tsx`) מציג
  את הסכום ולידו כפתור **"פירוט ההוצאות"** שפותח חלון צד שמאלי — שם מוסיפים שורה לכל הוצאה
  (תיאור + סכום), מוחקים שורות, ורואים סה"כ רץ. **הסכום לעולם לא מוקלד** אלא תמיד מחושב כסכום
  השורות, גם בצד הלקוח וגם שוב ב-`parseBranchForm` בשרת, כך ש-`setupCost` ו-`setupItems` לא
  יכולים לסתור זה את זה. חדר ותיק שיש לו רק `setupCost` בלי פירוט מקבל את המספר כשורה ראשונה
  ("עלות הקמה") ברגע שפותחים את החלון, כדי שלא ייעלם. `setupCost` נשאר המספר שכל שאר המערכת
  קוראת (תחזית ההון ב-`tx-data.ts`, `computer-room-accounting.ts`) — הפירוט נוסף לצידו ולא
  במקומו. בתחתית חלון הפירוט יש את הצ'קבוקס **"לחשבן בהנה"ח הראשית"**, שקובע אם עלות ההקמה
  של הסניף נכנסת לספר הראשי (`setupCountsToMain`; **דלוק כברירת מחדל** — ראו סעיף ההנה"ח
  הראשית). הרכיב משותף לחדרי מחשבים ולמשרד השיתופי (`apps/web/components/setup-cost-field.tsx`).

### השכרות (`/dashboard/rentals`) — perm: rentals
- **`/`** — סקירת השכרות (טאבים: `rentals-tabs.tsx`).
- **`/new`** — יצירת השכרה חדשה (`new-rental-form.tsx`). בחירת הלקוח מתבצעת דרך
  `CustomerCombobox` (`customer-combobox.tsx`) — תיבת חיפוש-והשלמה שמסננת בזמן
  הקלדה לפי שם/טלפון מתוך לקוחות הסניף שנבחר (סינון בצד הלקוח על רשימה שכבר
  נטענה), ולא רשימה נפתחת מלאה; בחירת סניף מאפסת את הלקוח שנבחר. מכיוון
  שהשכרה נשארת `status: "active"` ללא תאריך סיום קבוע עד להחזרה מפורשת
  (`markReturnedAction`/`closeRentalAction`), לא ניתן לפתוח השכרה נוספת על אותו
  פריט (מחשב/סטיק) כל עוד יש לו כבר השכרה פעילה: הדף טוען את רשימת ה-`itemId`
  המושכרים כרגע (`n_rentals` עם `status == "active"`) ומעביר אותה ל-`NewRentalForm`,
  שמנטרלת ומסמנת "מושכר כרגע" את האפשרויות המתאימות בתיבת הבחירה ומציגה הודעת
  אזהרה; `createRentalAction` (`../actions.ts`) חוסם את היצירה גם בצד השרת
  (`error=already-rented`) למקרה מרוץ שבו שני משתמשים מנסים להשכיר את אותו
  פריט בו-זמנית. **סטיק "תפוס" מכללת מחשב** - השכרת מחשב עם `pricingVariant`
  שאינו `"noInternet"` (ברירת המחדל) יוצאת ללקוח יחד עם הסטיק המשוייך אליו
  (`n_sticks.linkedLaptopId`), אז הוא לא באמת פנוי להשכרה נפרדת אף שאין לו
  רשומת `n_rentals` משלו - גם רשימת ה-`itemId` התפוסים בעמוד וגם הבדיקה
  בצד השרת ב-`createRentalAction` מתייחסות אליו ככזה. רק השכרת מחשב עם
  `pricingVariant === "noInternet"` (או השכרת הסטיק בעצמו) לא "תופסת" את הסטיק.
  כל פעולת שינוי במודול ההשכרות (`../actions.ts`,
  `laptops/actions.ts`) קוראת ל-`revalidatePath("/dashboard/rentals", "layout")` (ולא רק לנתיב
  הספציפי) - כדי שלקוח/מחשב/סטיק שנוצר עכשיו יופיע מיידית גם ב-`/new`, לא רק בעמוד שבו נוצר;
  בלי זה ה-router cache הצד-לקוח של Next יכול להציג רשימת לקוחות מיושנת ב-`/new` (הבאג של
  "לקוח לא קיים" בניסיון הראשון, שנפתר בפעם השנייה אחרי ניווט חוזר). בנוסף, `NewRentalForm`
  (`new-rental-form.tsx`) כבר לא משתמש ב-`<form action={createRentalAction}>` הטבעי - השליחה
  עוברת דרך `onSubmit` שבונה את ה-`FormData` ישירות מה-state של הקומפוננטה (לא סומך על ה-DOM/
  hidden inputs שה-form אוסף בזמן ה-submit) ומריצה את ה-Server Action דרך `startTransition`;
  שדות חסרים (לקוח/פריט/תאריך) מוצגים כהודעת שגיאה inline מיידית בלי לאבד את שאר הטופס, במקום
  redirect מלא ל-`?error=missing` שהיה מרוקן את כל מה שכבר מולא - זה מה שגרם לתחושת "עובד רק
  בפעם השנייה" גם אחרי שהלקוח כבר נבחר בפועל.
- **`/laptops`** — ניהול מחשבים ניידים + תמחור (CRUD). **המחירים בטופס המחשב הם חריגה
  אופציונלית ממחירון הסניף** (`/pricing`): שדה שנשאר ריק יורש את מחיר הסניף, וה-placeholder
  מציג מה ייגזר ("ברירת מחדל: 50"). מחירון המחשב הוא טבלה של שתי עמודות:
  **עם סטיק (אינטרנט)** = `dayPrice`/`weekPrice`/`monthPrice`, ו**בלי סטיק** =
  `noInternetDayPrice`/`noInternetWeekPrice`/`noInternetMonthPrice`. עמודת "בלי סטיק" היא רשות
  ושדה ריק בה נופל חזרה למחיר "עם סטיק" של אותה מדרגה (`laptopRatesFor`); `altPricing` כבר לא
  תיבת סימון אלא נגזר בשרת - true אם מולא בעמודה הזו מחיר כלשהו. בהשכרה חדשה בוחרים "עם סטיק"/
  "בלי סטיק" (`pricingVariant`), ו"בלי סטיק" גם משאיר את הסטיק פנוי להשכרה נפרדת.
  כשמסמנים "יש סטיק משוייך למחשב זה"
  (`hasStick`), טופס המחשב (`laptop-form.tsx`) חושף גם שדות תמחור סטיק: יום ראשון/יום
  שני/מהיום השלישי ואילך (`stickDay1`/`stickDay2`/`stickDay3plus`) **ובנוסף מחיר שבוע/חודש
  לסטיק** (`stickWeekPrice`/`stickMonthPrice` → `weekPrice`/`monthPrice` ב-`n_sticks`).
  "יום שני" ריק = כמו המחיר היומי השוטף, כך שתמחור "יום ראשון 20, מהיום השני 10" מוזן בשני
  שדות בלבד. בשמירה (`createLaptopAction`/
  `updateLaptopAction`, `../actions.ts`) הפעולה מסנכרנת אוטומטית רשומת `n_sticks` תואמת
  (`syncLinkedStick`) - יוצרת אותה בפעם הראשונה או מעדכנת רשומה קיימת לפי `linkedLaptopId`, בשם
  נגזר ממספר המחשב (למשל מחשב "מחשב 20" → סטיק "סטיק 20"). זה מה שבפועל הופך את "השכרת סטיק"
  ב-`/new` לעובדת: `n_sticks` לא נוצר משום מקום אחר, אז בלי הסינכרון הזה רשימת הסטיקים
  להשכרה תמיד ריקה. הפעולה **לא הורסת** סטיק קיים כשמכבים את `hasStick` - רק מפסיקה
  לעדכן אותו, כדי לא לפגוע בהיסטוריית השכרות/השכרה פעילה קיימת שלו. כפתור owner-only
  "יצירת סטיקים לכל המחשבים" בראש `/laptops` (`syncAllSticksAction`) הוא backfill אידמפוטני
  שרץ על **כל** המחשבים (לא רק אלה עם `hasStick` מסומן) ויוצר סטיק (במחיר 0 עד עריכה) לכל מחשב
  שעדיין אין לו רשומת סטיק מקושרת, ומדליק את `hasStick` עליו אם עוד לא היה מסומן - כדי שכל
  מחשב יהיה זמין להשכרת סטיק ב-`/new` גם אם לא הוגדרו לו מחירים ידנית.
  owner יכול גם לסמן שותפות פר-מחשב (`hasPartner`/`partnerName`/`partnerPct`, ברירת מחדל 15%) -
  ראו `computePartnerSettlement` בהמשך. תצוגת "מלאי מחשבים" (`laptops-list.tsx`) היא טבלה
  (שם/סניף/מחירים/סטיק/שותפות/פעולות), עם אותם בקרי סינון-לפי-סניף ומיון שהיו קודם.
- **`/clients`** — לקוחות: מסך הרשימה נפתח ללא טופס הוספה גלוי — כפתור
  "הוספת לקוח" בפינה השמאלית העליונה (`clients-header.tsx`, קומפוננטת
  `use client` שמכילה גם את כותרת העמוד) פותח/סוגר את `client-form.tsx`
  בתצוגה מקומית; לאחר שמירה מוצלחת ה-Server Action מבצע `redirect` חזרה
  ל-`/dashboard/rentals/clients` והטופס חוזר סגור כברירת מחדל. בכשל ולידציה
  (`?error=missing`) הטופס נפתח אוטומטית כדי להציג את השגיאה בהקשר. הטופס
  כולל שדה רשות "מאיפה הגיע אלינו" (`referralSource`, טקסט חופשי) למעקב
  שיווקי. כרטיס לקוח, לכידת כרטיס אשראי וטוקניזציה
  (`nedarim-card-capture.tsx`), חיוב (`client-charge-section.tsx`,
  `nedarim-charge-capture.tsx`), ייצוא/ייבוא אקסל. טבלת הלקוחות
  (`clients-table.tsx`) כוללת כפתורי סינון "הצג את שלי" (הסניף של המשתמש +
  סניפים עם `parentBranchId` שווה לסניף שלו) מול "הצג את של כולם" (כל הלקוחות
  שהמשתמש מורשה לראות לפי ההרשאות הקיימות) - **כפתור "הצג את של כולם" מוצג רק
  ל-`owner`** (`isOwner` מועבר מ-`clients/page.tsx`); partner/employee תמיד
  רואים רק את "שלי" (`myScopeBranchIds`), ללא אפשרות מעבר, כי אין להם ממילא
  שימוש בהרחבה הזו. וכן שדה חיפוש חופשי לפי שם/טלפון
  (סינון בזמן הקלדה על הרשימה שכבר נטענה בצד הלקוח, ללא שאילתה נוספת ל-Firestore)
  לאיתור מהיר של לקוח קיים מתוך רשימה גדולה — למשל כדי לבדוק אם לקוח כבר קיים
  לפני יצירת אחד חדש, או כדי לגשת מהר לכפתור "חייב" שלו. לכל לקוח עם טוקן כרטיס שמור
  (`gatewayToken` + `cardExpiry`) **שנפתר לו מסלול גבייה תקין** (לפי `collectionRouteId`
  שלו, או מסלול הסניף ללקוחות ישנים ללא ערך) מוצג בשורה כפתור "חייב" (מוצג רק ל-`owner`
  או למי שיש לו `perms.charging`), שפותח את `TokenChargeButton` הקיים
  (`../manage/token-charge-button.tsx`) וקורא ל-`/api/rentals/charge` לחיוב
  ישיר דרך הטוקן השמור, ללא הזנת פרטי כרטיס מחדש — עם תווית קבועה מעל הכפתור
  "שים לב: זה גובה לעסק X" (שם המסלול שנפתר). בדף עריכת לקוח בודד
  (`/clients/[id]`) מוצג כפתור "מחק לקוח" (`delete-client-button.tsx`) ל-`owner`
  ול-`partner` בלבד (`deleteClientAction` ב-`../actions.ts` חוסם גם בצד השרת
  תפקידים אחרים ומפנה ל-`?error=forbidden`). באותו דף, `ClientCardSection` מציגה
  לאיזה עסק כרטיס קיים משויך, ואם הוא לא במסלול `defaultForNewCards` הנוכחי — הודעת
  אזהרה שמסבירה שצריך להזין כרטיס מחדש כדי "להעביר" את הלקוח (טוקן קיים לא ניתן
  להעברה בין עסקים); בעת הזנת/עדכון כרטיס יש בורר לבחירת העסק שאליו ישויך הכרטיס
  החדש (ברירת מחדל: המסלול המסומן `defaultForNewCards`). `ClientChargeSection`
  (חיוב מיידי ללא טוקן) כולל אותו בורר עסק ותווית "שים לב: זה גובה לעסק X".
- **`/clients/complete-cards`** — מסך עזר להשלמת טוקניזציה בכמות: מציג בתור
  את כל הלקוחות בהיקף המשתמש שאין להם `cardLast4` שמור, עם `NedarimCardCapture`
  פתוח לכל לקוח בתורו (מתקדם אוטומטית ללקוח הבא אחרי שמירה מוצלחת, ניתן גם
  לדלג), ומציג לאיזה עסק הכרטיס ישמר. מטרתו לאפשר מעבר מהיר בין הרבה לקוחות בלי
  לנווט לדף עריכה נפרד לכל אחד — הכרטיס עדיין מוקלד תמיד ישירות באייפרם המאובטח
  של נדרים פלוס, אף פעם לא דרך שרת האפליקציה. כל כרטיס חדש שנשמר כאן משויך
  למסלול המסומן `defaultForNewCards` (ולא למסלול הסניף כמו קודם).
- **`/manage`** — בראש הדף לוח "מצב מחשבים וסטיקים": כרטיסייה לכל מחשב נייד בסניף, "פנוי"
  (טורקיז) / "מושכר" + שם השוכר (אדום) לפי השכרה `laptop` פעילה תואמת (`n_rentals` עם
  `kind`/`itemId`). מחשב "פנוי" שהסטיק המקושר אליו (`n_sticks.linkedLaptopId`) מושכר בנפרד
  מציג בנוסף שורת אזהרה אדומה "ללא אינטרנט" — כלומר המחשב עצמו זמין להשכרה, אבל בלי הסטיק
  שמספק לו אינטרנט. **סטיקים לא מוצגים בלוח כברירת מחדל** (כדי לא להציף אותו בכפילות מול
  המחשבים) — כרטיסיית סטיק מופיעה רק כשהוא מושכר בנפרד מהמחשב שלו (`kind: "stick"` פעיל),
  אדומה עם "מושכר" ושם השוכר; סטיק פנוי לא מקבל כרטיסייה משלו כלל.
  ניהול השכרות פעילות והיסטוריה, חובות (`unpaid-row-actions.tsx`). מתחת ל"השכרות פעילות" ומעל
  "היסטוריה אחרונה" מוצג קטע נפרד **"השכרות שלא שולמו"** (`rentals-lists.tsx`) - כל השכרה עם
  `paid: false` מתוך **כל** ההיסטוריה (לא רק 500 האחרונות שנשלחות ל"היסטוריה אחרונה" למטה),
  כדי שחוב ישן לא "ייקבר" מתחת לעמודי היסטוריה ותמיד יהיה נגיש בלחיצה אחת - אותה שורה
  (`HistoryRentalRow`) ואותן פעולות סימון-כשולם כמו בטבלת ההיסטוריה הרגילה. **"היסטוריה
  אחרונה"** עצמה (עד 500 השכרות שהוחזרו/הסתיימו לאחרונה, ממוינות מהחדש לישן) עברה מהצגת 20
  הראשונות בלבד לדפדוף בעמודים בצד הלקוח (20 בעמוד, כפתורי "הקודם"/"הבא" + "מציג X–Y מתוך Z"
  כמו בתיבת דואר) - כל 500 הרשומות כבר נטענות מ-Firestore ונשלחות ללקוח ממילא (השאילתה
  ל-`n_rentals` תמיד מביאה הכל), אז הדפדוף הוא רק הצגה מדורגת בצד הלקוח, לא שאילתות נוספות.
  חיפוש לפי שם/טלפון מסנן את שלוש הרשימות (פעילות, לא שולמו, היסטוריה) ומאפס את מספר העמוד.
  כפתור "גבייה מידית" בשורת השכרה פעילה (`active-rental-row.tsx`) גלוי רק
  ל-owner או למי שיש לו `perms.charging`; אם ללקוח יש `gatewayToken`+`cardExpiry`
  שמורים (ומסלול תקין נפתר להם) הוא מחייב ישירות דרך הטוקן
  (`token-charge-button.tsx` → `/api/rentals/charge`, עם תווית "שים לב: זה גובה
  לעסק X"), אחרת נופל חזרה להזנת כרטיס ידנית באייפרם (`nedarim-charge-capture.tsx`)
  עם בורר לבחירת העסק שאליו יירשם החיוב (ברירת מחדל: המסלול `defaultForNewCards`).
  לצד זה כפתור "קבלה (מזומן/העברה)" (`issueCashReceiptAction`, גם הוא מוגבל
  ל-`perms.charging`) מפיק ושולח קבלת EZcount ידנית עבור תשלום מזומן/העברה.
  חיוב אשראי מצליח (דרך `/api/rentals/charge`) מפיק קבלת EZcount אוטומטית
  ושולח אותה במייל ללקוח, כל עוד מוגדר לסניף מסלול עם `receiptsProvider: "ezcount"`.
  בטבלת ה"היסטוריה" כל שורה כוללת כפתור "עריכה" (`history-rental-row.tsx`)
  הפותח טופס inline לעדכון תאריך התחלה, תאריך החזרה, מחיר סופי, הערות ופריט
  (מחשב/סטיק - `updateRentalItemAction`, בודק שהפריט החדש שייך לאותו סניף
  ושאינו מושכר בהשכרה פעילה אחרת), (`updateRentalHistoryAction` ב-`../actions.ts`)
  — פתוח לכל מי שיש לו גישה למודול (לא owner-בלבד, בדומה ל-`closeRentalAction`).
  שורה ששולמה מציגה גם כפתור "ביטול סימון" (`markRentalUnpaidAction`) להחזרתה
  ל"לא שולם" במקרה של טעות - פתוח לכל מי שיש לו גישה, כמו סימון "שולם" עצמו.
  שם הלקוח בכל שורה (פעילה או היסטוריה) הוא קישור לדף עריכת הלקוח
  (`/dashboard/rentals/clients/[id]`) לתיקון שם/פרטים. מחיקת שורת השכרה
  (פעילה או היסטוריה - `deleteRentalAction`, `DeleteHistoryRentalButton`) פתוחה
  ל-owner **וגם** ל-partner (לא employee), כמו `deleteClientAction`; גם בשורה
  פעילה (`active-rental-row.tsx`) אפשר לשנות את הפריט המושכר מתוך התצוגה
  המורחבת, באותה בדיקה.
- **`/mine`** — ההשכרות שלי.
- **`/expenses`** — **מסך ההוצאות היחיד של מודול ההשכרות**, גם לבעלים וגם לשותף. הבעלים רואה
  את טבלת הסניפים; מנהל סניף מופנה משם ישירות ל-`/expenses/[branchId]` של הסניף שלו, כשה-
  `branchId` נלקח מה-session ולא מהטופס. עד 09/2026 ישבה לידו לשונית שנייה, **"ההוצאות שלי"**
  (`/rentals/my-expenses`), שהציעה למנהל הסניף מסלול הזנה מקביל לפי פרק יד׳. היא הוסרה — ראו
  פרק יד׳ בסעיף 12.
  הוצאות סניף השכרות (`n_fixed_expenses`/`n_var_expenses`), כולל "סניף" מדומה
  **`shared-rentals`** (`SHARED_RENTALS_BRANCH_ID`, `apps/web/lib/expense-shared-scope.ts`)
  להוצאות משותפות לכל סניפי ההשכרות יחד. **עמוד האינדקס הוא טבלת הסניפים בלבד**: השורה
  העליונה בה היא הספר המשותף עם המספרים שלו (`sharedRow`), בדיוק כמו בחדרי מחשבים. הבלוק
  התחתון שהיה שם — טופס "הוצאה על כמה סניפים" והרשימה שלו — הוסר, ונשארה דרך אחת בלבד
  להוצאה שחלה על יותר מסניף אחד: הספר המשותף. פריסת העמוד (`branch-expenses.tsx`): שני תיבות
  הוספה זו-לצד-זו (`grid md:grid-cols-2`) עם כותרת מודגשת מעל כל אחת - "הוצאות קבועות /
  חודשיות" מימין, "הוצאות חד פעמיות" משמאל (סדר-DOM קובע צד ב-RTL) - **רק** טופס ההוספה,
  בלי הרשימה. מתחת, בנפרד, עוד שורת שתי-עמודות עם הרשימות עצמן (קבועות מימין, חד-פעמיות
  משמאל), כדי שהתיבות למעלה יישארו קומפקטיות גם כשיש הרבה הוצאות. ראו סעיף 8 (חדרי מחשבים → `/expenses`) ו-10 להסבר
  המלא על איך הוצאות אלה מתחשבנות בהנה"ח הראשית. באותו דף, לכל סניף אמיתי (לא `shared-rentals`)
  יש גם קטע **"הכנסות" - owner בלבד** (`addBranchIncomeAction`/`deleteBranchIncomeAction`,
  `apps/web/app/dashboard/rentals/expenses/actions.ts`): הזנה ידנית של הבעלים (למשל הכנסות
  ישנות שלא נרשמו כהשכרות במערכת), נכתבת ל-`n_branch_income` עם שדה `collectedByOwner` (מוצג
  רק כשלסניף יש שותף - "מי מחזיק כרגע בכסף", אני/השותף). היא **מתנהגת כמו הכנסת השכרה רגילה**:
  `buildManualIncomeLines` (`apps/web/lib/branch-accounting-data.ts`) ממזגת אותה לתוך אותן
  income lines ש-`computeBranchFinancials` בונה מהשכרות אמיתיות, כך שהיא נכנסת ל"הכנסות
  החודש"/"הכנסות עד היום" ולמאזן ההעברה החודשית ב-`/dashboard/rentals/accounting` בדיוק כמו כל
  הכנסת השכרה אחרת. מה שכן נשאר זהה לכל הכנסת השכרה: היא **לעולם לא** נכתבת ל-`n_ah_income`
  ישירות מ-`n_rentals`/`n_branch_income` - מגיעה להנה"ח הראשית רק אם הבעלים מקליד אותה שם
  ידנית (סוג הכנסה `laptops`), **או** דרך המסלול השני, הרשמי, שתואר תחת `/accounting` למטה:
  סימון "הועבר" על ההעברה החודשית עצמה (`syncBranchTransferIncome`) - זה לא סותר את הכלל, זה
  ההבדל בין "הכנסת ניידים גולמית של הסניף" (לעולם לא אוטומטית) לבין "כסף שהבעלים בפועל אישר
  שקיבל" (כן אוטומטית, כי זו פעולה מפורשת של הבעלים בדיוק כמו הקלדה ידנית).

  **הספר המשותף שמתחלק (owner בלבד)** — בתוך `/expenses/shared-rentals`, על ההוצאה עצמה ולא
  בטופס נפרד. לכל שורה שם — **קבועה או חד-פעמית** — יש שדה אחד
  (`SharedExpenseSplitField`, `apps/web/components/expenses/shared-expense-split-field.tsx`)
  שעונה על שתי השאלות שתמיד נשאלות יחד: **על אילו סניפים** (כל הסניפים, כולל כאלה שייפתחו,
  או רשימה נבחרת → `branchIds`) ו**כמה אחוז ממנה עליי** (→ `ownerPct`). הכלל, במילים של בעל
  העסק: *פרסום משותף של 1,000 ₪, חצי עליי, ואם יש שם 5 סניפים הם מתחלקים בחצי השני* —
  500 ₪ עליי, 100 ₪ לכל סניף, ובספר של כל סניף שורה אחת של 200 ₪ שמתוכה 100 ₪ עליי.
  **בטפסי ההוספה המתג דלוק כברירת מחדל** (על כל הסניפים, 50%): הוצאה שנרשמת בספר המשותף
  היא כמעט תמיד הוצאה שחלה על הסניפים. בחלון העריכה ברירת המחדל נגזרת מהשורה עצמה, כדי
  ששורות שנרשמו לפני השינוי לא ישנו התנהגות בשקט.

  `ownerPct` הוא **גם המתג**: שורה משותפת שנושאת אותו נכנסת לספר של כל סניף שהיא חלה עליו
  (`loadBranchAccountingRawData` מוסיף לכל סניף עותק של השורה עם `amount` = הפרוסה שלו,
  ו-`lineOwnerShare` מגיע מ-`ownerPct` במקום מ-`owedBy`), ובמודל התנועה היא מקבלת
  `ownerShare` = חלק הבעלים ו-`allocations` = הפיצול לסניפים (`sharedSplitOf`,
  `lib/tx-data.ts`). שורה בלי `ownerPct` נשארת בדיוק כפי שהייתה: בספר המשותף בלבד, לא
  מגיעה לאף סניף, ו-`owedBy` הוא שקובע את חלק הבעלים. החשבון עצמו הוא
  `splitMultiBranchExpense` (`apps/web/lib/multi-branch-expense.ts`), מודול טהור (בלי
  `firebase-admin`) — אותה פונקציה בדיוק משרתת את התצוגה החיה בטופס, את ההנה"ח ואת מודל
  התנועה, כך שמה שרואים בטופס הוא מה שנרשם. מקבילה מכוונת ל-`splitAdArea`
  (`lib/ad-areas.ts`), שממנה המודל הוכלל.

  זה מה שהחליף את `n_multi_branch_expenses`: הקולקשן ההוא נולד רק כדי לבטא אחוז חופשי,
  לא ידע הוצאות קבועות, וישב בטופס נפרד בתחתית עמוד האינדקס. הוא סגור לכתיבה, והשורות
  שכבר נרשמו בו מוצגות למחיקה בתוך הספר המשותף (`legacy-multi-branch.tsx`).

- **`/accounting`** — הנה"ח ברמת סניף השכרות. **נבנה מחדש (2026-09) לטבלה אחת בלבד.**

  מה שהיה כאן ניסה להיות כל המודול: טבלת התחשבנות חודשית, כרטיסי סניפים, היסטוריית ספר מלאה,
  טפסי הכנסה והוצאה, שליחת דוחות ושורת שותפי מחשבים. כמעט כל זה ענה על שאלות שנשאלות במקום
  אחר — **טבלת ההתחשבנות עברה ל-`/dashboard/accounting/mobile`**, שם היא יושבת ליד הכסף
  שהיא מזיזה; הכנסות נרשמות בספר הראשי; הוצאות במסך ההוצאות.

  מה שנשאר הוא מה שבאמת נשאל כאן — **שורה אחת לכל סניף, חמש עמודות**
  (`branch-summary-table.tsx`):

  | עמודה | מהיכן | מה זה אומר |
  |---|---|---|
  | ההוצאות שלי עד היום | `ownerInvestedToDate` | החלק שלי בעלות, לפי "על מי החוב" של כל שורה |
  | ששילמתי בפועל עד היום | `ownerPaidCashToDate` | כל שורה שאני שילמתי, **במלוא הסכום** — כולל מה ששילמתי עבור הסניף ואמור לחזור אליי |
  | ההכנסות שלי עד היום | `ownerEarnedToDate` | החלק שלי בהכנסות |
  | רווח עד היום | `ownerBalanceToDate` | ההפרש |
  | נכנס החודש | `settlementNetToOwner` | + מגיע אליי · − אני מעביר |

  שתי העמודות הראשונות נראות דומות ואינן: "כמה הוצאתי" ו"כמה מזה באמת שלי" הם שני מספרים
  שונים, ולכן `ownerPaidCashToDate` נוסף ל-`BranchFinancials` ולא נגזר מהקיים.

  **אין כאן טפסים** — המסך נשאל, לא נכתב. בורר חודש בראש, וכפתור **"הצג גם סניפים שנסגרו"**
  (`?closed=1`) שמכניס לטבלה סניפים עם `closedAt`/`deleted`, כדי שההיסטוריה לא תיסגר.

  **הטבלה הזו היא של הבעלים בלבד**, וכך גם בורר החודש וכפתור הסניפים שנסגרו: כל חמש
  העמודות נגזרות מ-`owner*` — החלק של **הבעלים**, מתחת לכותרות בגוף ראשון ("ההוצאות שלי",
  "ההכנסות שלי"). מסך שמראה לשותף את חלקו של הבעלים ומכנה אותו "שלי" הוא לא מסך מסונן, הוא
  מסך של מישהו אחר.

  **partner/employee (לא owner) רואים במקומה `PartnerAccountingView`** (`partner-view.tsx`),
  וזה **כל** מה שיש במסך שלהם:
  - **שש משבצות**, שלוש בכל שורה (RTL — הראשונה מימין): הכנסות / הוצאות / מאזן, פעם אחת
    לחודש הנוכחי ופעם אחת "עד היום". כל סכום הוא **חלקו של השותף**: `incomeThisMonth`,
    `expenseThisMonth`, `balanceThisMonth` ו-`*ToDate` מ-`computeBranchFinancials` — כלומר
    ההכנסה כפול `partnerPct`, וההוצאה כ-`amount - ownerShare` פר שורה. הוצאה שכולה על הבעלים
    יוצאת 0 ואינה מופיעה כלל; הוצאה משותפת נכנסת בחלקו בלבד, בלי קשר למי שילם אותה בפועל
    (מי שילם היא שאלה של החשבון הפתוח, לא של הרווח).
  - **"חשבון פתוח"** מתחתן: `buildBranchLedger(...).currentBalance` — היתרה **המצטברת** על
    פני כל החודשים, אותו מספר בדיוק שמזין את `UnifiedBranchesTable` ב-
    `/dashboard/accounting/mobile`, כדי ששני הצדדים יראו את אותו חוב. חיובי = השותף חייב
    לבעלים, שלילי = הבעלים חייב לו, ויורד ל-0 ברגע שהבעלים מסמן את ההעברה
    (`n_branch_transfers`).
  - אין לשותף בורר חודשים (`?month=` בכתובת לא מזיז לו כלום — תמיד החודש הנוכחי), אין כפתור
    "הצג גם סניפים שנסגרו", ואין טבלה.
  - **הסניפים שלו** נקבעים לפי `user.branchId` **וגם** לפי `Branch.partnerEmail` שמתאים
    למייל שלו, כך ששותף עם כמה סניפים — או כזה שלא הוצמד לו `branchId` — עדיין רואה את שלו.
    סניף שנסגר מוצג לו מסומן "(נסגר)": החשבון הפתוח שלו עדיין שלו.
  - מסלול ה-non-owner **תמיד** מסתיים ב-`return`, גם כשלא נמצא לו אף סניף (אז מוצגת הודעה
    לפנות לבעלים). זהו הגבול עצמו — אין נפילה לגוף העמוד.

  **רווח פר-מחשב** עבר גם הוא: הוא נשאל על פני כל הסניפים יחד ולכן מקומו ב-
  `/dashboard/accounting/mobile` (ראו סעיף ההנה"ח). `ComputerProfitTable` ו-
  `branch-view.tsx`/`owner-overview.tsx` נמחקו.
  **חשוב, ונשמר:** `computersActiveInMonth` יכולה להחזיר `0` (סניף בלי אף מחשב רשום עדיין) -
  `buildComputerProfitTrend` **לא** מחליף את זה ב-1 (זה היה באג שהראה "1 מחשב" לסניף בלי אף
  מחשב אמיתי) - עם 0 מחשבים `profitPerComputer` הוא 0 ו-`isHealthy` תמיד `false`.

- **`/branches`** — ניהול סניפי השכרה + audit הרשאות. טבלה (לא רשימת כרטיסים): סניף/סוג/מיקום/
  **טלפון** (`Branch.phone`, שדה חדש - טלפון ליצירת קשר עם הסניף, נערך ב-`rental-branch-form.tsx`)/
  שותף/**מספר מחשבים**. מספר המחשבים נספר **בזמן אמת** מ-`n_laptops` לפי `branchId`
  (`branches/page.tsx`) - לא מהערכת `computerProfitTrend` - כדי שתמיד יתאים בדיוק למה שבאמת
  רשום ב-`/laptops`. הרשימה כאן (וכל picker/dropdown של סניף בשאר המודול) **מסננת סניפים
  שנמחקו** (`Branch.deleted`).

  **מחיקת סניף = soft-delete, לא מחיקה אמיתית** (`deleteRentalBranchAction`,
  `branches/actions.ts`): קובע `{ deleted: true, deletedAt }` על מסמך ה-`n_branches` במקום
  `.delete()` - כדי שכל ההוצאות/הכנסות/העברות שכבר משוייכות ל-`branchId` שלו (`n_fixed_expenses`,
  `n_var_expenses`, `n_rentals`, `n_branch_transfers`, `n_branch_income`) יישארו ניתנות
  לפתרון-שם ולצפייה בהיסטוריית ההנה"ח (סעיף לעיל) גם אחרי המחיקה - Firestore doc אמיתי לא
  נמחק אף פעם. **כל** שאילתת סניפים פעילה במודול ההשכרות (pickers ליצירת השכרה/לקוח/מחשב/הוצאה,
  רשימת `/branches`, רשימת `/laptops`, טפסי `/branches/[id]`+`/new` וכו') מסננת
  `!b.deleted` - החריג היחיד הוא קטע "היסטוריה מלאה" ב-`/accounting` שמראה גם סניפים שנמחקו
  בכוונה. בטפסי עריכה שיש בהם בחירת סניף לישות קיימת (עריכת מחשב/לקוח) הסניף הנוכחי של הישות
  נשאר ברשימה גם אם נמחק (מתוייג &quot;(נמחק)&quot;) - כדי שלא יקרה שמירה בטעות שמעבירה אותה
  לסניף אחר רק כי הסניף המקורי לא הופיע ברשימה. **הערה:** סניפים שנמחקו *לפני* השינוי הזה
  (בזמן שהמחיקה עוד היתה `.delete()` אמיתי) לא ניתנים לשחזור - השדה `Branch.phone`/`deleted`
  חל רק על מחיקות מכאן ואילך.
- **`/labels`** — הדפסת מדבקות ללקוחות/פריטים + הגדרות לוגו.
- **API `/api/rentals/charge`** — חיוב כרטיס דרך Nedarim Plus (`DebitCard.aspx`)
  לפי `gatewayToken`/`cardExpiry` שמורים על הלקוח; מוגן בהרשאת `charging` בצד
  שרת, ומזהה המוסד (`Mosad`) נפתר דינמית דרך `resolveNedarimCreds(branchId,
  client.collectionRouteId)` — קודם לפי המסלול שנשמר על הלקוח (העסק שהטוקן שלו
  שייך אליו בפועל), ורק אם אין ערך כזה (לקוחות ישנים) נופל חזרה לפתרון לפי סניף
  כמו קודם. לא hardcoded.
- **API `/api/rentals/clients/export|template`** — ייצוא/תבנית אקסל של לקוחות.

**מסך המחירון נמחק (2026-09).** `/dashboard/rentals/pricing` והלשונית "מחירון" הוסרו לבקשת
הבעלים. **השדה `n_branches.rentalPricing` נשאר** (`BranchRentalPricing`: `laptop` = יום/שבוע/חודש
× עם-סטיק/בלי-סטיק, `stick` = יום ראשון/יום שני/כל יום נוסף + שבוע/חודש) וכך גם כל מנוע
התמחור — מחיקתו הייתה משאירה כל סניף בלי מחיר ברירת מחדל ושוברת את חישוב ההשכרות. מה שנמחק
הוא **המסך לעריכת ברירת המחדל**, לא הנתון: **כל מחיר על מחשב/סטיק בודד הוא חריגה אופציונלית** —
שדה ריק (0) ממשיך לרשת את מחיר הסניף דרך `effectiveLaptopRates()` / `effectiveStickRates()`,
שמופעלות בכל מקום שמציג או מחשב מחיר (סגירת השכרה, השכרה חדשה, `createRentalAction`, מלאי
המחשבים). מכאן ואילך מחירים נקבעים פר-מחשב במסך "מחשבים".

תמחור השכרה: `lib/rental-pricing.ts` — **מנוע אחד לכל המערכת ולכל הסניפים**, גם למחשבים
ניידים וגם לסטיקים. הפונקציה המרכזית היא `calcQuote(startDate, endDate, rates)`, ומעליה
`calcRentalQuote` (מחשב, לפי `laptopRatesFor` שבוחר מחירון "עם סטיק"/"בלי סטיק") ו-
`calcStickQuote` (סטיק: `day1`/`day2`/`day3plus` + `weekPrice`/`monthPrice`). מדרגות היום
הראשון/השני חלות רק בתחילת ההשכרה — ימים שנשארים אחרי חודש/שבוע מחויבים במחיר היומי השוטף,
ותמיד נבחר השילוב הזול ביותר ללקוח.
המודל: (1) סופרים **חודשים קלנדריים שלמים** מתאריך ההתחלה — 12/07 → 12/08 = חודש
אחד (עם הצמדה לסוף חודש: 31/01 + חודש = 28/02); (2) יתרת הימים נספרת כ**ימי חיוב**
כששישי ושבת יחד = יום אחד (שבת לא נספרת בנפרד, ולכן שבוע = 6 ימי חיוב); (3) יתרת
הימים מתומחרת בשילוב הזול ביותר של שבועות/ימים ולעולם לא יותר ממחיר חודש שלם.
דוגמה: 12/07 → 15/08 עם חודש=550 ויום=50 → `חודש × 550 + 2 ימים × 50 = 650 ₪`
(13/08 ו-14/08 נספרים, 15/08 שבת נבלעת ביום שישי). **אין חישוב פרו-רטה** — כל
מחיר עובר `roundPrice()` ונשמר כשקל שלם (גם `calcPrice` בפתיחת השכרה, `finalPrice`
בסגירה/עריכה, וסכום הקבלה ב-`issueCashReceiptAction`). `calcRentalQuote()` מחזיר
גם `periodLabel` ו-`breakdown` להצגת פירוט החישוב למשתמש, ומסך סגירת ההשכרה
(`active-rental-row.tsx`) מציג אותם לצד כפתור "תיקון המחיר ידנית" שמאפשר לדרוס את
המחיר (עם שדה סיבה חובה, נשמר ב-`priceOverrideReason`).

### משרד שיתופי (`/dashboard/coworking`) — perm: coworking

**שתי לשוניות** (`coworking-tabs.tsx`): **סניפים ועמדות** · **הנה"ח**. הליבה ב-
`apps/web/lib/coworking.ts`.

> **למה שתיים ולא חמש.** היו כאן "לקוחות ותשלומים", "סניפים", "עמדות", "הוצאות" ו"הנה"ח" —
> וארבע מהן תיארו את אותו דבר מזוויות שונות: לקוח *הוא* מי שיושב בעמדה, תשלום *הוא* שכר
> הדירה של העמדה, וההוצאות היו אותם סכומים שההנה"ח כבר סיכמה. במשרד עם ארבע עמדות בסניף
> אחד יש בדיוק שתי שאלות — מי יושב בכל עמדה ומי שילם, וכמה יצא מול כמה נכנס — ולכן שני
> מסכים. `/coworking/new`, `/coworking/stations`, `/coworking/branches` (רשימה)
> ו-`/coworking/expenses` נמחקו; `/coworking/branches/new` ו-`/branches/[id]` נשארו כטפסים
> בלבד, שהמסך המאוחד מקשר אליהם.

**סניפים ועמדות (`/`)** — מסך אחד בשלוש שכבות:

1. **כרטיס הסניף** — שם, מיקום, טלפון, תאריך פתיחה ו**עלות ההקמה** (עם תגית אם היא נספרת
   בראשי), וקישורים לעריכת הסניף ולהוספת סניף. עם יותר מסניף אחד מופיע בורר
   (`?branchId=`), שקיים כדי שסניף שני לא ישבור את המסך — לא כדי לנהל רשת.
2. **ארבע משבצות ממוספרות 1–4** — קטנות בכוונה, כל אחת אומרת דבר אחד: **מושכרת** או
   **פנויה**, ולמושכרת גם אם שולם החודש. **ארבע עמדות פיזיות, ולכן אין "הוספת עמדה"**:
   מספר העמדה הוא הזהות, ומסמך `n_cw_stations` נוצר מעצמו (`resolveStationId`) בפעם
   הראשונה שמשכירים אותה.
3. **כרטיס לכל עמדה** במבנה של נייד מושכר — שוכר, **תאריך התחלה**, **תאריך סיום**, מחיר
   חודשי, מצב התשלום של החודש, סיום השכרה. עמדה פנויה מציגה במקומו טופס השכרה.

ההשכרה עצמה היא רשומת `n_cw_clients` רגילה (`rentStationAction`), כך שכל חישובי המנוי
הקיימים — `billableMonths`, `unpaidMonths`, `clientStatus` — ממשיכים לעבוד בלי שינוי.

**היסטוריית ההשכרות** יושבת בתחתית אותו מסך (`rental-history.tsx`) ומציגה את **כל**
ההשכרות — פעילות והיסטוריות באותה רשימה, החדשה למעלה — עם תקופה, סה"כ לחיוב, סה"כ ששולם
וחוב. כל שורה נפתחת ל**לוח החודשים המלא** של אותה השכרה (`rentalMonths`/`rentalTotals`),
שבו אפשר לסמן תשלום או לבטל סימון **גם לחודש שעבר מזמן**, ולבעלים גם **למחוק את ההשכרה**
(`deleteRentalAction` — מחיקה אמיתית ולא רכה, כי התשלומים יושבים בתוך המסמך ולא נשארות
שורות יתומות; האישור מונה כמה תשלומים יימחקו, כי הם הכנסה שנספרה בהנה"ח הראשית והמחיקה
גורעת משם כסף). בלי המסך הזה שוכר שנרשם לו
תאריך סיום פשוט נעלם, יחד עם השאלה אם נשאר חייב — והשאלה הזו לא מסתיימת עם השכירות.
חודש שיש בו תשלום אך אינו בטווח החיוב מוצג גם הוא, כי הסתרת כסף שנרשם גרועה מהצגת שורה
חריגה.

> **יום התשלום = יום תחילת השכירות.** `payDay` נגזר מ-`startDate` ברישום ההשכרה, ולכן בכל 1
> בחודש נפתח חודש חדש לגבייה מעצמו. כפתור **"סמן כשולם"** (`markStationPaidAction`) רושם
> את התשלום עם **`countsToMain: true` תמיד** — הבעלים ביקש שסימון "שולם" יהיה גם הרישום
> בהנה"ח הראשית, ולכן אין כאן שאלה נוספת. אותה תמונה מופיעה גם בדף הבית (`/dashboard`)
> ככרטיס "תשלומי משרד שיתופי", עם קישור חזרה למסך.

**השכרות ללא סניף פעיל ("יתומות").** כל מסכי המודול מסננים לפי הסניף הנבחר, אבל כרטיס
התשלומים שבדף הבית וגם `loadMainLedger` קוראים את **כל** `n_cw_clients` בלי סינון. לכן
השכרה שה-`branchId` שלה מצביע על סניף מחוק, על סניף מסוג אחר או על מסמך שלא קיים (בדרך
כלל שריד מ-`app.html` הישן) ייצרה **התראת תשלום בדף הבית בלי שום מסך שמאחוריה** — רואים
שמישהו חייב, ואי אפשר למצוא אותו בשום מקום. הטיפול:

- `CoworkingClientStatus.orphan` מסומן ב-`loadCoworkingData` לפי רשימת הסניפים החיים,
  ו-`orphanReason` מסביר **למה** (אין `branchId` · הסניף לא קיים · הסניף נמחק · הסניף
  אינו משרד שיתופי).
- מסך **סניפים ועמדות** מציג סעיף **"השכרות ללא סניף פעיל"** (`OrphanRentals`) — גם כשאין
  אף סניף משרד שיתופי, מצב שבו כל שאר המסך ריק. הסעיף הוא `RentalHistory` בגוון אזהרה,
  כלומר לוח החודשים המלא זמין בו כרגיל, ולכל שורה נוסף טופס **שיוך לסניף ולעמדה**
  (`assignRentalToBranchAction`, בעלים בלבד — מעדכן `branchId`/`stationId`/`stationNumber`
  ואפשר להשלים בו מחיר חודשי; התשלומים שנרשמו לא משתנים). מחיקה זמינה כרגיל דרך
  `deleteRentalAction`.
- **דף הבית** מסמן שורה יתומה בתגית "ללא סניף פעיל" ומסביר בשורה אחת למה היא לא מופיעה
  בעמדות, כך שההתראה מפנה למקום שבו מטפלים בה במקום להיראות כתקלה.
- **הנה"ח המודול** סופרת את תשלומי ההשכרות היתומות ב"קיבלתי עד היום" (לבעלים בלבד, עם
  הערה כמה מתוך הסכום הגיע מהן), כי הכסף הזה כבר נספר בהנה"ח הראשית והשמטתו כאן יצרה
  פער בין שני המסכים.

> **"פעילה" היא הגדרה אחת** (`isActiveOn`): אין תאריך סיום, **או** שתאריך הסיום עוד לא
> הגיע. קודם מסך העמדות שאל כך ודף הבית שאל רק "אין תאריך סיום", ולכן השכרה עם סיום עתידי
> הופיעה כמושכרת במסך אבל נעלמה מהתראת התשלומים — למרות שהחודש הזה עדיין נגבה.

**הנה"ח (`/accounting`)** — המאזן **וגם ניהול ההוצאות**. שלושת המספרים (שילמתי · קיבלתי ·
מאזן, מ-`buildCoworkingLedger` שמקבל גם את הסניפים בשביל עלות ההקמה שלהם), הפירוט לשלושת
סוגי ההוצאה, ומתחתיהם טפסי ההזנה והרשימות של **הוצאות קבועות** (`n_fixed_expenses`)
ו**הוצאות משתנות** (`n_var_expenses`) וכרטיס ה**הוצאות הקבועות המשתנות**. לכל הוצאה
`countsToMain`.

> **הוצאות הקמה כבר לא נרשמות כשורות הוצאה.** הן שדה על הסניף (`setupCost`/`setupItems`
> בטופס הסניף), ששם הן באמת תכונה של הסניף ולא אירוע חודשי. שורות ישנות בקטגוריה
> `SETUP_CATEGORY` (`"הקמה"`) **נשארות בדאטה וממשיכות להיספר** כ-`setupFromExpenses`, כדי
> ששום סכום קיים לא ייעלם; פשוט אין יותר מסך שמזין חדשות.

### הנהלת חשבונות (`/dashboard/accounting`) — perm: accounting, owner בלבד

**הכלל היחיד: `countsToMain`.**

לכל שקל בעסק יש שתי שאלות נפרדות, ועד היום הן היו מעורבבות:

1. **על מי הוא נופל בסניף** — `paidBy` / `owedBy` / `ownerPct`. זה חשבון מול שותף.
2. **האם הוא נכנס לספר הראשי** — `countsToMain`. זה חשבון של העסק מול עצמו.

השאלה השנייה נגזרה קודם מהראשונה, וכל מסך גזר אותה אחרת — מכאן מודל התנועות, ספר התזרים,
המראות (`linkedAhExpenseId`), המזכר ההוני ומסך "בדיקת שלמות" שניסה להסביר בדיעבד למה שני
מספרים לא הסתדרו. עכשיו זה שדה בוליאני על כל שורת הוצאה/הכנסה, מישהו מסמן אותו, והספר הראשי
הוא הסכום של המסומנים. אין נוסחה, אין העתק, ואין מה שיכול לסטות.

`undefined` נחשב `false` **בכוונה**: כל הדאטה ההיסטורי נכנס למצב "לא מתחשבן בראשי", כדי שאף
סכום ישן לא יופיע פתאום בשורה התחתונה בלי שמישהו אמר שהוא שייך. את הדגל מסמנים בטופס שבו
ההוצאה נרשמת. העזרים ב-`apps/web/lib/counts-to-main.ts`; הצ'קבוקס האחיד ב-
`apps/web/components/counts-to-main-field.tsx` — אותה קוביה בכל טופס בעסק.

**מבנה המודול (`accounting-tabs.tsx`) — שני מסכים:**

| מסך | השאלה שהוא עונה עליה |
|---|---|
| `/dashboard/accounting` ("ראשי") | כמה הוצאנו, כמה הכנסנו, מה המאזן — ומה מרכיב אותם. **כל ההזנה וכל הטבלאות של ההנה"ח נמצאות כאן** |
| `/mobile` ("ניידים") | כמה כל מחשב מרוויח לי פר סניף ופר חודש (מימין), וכמה כל סניף צריך להעביר החודש (משמאל) |

בנוסף `/routes` (הגדרות גבייה וקבלות) נשאר כקישור צדדי — מודול ההשכרות והפקת הקבלות תלויים בו.

**`/extra-expenses` ("הוצאות נוספות") נמחק (2026-09).** כל מה שהוא החזיק — רכישות חד-פעמיות,
הוצאות קבועות של העסק, הוצאות קבועות משתנות וסיכום הרכישות החוזרות — נמצא היום ב"ראשי"
עצמו: ארבע מתוך שש הטבלאות שבסרגל שלו, ושניים משלושת הסוגים בחלון "הוספת הוצאה". גם פעולות
הניהול עברו איתו: הפסקה/חידוש, מחיקה, ו-`RecurringHistoryPanel` המלא (שיושב מקופל מתחת
לטבלת "עדכון קבוע משתנה"). לשונית שנייה לאותו דאטה היא רק עוד מקום שצריך לזכור להיכנס
אליו. הכתובת נשארה בחיים כ-`redirect` ל-`/dashboard/accounting`, כדי שסימנייה ישנה לא
תיפול ל-404.

**מה נמחק בבנייה מחדש (2026-09):** `overview`, `bottom-line`, `entries`, `assets`, `purchases`,
`recurring-purchases` (רכישה חוזרת היא סימון על ההוצאה, לא מודול — ראו למטה),
`inventory`, `sales`, `policies`, `review`, `integrity`, `branches`, וכן הליבות
`lib/accounting-overview.ts`, `lib/business-ledger.ts`, `lib/integrity.ts`,
`lib/accounting-entries*.ts`, `lib/accounting-categories.ts`, `lib/branch-opening-proposals.ts`.
כולן ניסו לענות על שאלה אחת — האם הסכום הזה שייך לספר הראשי — וכל אחת ענתה עליה אחרת.
**הדאטה לא נמחק**: כל הקולקשנים נשארו, ו-`countsToMain` נערך בטופס שבו ההוצאה נרשמת.

**`legacy` ("עדכון רטרואקטיבי") הוסר (2026-09):** הוא נבנה כמסך חד-פעמי להחליט על דאטה
שנרשם לפני שהדגל היה קיים, וההחלטה הזו כבר התקבלה. מסך עם "סמן הכל" / "הסר הכל" על מאות
שורות היסטוריות הוא, אחרי שסיים את תפקידו, רק דרך להזיז סכומים בספר הראשי בטעות. שינוי
הדגל על שורה קיימת נעשה מכאן והלאה בטופס שלה.

#### `/dashboard/accounting` — הספר הראשי

שלושה מספרים בראש (הוצאנו עד היום · הכנסנו עד היום · מאזן, ולצידם אותם שלושה לחודש הנוכחי),
מתחתיהם **שני כפתורי הזנה**, ומתחתם **סרגל טבלאות שסגור כברירת מחדל**. הכל מ-
`apps/web/lib/main-ledger.ts` ומהקולקשנים שמסך "הוצאות נוספות" קרא לפני שנמחק
(`ledger-workspace.tsx`).

**שני כפתורים במקום טופס פרוש (2026-09):** הספר הראשי הוא מסך שמסתכלים בו, והטופס הוא פעולה
שעושים ומסיימים — טופס פרוש תמיד גזל את המקום ממה שבאמת רוצים לראות. נשארו שני כפתורים,
וכל אחד פותח חלון (`modal.tsx`) עם בדיוק אותם שדות ואותן Server Actions שהיו בטופס:

| כפתור | מה נפתח | לאן זה נכתב |
|---|---|---|
| **הוספת הכנסה** (`add-income-button.tsx`) | בחירת סוג — אשראי / מזומן / ניידים / מכירה — ואז השדות של אותו סוג בלבד | `n_ah_income` (`createIncomeAction`) |
| **הוספת הוצאה** (`add-expense-button.tsx`) | בחירת סוג — **הוצאה קבועה** / **קבועה משתנה** / **הוצאה-רכישה** — ואז השדות המלאים של אותו סוג | `n_ah_fixed_expenses` / `n_recurring_var_expenses` (scope `main`) / `n_ah_expenses` |

שלוש אפשרויות ההוצאה הן אותה שאלה בשלוש תשובות — האם ההוצאה חוזרת, והאם הסכום ידוע — ולכן
הן שלושה צ'יפים בחלון אחד ולא שלושה מסכים. עד כה כדי לרשום הוצאה קבועה היה צריך לעזוב את
הספר הראשי ולעבור ל-`/extra-expenses`; המסך ההוא כבר לא קיים.

**סרגל הטבלאות** (`ledger-workspace.tsx`) יושב מתחת לכפתורים ונראה כמו סרגל הניווט שמעליו,
אבל הוא בורר תצוגה ולא ניווט: **ברירת המחדל היא מסך נקי**, לחיצה פותחת טבלה אחת, לחיצה
חוזרת או "סגירה" מחזירה למסך הנקי. שש הטבלאות של ההנה"ח לא נקראות יחד אף פעם, ולכן הן לא
מוצגות יחד:

| כפתור | הטבלה | המקור |
|---|---|---|
| הכנסות | כל שורות ההכנסה של הספר, עם הפקת מסמך ומחיקה | `MainLedger.income` |
| הוצאות | כל שורות ההוצאה שסומנו `countsToMain` | `MainLedger.expenses` |
| רכישות והוצאות ח"פ | הרכישות החד-פעמיות של העסק עצמו + מחיקה (`purchases-table.tsx`) | `n_ah_expenses` |
| רכישות חוזרות | כמה באמת הולך על כל מוצר, בכל העסק (`recurring-purchases-table.tsx`) | `loadRecurringPurchaseIndex` |
| הוצאות קבועות | ההוצאות הקבועות של העסק + צבירה, הפסקה ומחיקה (`fixed-expenses-table.tsx`) | `n_ah_fixed_expenses` |
| עדכון קבוע משתנה | הזנת הסכום החודשי לכל ההוצאות הקבועות המשתנות **מכל המודולים**, + הפסקה/חידוש/מחיקה והיסטוריה מלאה (`recurring-update-table.tsx`) | `n_recurring_var_expenses`, כל ה-scopes |

**טבלת "עדכון קבוע משתנה"** היא הסיבה העיקרית שהסרגל קיים: זו הפעולה החוזרת היחידה שההנה"ח
דורשת (ב-1 לחודש — כמה היה החשמל), והיא הייתה מפוזרת על ארבעה מסכים, ולכן נעשתה חלקית.
למעלה בורר חודש אחד (ברירת המחדל: **החודש שנסגר**; החודש הרץ ברשימה בכל זאת, למי שיודע את
הסכום מראש), ובכל שורה שדה + כפתור שכותב ישירות דרך `setRecurringMonthAmountAction` למודול
שההוצאה שייכת לו. בסוף כל שורה יושבות גם **הפסקה/חידוש ומחיקה** (`recurring-row-controls.tsx`
— אותן Server Actions, בגרסת לקוח כי הן בתוך טבלה אינטראקטיבית), ומתחת לטבלה מקופלים
פאנלי `RecurringHistoryPanel` המלאים: מילוי שנה שלמה, תיקון חודש ישן, שינוי תדירות ועריכת
פרטי ההוצאה. הפאנלים הם Server Components ולכן נבנים ב-`page.tsx` ונמסרים לטבלה כ-`ReactNode`.

**כל הטבלאות חולקות קומפוננטה אחת** — `accounting/data-table.tsx`. היא מחזיקה את החיפוש
החופשי, בוררי הסינון (כל עמודה שסומנה `filterable` מייצרת תפריט מהערכים שלה), טווח התאריכים,
הסידור בלחיצה על כותרת, והעימוד (50 שורות כברירת מחדל; 25 / 50 / 100 / 250). כל טבלה מגדירה
רק את העמודות שלה, וכל עמודה מגדירה שתי פונקציות: `value` (מה שממיינים ומסננים לפיו) ו-
`render` (מה שרואים) — ההפרדה הזו היא מה שמאפשר לעמודה להציג תג או כפתור ועדיין להיות
ממוינת. הסכום שמוצג כשיש סינון פעיל הוא של **השורות המסוננות**, לא של העמוד, והסכום שבכותרת
הוא תמיד המלא. הכל רץ בצד הלקוח על שורות שנטענו ממילא — ולכן מעבר עמוד או פתיחת טבלה לא
עולים קריאה נוספת ל-Firestore.

`ledger-table.tsx` נשארה הקומפוננטה של ההכנסות/ההוצאות אבל מחזיקה מעכשיו רק את הגדרת
העמודות (תאריך · תיאור · סוג/קטגוריה · מקור · סכום, ובהכנסות עמודה שישית לפעולות). פעולות
ההכנסה (סימון/הפקת מסמך, מחיקה) מוצגות כאייקונים — `IssueReceiptButton` ו-`DeleteEntryButton`
מקבלים `compact`; בשאר המסכים הם נשארים עם הטקסט.

**מה נספר כהוצאה** (רק כש-`countsToMain === true`):

| מקור | איך נספר |
|---|---|
| `n_fixed_expenses` | נצבר חודש-חודש מהחודש של `startDate` ועד היום/`endDate` (`fixedExpenseAccrued`) |
| `n_var_expenses` | הסכום, פעם אחת |
| `n_multi_branch_expenses` | הסכום המלא, פעם אחת (לא מתחלק — הספר הראשי הוא ספר אחד) |
| `n_recurring_var_expenses` | העלות החודשית אחרי פריסה (`monthlyAllocation`): הוצאה חודשית נספרת במלואה בחודש שלה; תשלום רב-חודשי פרוס מתחלק שווה בשווה על חודשי המחזור שהוא משלם עליהם, וחודש שטרם הגיע לא נספר עדיין |
| `n_ah_expenses` | רכישות והוצאות חד-פעמיות של העסק עצמו |
| `n_ah_fixed_expenses` | הוצאה קבועה של העסק עצמו — **שורה לכל חודש** שההוצאה הייתה פעילה בו, בסכום החודשי (`mainFixedExpenseMonths`). לא שורה אחת מצטברת: שכירות המשרד היא הוצאה של אוקטובר בדיוק כמו של ספטמבר, ובלי שורה לכל חודש היא הייתה נעלמת מ"כמה הוצאנו החודש" בכל חודש חוץ מהראשון |
| `n_branches.setupCost` | עלות ההקמה של הסניף — שורה אחת לסניף, נתלית בחודש `openedAt`/`founded` שלו. **לא** נשלטת ב-`countsToMain` אלא ב-`setupCountsToMain`, שדלוק כברירת מחדל (ראו החריג בסעיף מודל הנתונים). סניף מחוק לא נספר |

**מה נספר כהכנסה**: כל `n_ah_income` (ארבעת הסוגים למטה), ותשלומי משרד שיתופי שסומנו.
`n_branch_income` **לעולם לא** — הוא אינדיקציה פר-סניף, לא כסף אמיתי.

שורת `cash` נספרת כאן, **וגם מוצגת** בדשבורד המעקב של חדר המחשבים שממנו נמשכה
(`/computer-rooms-accounting/[id]`, ראו סעיף 8). זו קריאה בכיוון אחד ולא כתיבה כפולה: הספר
הראשי נשאר המקום היחיד שבו השקל הזה נספר, ומוחקים אותו רק כאן.

**הוספת הכנסה — ארבעה סוגים**, ו-`type` הוא מה שקובע איזה שדה נוסף חובה:

| סוג | `type` | מה עוד נדרש |
|---|---|---|
| אשראי | `credit` | תאריך וסכום בלבד |
| מזומן | `cash` | `branchId` של סניף חדר מחשבים — מאיזו קופה נמשך |
| ניידים | `laptops` | `branchId` של סניף ניידים + סימון/הפקת קבלה |
| מכירת מחשבים | `sale` | `soldTo` (רשות) — למי נמכר |

**הפקת מסמך על הכנסת ניידים** (`receipt-actions.ts` → `lib/ezcount.ts`): המערכת מקימה את
הלקוח ב-EZcount לפי השם והמייל, מפיקה **חשבונית מס קבלה** (סוג 320) ושולחת אותה במייל באותה
קריאה; מספר המסמך וקישור ה-PDF נשמרים על שורת ההכנסה (`receiptDocNumber` / `receiptPdfUrl`).
במודל אפשר לבחור "קבלה בלבד" (סוג 400) למקרה שכבר קיימת חשבונית נפרדת.

**המערכת לא מפיקה מסמך על עסקת אשראי. אף פעם.** זה אילוץ עסקי, לא העדפה: הסליקה עוברת דרך
נדרים פלוס, ושם מוגדר שכל עסקה מפיקה חשבונית מס קבלה **בעצמה**. מסמך שני על אותו תשלום הוא
כפילות מול רשות המסים, וזה כבר קרה.

הכלל מנוסח כך: **מסמך מופק רק על כסף שלא נסלק** — מזומן שנמשך מקופה, והעברות (מסניף ניידים
או בנקאית). הוא נאכף בארבע נקודות, ולא רק ב-UI, כי כפתור מוסתר הוא לא אכיפה:

| היכן | מה נחסם |
|---|---|
| `EzcountReceiptParams.paymentType` | הטיפוס מקבל `1` (מזומן) או `4` (העברה) בלבד — אשראי (3) הוסר |
| `createEzcountReceipt` | בדיקת ריצה שמסרבת ל-`payment_type` 3 גם אם הגיע ב-cast |
| `issueIncomeReceiptAction` | מסרב לשורת `n_ah_income` מסוג `credit` |
| `issueCashReceiptAction` | מסרב להשכרה שנגבתה ב-`nedarim`/`route` |

בספר הראשי כפתור ההפקה מוצג רק על סוגי ההכנסה `laptops` (העברה מסניף) ו-`cash` (משיכה
מקופה). שורת `credit` לא מקבלת כפתור כלל.

**ההפקה ידנית בלבד, ולעולם לא אוטומטית.** לכן:

- שלושת מקומות הקריאה ל-`createEzcountReceipt` הם לחיצת כפתור מפורשת על שורה ספציפית.
- `/api/rentals/charge` (החיוב דרך נדרים) **לא מפיק שום מסמך**. `receiptPdfLink`/`receiptError`
  שעוברים ב-`handleChargeSuccess` מגיעים ריקים תמיד, בכוונה.
- `chargeViaRoute` (`lib/collection-charge.ts`) כותב רשומת `n_receipts` עם
  `status: "simulated"` — רישום פנימי, לא מסמך.
- לצד כפתור ההפקה יש תיבת סימון **"יצא מסמך"** שרק מסמנת ולא מפיקה דבר. זו האפשרות הנכונה
  לרוב התשלומים כאן, שנסלקו דרך נדרים.

**מה נדרש להגדיר** תחת `/routes`: מסלול גבייה עם `receiptsProvider = "ezcount"`,
`receiptsApiKey` = ה-API key של החשבון, ו-`receiptsCompanyId` = ה-developer email שלו. מסלול
המשוייך לסניף גובר על מסלול גלובלי; עמודת "קבלות" בטבלת המסלולים מראה אם ההגדרה שלמה.
ה-`developer_email` הוא פשוט **כתובת המייל שאיתה נכנסים לחשבון EZcount** — אין שדה בשם הזה
במסך ה-API שלהם, כך הם קוראים לה ב-API בלבד.

#### `/mobile` — ניידים (מעקב + העברות)

מסך אחד, שני חצאים: **מימין** מעקב הרווח פר מחשב, **משמאל** ההעברות של החודש. עד 2026-09
אלה היו שתי לשוניות (`/laptop-branches` ו-`/transfers`), והן אוחדו כי הן אותה שאלה משתי
זוויות — "הסניף הזה מרוויח לי מספיק?" ו"כמה הוא חייב לי בסוף החודש?" — ומעבר בין לשוניות
כדי להצליב ביניהן הבטיח שמסתכלים רק על אחת. **שתי הכתובות הישנות נשארו כ-`redirect`** לכאן,
עם הפרמטר שביקשו, כדי שסימנייה או קישור במייל לא ייפלו ל-404.

במסך שני צירי זמן נפרדים, בכוונה: `?end=` הוא סוף חלון 12 החודשים של המעקב, ו-`?month=` הוא
חודש ההעברות. החלון הוא השוואה על פני שנה וההעברה היא פעולה על חודש אחד; כפייה של אותו ערך
על שניהם הייתה מקצרת את אחד מהם בלי סיבה. כל ניווט בצד אחד שומר את הפרמטר של הצד השני.

##### הצד הימני — מעקב סניפים ניידים

טבלה אחת: חודשים לרוחב (חלון של 12, עם ניווט אחורה/קדימה), סניפים לאורך. בכל תא הרווח
**התפעולי** שלי מהסניף באותו חודש מחולק במספר המחשבים שהיו בו באותו חודש — מחשב שנוסף באמצע
נספר רק מהחודש שנוסף בו (`computersActiveInMonth` לפי `addedDate`). ירוק = עומד ביעד
`PROFIT_PER_COMPUTER_TARGET` (150 ₪ + מע"מ). **תא ריק = הסניף לא היה קיים** — לא אפס; ההבחנה
הזו היא כל התועלת של המסך. הליבה: `apps/web/lib/laptop-branch-tracking.ts`.

**המונה הוא `ownerOperatingProfitThisMonth`, לא `ownerNetProfitThisMonth`.** שורה שהבעלים
שילם והחוב כולו עליו — כלומר שורה שהסניף לא לוקח בה חלק — היא **רכש**: קניית מחשבים וציוד.
זו השקעה בסניף, לא עלות תפעול שלו, וחודש שנקנו בו שני מחשבים היה נראה כאן כמו חודש הפסד
בסניף שתפקד בדיוק כרגיל. הוצאות משותפות והוצאות שהסניף נושא בחלקן — כן נספרות. השאלה "כמה
באמת יצא לי מהכיס" נשאלת ב-`/dashboard/rentals/accounting` (עמודת "ששילמתי בפועל"), ולכן
`ownerNetProfitThisMonth` נשאר מה שהוא ולא שונה.

`computerProfitTrend` (רצועת 12 חודשים פר-סניף) ו-`buildComputerProfitTrend` **נמחקו**:
הם חישבו את אותה שאלה בכלל אחר — הרכש נספר בתוך הרווח — והשארתם הייתה מייצרת מספר שני
לאותה שאלה.

מתחת לטבלה: **אחוזים מהברוטו של הסניף** (`lib/revenue-shares.ts`). קדם לזה חישוב אנונימי
בשם "חלק המזכירה" (`SECRETARY_PCT`/`computeSecretaryShare`) שרץ על כל הסניפים שלי, על כל
החודשים מאז ומעולם, בלי שם ובלי זיכרון של מה כבר הועבר — הוא **נמחק**. במקומו הסדר מפורש:
אדם בשם, סניף אחד, **תאריך התחלה**, וחוב שמצטבר בצד ההעברות שבאותו מסך עד שמסמנים שהועבר.
הכרטיס כאן מציג רק את המצב לחודש הנבחר; הסכום עצמו כבר מנוכה מהרווח שבטבלה למעלה.
הסדר שהוגדר ולא נתפס על אף סניף מקבל כאן אזהרה צהובה — אחרת הוא שקט ולא מנכה כלום.

**הרווח-פר-מחשב בטבלה הוא אחרי האחוזים האלה**, כי `ownerOperatingProfitThisMonth` כבר
מנוכה בהם.

##### הצד השמאלי — העברות חודשיות

במסך עצמו יושבת **הטבלה המצומצמת** (`compact-transfers-table.tsx`): סניף, יתרה (מה שנגרר
מחודשים קודמים), צריך להעביר (השורה התחתונה, כולל היתרה) והועבר. זו ההחלטה היומית. מעליה
בורר החודש וכפתורי הדו"ח (תצוגה מקדימה, מייל בדיקה, שליחה לכל הסניפים), ולצדם **"טבלה
רחבה"** — כפתור שפותח בחלון את `UnifiedBranchesTable` המלאה, על כל עמודותיה. הפיצול הוא לפי
מה עושים ומה מבררים: ההוצאות, ההכנסות והקבלות של אותו חודש הם ההסבר מאחורי הסכום, והם לא
צריכים לתפוס חצי מסך כל יום.

שתי הטבלאות קוראות את אותן שורות מ-`apps/web/lib/branch-transfer-rows.ts`
(`buildBranchTransferRows` / `sumBranchTransferRows`), ולכן לא ייתכן מצב שהמצומצמת והרחבה
מראות שני סכומים לאותו סניף.

`UnifiedBranchesTable` (עברה לכאן מ-`/dashboard/rentals/accounting`, יחד עם
`transfer-mark-cell`, `receipt-checkbox`, `report-buttons`, `report-actions`, `branch-actions`)
— יתרה מחודש קודם, הוצאות, הכנסות, צריך להעביר, כולל חודש קודם, הועבר, הוצאנו קבלה.

שני שינויים מהותיים:

1. **הסניפים שלי לא מוצגים.** מסוננים ב-`isMine === false`; סניף שאני הבעלים היחיד שלו לא
   מעביר לי כלום. שורה כזו הייתה מוסיפה סכום שאף אחד לא אמור לשלם. שמות הסניפים שהושמטו
   מוצגים כהערה מתחת לטבלה.
2. **אחוזים שאני צריך להעביר, מצטברים** (`apps/web/lib/partner-payouts.ts`,
   `n_partner_payouts`). החוב לכל מי שיש לו אחוז מהברוטו מחושב כהפרש בין מה שהצטבר לזכותו
   לבין מה שנרשם ששולם, על פני כל החודשים — ולכן חודש שלא סומן ממשיך להופיע ביתרה עד
   שיסומן. מזהה המסמך דטרמיניסטי (`payoutDocId`), כך שסימון הוא upsert ולא יכול ליצור שתי
   רשומות לאותו חודש; `paidAmount` של 0 **מוחק** את הרשומה, כי "לא העברתי" הוא היעדר רשומה.
   שני סוגי הסדרים נכנסים לאותה טבלה (ראו `lib/revenue-shares.ts`):
   - **פר-מחשב** — מוגדר על המחשב עצמו (`Laptop.hasPartner` / `partnerName` / `partnerPct`,
     ברירת מחדל 15%), **בלי תאריך התחלה**: נספר מהיום הראשון שהמחשב הושכר.
     שותפות שסומנה **בלי שם** היא חוב בלי נושה: הסכום ממשיך לרדת מהרווח, אבל הכרטיס
     שלה בטבלה צהוב, עולה לראש הרשימה ומפנה לעריכת המחשב (`RevenueShareLine.unnamed`).
     גם ברשימת המחשבים היא מסומנת "חסר שם" ולא מוצגת כאחוז תקין.
   - **פר-סניף** — מוגדר בקוד ב-`BRANCH_REVENUE_SHARES`: אדם, אחוז, סניף ו-`startDate`.
     התאריך נחתך **פר-השכרה ולא פר-חודש**, כך שחודש ההתחלה מתחלק ולא נלקח במלואו.
     חל על **כל** הכנסות הסניף (השכרות שנגבו + שורות `n_branch_income`), ולא על מחשב
     מסוים. **אין תנאי `isMine`**: ההסדר מוגדר במפורש על סניף, והוא חוב שלי מול אדם
     שלישי — הוא יורד מהרווח שלי ולא נוגע בהתחשבנות מול השותף בסניף. שני מצבי כשל
     מסומנים במסך במקום להישאר שקטים: הסדר שלא נתפס על אף סניף (אזהרה צהובה),
     והסדר שנתפס על יותר מסניף אחד ולכן נגבה פעמיים (`ambiguousRevenueShares`,
     אזהרה אדומה). **שני ההסדרים מצטברים**: מחשב שיש עליו אחוז פרטי, בסניף שיש
     עליו אחוז כולל, נושא את שניהם.

**הדו"ח החודשי במייל** (`lib/branch-report-send.ts`, `/api/rentals/monthly-reports`) נשלח
אוטומטית ב-1 לחודש על החודש שנסגר (`apps/web/vercel.json`, cron `0 6 1 * *`, מוגן ב-
`REPORTS_CRON_SECRET`/`CRON_SECRET`), ומדלג על חודש שכבר נשלח (`reportSentAt`).
**סניף שלי לא מקבל מייל ואין לו הוראות העברה** — הסינון `isMine === false` חל גם על השליחה
וגם על התצוגה המקדימה.

#### ההוצאות של העסק עצמו — שלושה סוגים

ההוצאות שאינן של אף סניף. **שלושה** סוגים, והם שלוש תשובות לאותן שתי שאלות: האם ההוצאה
חוזרת כל חודש, והאם הסכום שלה ידוע מראש. עד 2026-09 הם ישבו במסך נפרד (`/extra-expenses`);
היום שלושתם נרשמים בחלון "הוספת הוצאה" של הספר הראשי ומוצגים בסרגל הטבלאות שלו.

1. **הוצאות קבועות משתנות** (`n_recurring_var_expenses`, `scope: "main"`) — משכורת מזכירה,
   מע"מ, חשמל. חוזרת, והסכום משתנה: המערכת מבקשת סכום לכל חודש שנסגר.
2. **הוצאות קבועות** (`n_ah_fixed_expenses`) — שכירות משרד, רואה חשבון, מנוי תוכנה. חוזרת,
   והסכום ידוע: שורה אחת עם סכום חודשי + תאריך התחלה, והיא נצברת לבד מחודש ההתחלה והלאה.
   כרטיס `main-fixed-expenses-card.tsx` עם טופס הוספה (שם · סכום חודשי · תאריך התחלה ·
   קטגוריה · תחום · `countsToMain`, שנפתח **מסומן** כי זו הוצאה של העסק) בחלון "הוספת
   הוצאה", וטבלת `fixed-expenses-table.tsx` עם הצבירה עד היום וסטטוס פעילה/הופסקה.
   **"הפסקה" ולא מחיקה**: `endDate`
   משאיר בספר את החודשים שההוצאה כן הייתה פעילה בהם, ומחיקה (שמוציאה אותה למפרע מכל
   החודשים) היא לטעות הקלדה בלבד. שינוי סכום = להפסיק את הקיימת ולפתוח חדשה, בדיוק כמו
   בהוצאה קבועה של סניף. פעולות: `createMainFixedExpenseAction` /
   `endMainFixedExpenseAction` / `deleteMainFixedExpenseAction` (`accounting/actions.ts`,
   owner בלבד).
3. **רכישות והוצאות חד-פעמיות** (`n_ah_expenses`, `countsToMain: true` כברירת מחדל).
   `linkedBranchIds` הוא **תיעוד בלבד**: קניתי משטח מחשבים, ההוצאה כולה שלי, ואני רק רוצה
   לזכור לאילו סניפים הם הלכו — בלי שייפתח מולם התחשבנות.

הוצאה קבועה של העסק נרשמה עד כה כרכישה חד-פעמית בכל חודש מחדש (שורה חדשה לתמיד, ואי אפשר
לראות אותה כשורה אחת) או כקבועה משתנה שמבקשת סכום כל חודש למרות שהסכום ידוע — בדיוק הפער
שהחלק השני סוגר.

#### הוצאות קבועות משתנות — הקטגוריה השלישית

`n_recurring_var_expenses` · `apps/web/lib/recurring-expenses.ts` ·
`apps/web/components/recurring-expenses/`.

חשמל הוא הוצאה **קבועה** — הוא חוזר כל חודש ורוצים לראות אותו כשורה אחת לאורך שנה. הוא גם
**משתנה** — הסכום אחר בכל חודש. שתי המערכות הקיימות לא ידעו להחזיק את שני הדברים יחד:
`n_fixed_expenses` הניחה סכום אחד לתמיד, ו-`n_var_expenses` הניחה אירוע חד-פעמי בלי המשך.

כאן זו שורה אחת (`name`, `scope`, `branchId?`, `startDate`, `frequency`, `spread`,
`countsToMain`) עם מערך `amounts` של `{month, amount, updatedAt}`. **חודש בלי סכום הוא לא
אפס — הוא "עוד לא עודכן"**, וזה מה שהתזכורת מחפשת (`missingMonths` / `buildReminders`):
"לא שילמתי חשמל" ו"לא רשמתי כמה שילמתי" הן שתי אמירות שונות לחלוטין. הכרטיס מציג טבלה של
חודשים ובכל תא חסר טופס קטן לעדכון במקום, ובראשו התראה על מה שחסר. `scope` קובע היכן
הכרטיס מופיע: `computers` בעמוד הסניף, `coworking` במסך ההוצאות של המשרד השיתופי,
`main` בטבלת "עדכון קבוע משתנה" של הספר הראשי (שמציגה למעשה את **כל** ה-scopes יחד).

**תדירות (`frequency`)** — `monthly` (ברירת המחדל, וגם מה שכל רשומה שנוצרה לפני השדה מקבלת),
`bimonthly`, `quarterly`, `yearly`. המחזור נמדד מחודש ה-`startDate`, ו-`dueMonths` מחזיר רק
את חודשי החיוב: ארנונה דו-חודשית שהתחילה ב-01/2025 נדרשת ב-01, 03, 05. התראה על חודש שאין בו
מה לשלם היא רעש, ורעש מלמד להתעלם מההתראות — ולכן התדירות היא שדה ולא הערה.

**שינוי תדירות באמצע הדרך (`frequencyChanges`, 2026-09)** — חשמל שהיה דו-חודשי ועבר לחודשי
הוא **אותה הוצאה**, ופתיחת שורה שנייה בשבילו שוברת בדיוק את הסיכום שבשבילו המודול קיים.
לכן `frequency` היא התדירות מ-`startDate`, ו-`frequencyChanges: {from, frequency}[]` הוא
קו-זמן של שינויים ממנה. `frequencySegments()` מפרק את חיי ההוצאה לתקופות, **והמחזור נמדד
מחדש בתחילת כל תקופה**: דו-חודשי מ-01/2026 נדרש ב-01, 03, 05, ומהרגע שהוא חודשי ב-06/2026
הוא נדרש ב-06, 07, 08. `dueMonths` רץ על התקופות, `frequencyOf()` מחזירה את התדירות
**הנוכחית** (לתצוגה) ו-`baseFrequency()` את ההתחלתית (זו שטופס ההגדרות עורך). שדה חסר =
תדירות אחת לכל החיים, בדיוק כמו קודם. ב-`monthlyAllocationDetailed` הפריסה נלקחת מהמחזור
של **חודש התשלום עצמו**, כדי ששינוי תדירות לא ישכתב למפרע את עלות העבר. הניהול ב-
`RecurringHistoryPanel` → "תדירות לאורך הזמן": רשימת התקופות, הוספת שינוי וביטולו.

**הפסקה במקום מחיקה (2026-09)** — לכל הוצאה קבועה משתנה יש בכרטיס **"הפסקה"** (עם תאריך)
ו**"חידוש"** (`RecurringStopControl`, `endRecurringVariableExpenseAction` /
`resumeRecurringVariableExpenseAction`), במקום שהדרך היחידה לסגור הוצאה שנגמרה תהיה מחיקה —
שמוציאה אותה מכל החודשים למפרע ומוחקת גם את מה ששולם. אותו פער נסגר גם בהוצאות הקבועות של
**המשרד השיתופי**, שהיו עד כה מחיקה בלבד: `endCoworkingFixedExpenseAction` /
`resumeCoworkingFixedExpenseAction` ו"סיום"/"חידוש" ברשימה (בחדרי מחשבים ובהשכרות "סיום"
כבר היה קיים). מחיקה נשארת למה שנרשם בטעות.

**פריסה (`spread`)** — ביטוח שנתי של 12,000 ₪ ששולם בינואר הוא 12,000 ₪ במזומן בינואר אבל
1,000 ₪ עלות בכל חודש. בלי פריסה ינואר נראה חודש אסון ושאר השנה נראית זולה מכפי שהיא, ואי
אפשר להשוות חודש לחודש. `monthlyAllocation` מחזיר לכל חודש כמה מההוצאה נופל עליו, וזה מה
שנכנס להנה"ח הראשית; `totalToDate` נשאר מה ששולם בפועל. שני המספרים אמיתיים ולכן שניהם
נשמרים. `spread: false` מחזיר את הכל לחודש התשלום. במחזור חודשי אין לשדה משמעות — ולכן
**התנהגות כל הרשומות הקיימות לא משתנה**.

**עריכת היסטוריה** (`RecurringHistoryPanel`) — הטבלה בכרטיס מציגה חלון של החודשים האחרונים,
וזה נכון לעבודה השוטפת ושגוי לגמרי כשרושמים במערכת הוצאה שקיימת כבר שנה: החודשים החסרים לא
נמצאים על המסך, אז ההתראה מתלוננת על מה שאי אפשר לתקן. הפאנל שמתחת לטבלה פותח את **כל**
החודשים מיום ההתחלה — כל אחד עם הסכום שלו לעריכה ולא רק החסרים, כי טעות הקלדה מלפני חצי שנה
היא אותו צורך — וכולל **מילוי מהיר** של טווח חודשים בסכום אחד (`fillRecurringHistoryAction`;
כותב רק לחודשי חיוב, ולא דורס חודש קיים אלא אם ביקשו) וניקוי חודש בודד
(`clearRecurringMonthAction`). באותו פאנל נערכות גם הגדרות ההוצאה עצמה, ובהן התדירות
והפריסה — כדי שאפשר יהיה להגדיר אותן גם על שורה שנוצרה לפני שהשדות היו קיימים.

**התזכורת בדף הבית** (`HomeRecurringReminders`, `/dashboard`) — ההתראה חיה גם בעמוד שנפתח
ממילא בכל בוקר, ולא רק בתוך מסך ההוצאות של הסניף: תזכורת שצריך לזכור להיכנס אליה אינה
תזכורת. הכרטיס מציג את כל ההוצאות שחסר להן חודש, עם שדה סכום במקום (מוצע: האחרון שנרשם)
כדי שהעדכון יקרה בלי לעזוב את הדף, וקישור למסך המלא. הסינון זהה ל-`requireAccess` שבפעולות:
בעלים רואה הכל, עובד סניף רואה רק את הסניף שלו, וכל scope מותנה בהרשאת המודול שלו — כדי
שלא יוצג טופס שכל שליחה שלו תיפול על "אין הרשאה".


#### רכישות חוזרות — סימון על ההוצאה, לא מודול

`n_expense_types` · `apps/web/lib/recurring-purchases.ts` ·
`apps/web/components/recurring-purchases/` (`expense-type-field.tsx`,
`recurring-purchase-badge.tsx`, `recurring-purchases-summary.tsx`).

נייר למדפסת, שקיות אשפה, פחיות. כל קנייה כזו היא באמת הוצאה חד-פעמית — קונים כשנגמר,
בסכום אחר ובתאריך לא צפוי — ולכן היא נשארת שורה רגילה בדיוק במקום שבו נרשמה:
`n_var_expenses` (סניף), `n_multi_branch_expenses` (כמה סניפים) או `n_ah_expenses`
(העסק עצמו). הכסף נספר בהנה"ח בחודש שבו יצא, ו**שום דבר כאן לא מזיז שקל ולא נוגע
ב-`main-ledger`**.

מה שחסר היה לא מקום אחר לרשום בו, אלא **הידיעה ששתי השורות האלה הן אותו מוצר**: שלוש
קניות נייר של 300 ₪ מפוזרות על השנה נראות כלום כל אחת לחוד, וביחד הן 900 ₪ שראוי לדעת
עליהם. `expenseTypeId` על ההוצאה הוא החוט שמחבר אותן.

**אין לזה מסך משלו (2026-09).** הלשונית `/dashboard/accounting/recurring-purchases`
נמחקה: מודול נפרד הוא מקום שצריך לזכור להיכנס אליו כדי לראות מה שאפשר להראות בדיוק במקום
שבו רושמים. מה שיש במקומו:

| איפה | מה |
|---|---|
| **טופס ההוצאה** (`ExpenseTypeField`) | "רכישה חוזרת (לא חובה)" בכל טופס הוצאה שוטפת בעסק: חדרי מחשבים, השכרות, משרד שיתופי, הוצאה על כמה סניפים, ו**"הוצאות נוספות" בהנה"ח הראשית** (`n_ah_expenses`, שלא היה מסומן עד כה). זה הרגע היחיד שבו באמת יודעים את התשובה |
| **תג על השורה** (`RecurringPurchaseBadge`) | כל הוצאה מסומנת אומרת ליד עצמה מה המוצר וכמה הלך עליו השנה **בכל העסק**. זה החיבור, ושם הוא נראה |
| **בלוק מתקפל בתחתית מסך ההוצאות** (`RecurringPurchasesSummary`) | הסוגים שמופיעים במסך הנוכחי: כמה קניות השנה, סה"כ, חלוקה ל-12 (להשוואה מול הוצאה קבועה — מספר של דוח בלבד), כמה סניפים וקנייה אחרונה |
| **טבלת "רכישות חוזרות" בספר הראשי** (`recurring-purchases-table.tsx`) | אותם מספרים בדיוק, כטבלה מסוננת וממוינת — עדיין לא מסך משלה אלא אחת מחמש הטבלאות של `/dashboard/accounting` |

**הסוגים גלובליים.** `loadRecurringPurchaseTypes()` כבר לא מסננת לפי מודול, והדדופ
ב-`resolveExpenseTypeIdFromForm` הוא לפי שם בלבד: אותו "נייר למדפסת" שנקנה בחדר מחשבים
ובהשכרות הוא אותו מוצר. הסינון הישן הוא בדיוק מה שמנע את החיבור — הוא יצר סוג שני באותו
שם בכל מודול, וכל אחד החזיק חצי מהמספר. `module` על הסוג נשאר כתיעוד של איפה הוא נוצר.

**הסניפים נגזרים מהרכישות** (`branchesOfPurchases`) ולא מוגדרים על הסוג: הקנייה כבר יודעת
אם היא של סניף אחד, של כמה סניפים או של כל הסניפים (`shared-*`), ורכישה של העסק עצמו לא
מביאה סניף. `RecurringPurchaseType.branchIds` הוסר — הוא היה אותו מידע פעמיים, ולא מסונכרן
ברגע שנפתח סניף.

**הפסקה ולא מחיקה**: סוג שהופסק (`archived`) לא מוצע בטפסים וההיסטוריה שלו ממשיכה להיספר.

**מתי ההתראה נדלקת (2026-09):** רק על חודש ש**נסגר**. `buildReminders` מקבל את החודש
הנוכחי ובונה את ההתראות עד `lastClosedMonth()` — החודש שלפניו — ולכן חשמל ספטמבר נדרש
ב-1 באוקטובר, לא במהלך ספטמבר. קודם ההתראה נדלקה על החודש הרץ כבר ב-1 בו, לפני שהחשבון
בכלל הגיע, ולכן היא הייתה דולקת כל החודש ואיבדה את המשמעות. תא החודש הנוכחי נשאר פתוח
להזנה מוקדמת — הוא פשוט לא נצבע בכתום ולא נספר ב"צריך עדכון". `missingMonths` עצמו לא
שינה משמעות (הוא עונה "אילו חודשים חסרים עד `upto`"); מי ששואל על פיגור מעביר לו
`lastClosedMonth()` — וכך גם `RecurringHistoryPanel` בסימון "חסר". `dueNow` בהתראה הוא
עכשיו "החודש שנסגר זה עתה חסר", כלומר מה שצריך לעדכן היום, ו-`overdue` הוא כל מה שישן
ממנו. עם תדירות רב-חודשית הכלל זהה: נספרים רק חודשי חיוב (`dueMonths`) שכבר נסגרו.


### משימות ונהלים (`/dashboard/duxus`) — perm: duxus

מודול ניהול/תפעול פנימי (השם המוצג "משימות ונהלים"; הנתיב הטכני `/dashboard/duxus`
ומפתח ההרשאה `duxus` נשארו כשם קוד פנימי). **המודול נבנה מחדש (2026-09) לפי מסמך
האפיון "אפיון מודול משימות ונהלים"** - כל הפניה לסעיף בהמשך היא לסעיף באותו מסמך.

ארבע לשוניות תחת `DuxusTabs` (`duxus-tabs.tsx`), בסדר העבודה:

1. **משימות ויעדים** (`/dashboard/duxus/rocks`) — ברירת המחדל; מתפצלת לארבע
   תת-לשוניות (`tasks-tabs.tsx`): **השבוע** · **החודש** · **הרבעון** · **ארכיון**.
2. **נהלים** (`/dashboard/duxus/procedures`) — מאגר הנהלים.
3. **היסטוריה** (`/dashboard/duxus/history`) — תוכנית מול ביצוע ומדדי תקופה.
4. **הגדרות** (`/dashboard/duxus/settings`) — שבוע עבודה, יום אזהרה, מספר סלעים מומלץ.

#### החלטת היסוד: אבן דרך נוצרת פעם אחת בלבד

**רבעון, חודש ושבוע אינם עותקים של המשימה אלא *שיוכים* שלה לתקופות עבודה.**
אבן דרך אחת חיה ב-`n_milestones`, הסטטוס שלה נשמר בה פעם אחת, וההתחייבות לכל
תקופה יושבת ב-`n_period_assignments`. מכאן נגזר הכל:

- סימון "הושלם" במסך השבוע מתעדכן **מיד** גם בחודש, ברבעון ובהיסטוריה - כי אין מה
  לסנכרן, זו אותה רשומה (קריטריון קבלה 1).
- אין `completedMonthly`/`completedWeekly`, אין שכפול רשומות בין תקופות, ואין
  "חוב משימות" שמוחק היסטוריה - משימה שלא הושלמה פשוט מוצעת לשיבוץ מחדש.
- מזהה השיוך דטרמיניסטי (`${milestoneId}__${periodType}__${periodKey}`), ולכן
  **שיוך כפול לאותה תקופה נחסם ברמת בסיס הנתונים** ולא רק בממשק (קריטריון קבלה 2).

#### תאימות לאחור בלי מיגרציה

הדאטה שנוצר לפני המודל הזה ממשיך לעבוד כמות שהוא, בשתי גזירות בזמן קריאה
(`toMilestone` ו-`synthesizeAssignments` ב-`rocks/actions.ts`):

- `status` נגזר מ-`done` כשהשדה חסר.
- `monthKey`/`weekKey` הישנים הופכים לשיוכי חודש/שבוע מסונתזים, ולכל אבן דרך
  שהרבעון הוא ביתה נוצר שיוך-רבעון **משתמע** (היא שייכת לרבעון דרך הסלע שלה, ואין
  טעם לכתוב על כך רשומה). שיוך שנכתב בפועל תמיד גובר - אותו מזהה דטרמיניסטי.

**אין כתיבה ל-DB בזמן רינדור ואין סקריפט מיגרציה.**

#### סטטוסים וחישוב התקדמות (סעיף 6)

רק **אבן דרך** מקבלת סטטוס ידני: `not_started` · `in_progress` · `waiting` ·
`done` · `cancelled`. הכללים נאכפים בשרת, לא רק בממשק:

- `waiting` מחייב הערה ("במי/במה תלוי"), `cancelled` מחייב סיבת ביטול.
- **פתיחה מחדש היא פעולה מפורשת** (`reopenMilestoneAction`) ולא לחיצה חוזרת על
  תיבת הסימון - היא דורשת סיבה, מגדילה `reopenCount`, ונרשמת ביומן. תאריך
  ההשלמה הקודם יורד מהרשומה אך **נשאר ביומן**.
- **אחוז ההתקדמות = הושלמו / פעילות, ללא מבוטלות.** תת-סלע/סלע נחשב "הושלם" רק
  כשכל הפעילות הושלמו **וקיימת לפחות אחת** - תת-סלע ריק לעולם לא מתחזה להושלם.
- הוספת אבן דרך לתת-סלע שהושלם **פותחת אותו ואת הסלע שמעליו מחדש** אוטומטית
  (`recomputeRockCompletion`, מטפס גם לאב) - קריטריון קבלה 6.

`task-status.ts` מרכז את כל הלוגיקה הזו כפונקציות טהורות, ולכן השרת והלקוח
מחשבים בדיוק אותו דבר.

#### התראות וצבעים (סעיף 9)

העיצוב רגוע בכוונה: **אין צביעה של שורה שלמה בצבע חזק** - כל גוון מוגדר ב-`TONES`
כשלישייה של פס צד דק, נקודה ותג קטן. סדר העדיפויות: הושלם/בוטל → עבר יעד (אדום
עדין) → ממתין (סגול) → אזהרת שבוע (כתום) → בביצוע/טרם התחיל.

- **האזהרה הכתומה** מגיעה רק למשימת שבוע שטרם התחילה, ורק מיום האזהרה ואילך.
  היום עצמו הוא **הגדרת מערכת** (`warningWeekday`, ברירת מחדל רביעי) ולא מקובע בקוד.
- **תג אזהרה לתת-סלע בלבד** (`rockWarning`): עבר יעד, או שכל מה שנבחר בו לתקופה
  עדיין לא התחיל. הסלע מציג סיכום של ילדיו ואינו נצבע ידנית.
- `today` ו-`weekWarningActive` **מחושבים בשרת** ומועברים כ-props (`board-loader.tsx`),
  כדי שהשרת והלקוח יציירו זהה ולא ייווצר פער הידרציה על גבול חצות.

#### המסכים

- **השבוע** (`rocks/week`) — מה עושים עכשיו. כל הפעולות זמינות מהשורה בלי לפתוח
  חלון (סטטוס, סימון בוצע, אחראי, הערה). בראש הרשימה "לא הושלם משבוע קודם" עם
  התחייבות מחודשת בלחיצה, ומה שהושלם יורד ל-`<details>` מכווץ בתחתית. דפדוף בין
  שבועות; שבוע שאינו הפתוח מוצג לקריאה בלבד.
- **החודש** (`rocks/month`) — תקציר בראש המסך (נבחרו/הושלמו/בביצוע/טרם התחילו/
  ממתינים/איחורים, `period-summary.tsx`), ואבני הדרך מקובצות לפי סלע ותת-סלע. אבן
  דרך שנמשכה לשבוע **ממשיכה להופיע כאן** עם התג "בשבוע" - כי היא אותה רשומה.
- **הרבעון** (`rocks/quarter`) — התכנון עצמו: עץ הסלעים המלא (`RockMilestoneTree`),
  הוספה ועריכה מהירה בכל רמה, וההתקדמות המחושבת. **הסימון "בוצע" לא מוצג כאן** -
  הוא נעשה בשבוע ובחודש, שם עובדים בפועל. הוספת סלע מעבר למספר המומלץ מציגה
  **אזהרה ולא חסימה** (סעיף 7.1).
- **ארכיון** (`rocks/archive`) — ציר הזמן של כל הרבעונים עם סיכום לכל אחד, ארכוב/
  החזרה לפעיל ומעבר לאשף הרבעון החדש.

#### מצב ישיבה - בחירה מרובה (סעיף 7.4)

`meeting-picker.tsx` הוא הלב של הישיבה החודשית והשבועית: עשרות שורות, תיבת בחירה
קבועה ליד כל אחת, **בלי לפתוח אף משימה**, ואישור אחד לכולן.

- סינון לפי סלע, אחראי, סטטוס, תאריך יעד וטקסט חופשי - ו**הסינון אינו מאפס
  בחירות** שכבר נעשו (קריטריון קבלה 11): הבחירה חיה ב-`Set` נפרד מהתצוגה.
- "בחר הכול המוצג", "נקה בחירה", ו"בחר תת-סלע" בשורת הקבוצה.
- אבן דרך שכבר משויכת לתקופה מוצגת **מסומנת ומנוטרלת**, כדי שלא יווצר שיוך כפול.
- סרגל פעולה דביק בתחתית עם מונה הנבחרים, שנשאר גלוי בגלילה.

#### חלונית הצד של אבן דרך (סעיף 10)

`milestone-panel.tsx` - **חלונית צד, לא עמוד חדש**: פרטים, סלע ותת-סלע (כולל
העברה לתת-סלע אחר, שנרשמת ביומן), אחראי, עדיפות, יעד, הערות, שיוכי התקופות עם
התוצאה של כל אחד, ויומן הפעולות המלא לפי סדר כרונולוגי. סגירה עם שינויים שלא
נשמרו מציגה אזהרה.

**נעילה אופטימית:** `updateMilestoneAction` מקבל את `updatedAt` שהלקוח ראה ודוחה
עדכון על גרסה ישנה עם בקשת רענון, במקום דריסה שקטה (סעיף 14).

#### המשכיות בין תקופות ונעילת תוצאות (סעיף 8)

פתיחת תקופה חדשה **לא מוחקת ולא נועלת** את הקודמת, אבל כן **נועלת את תוצאות
ההתחייבות** שלה (`closePeriodOutcomes`): כל שיוך שנשאר `open` מקבל `done` /
`cancelled` / `missed` לפי הסטטוס באותו רגע. כך משימה שלא הושלמה נשארת מתועדת
כ"לא הושלמה" בשבוע הישן **גם אם תושלם אחר כך** - זו האמת ההיסטורית.

#### פתיחת רבעון חדש (`rocks/rollover`)

האשף מציג את כל מה שפתוח ברבעון הנוכחי, ומה שנבחר **אינו משוכפל**: אותה אבן דרך
מקבלת שיוך-רבעון נוסף, `carryOverCount` גדל ב-1, והסלע שלה נגרר איתה לתצוגה ומסומן
"מרבעון קודם". מה שכבר הושלם או בוטל נשאר ברבעון הישן ולא מוצע לבחירה.

#### ארכיון = קריאה בלבד (נאכף בשרת)

כל פעולת כתיבה עוברת דרך `quarterWriteBlock` / `milestoneIdsWriteBlock` /
`rockWriteBlock` ומוחזרת עם הודעה בעברית - הגנה כפולה, לא הסתרת כפתורים.

**חריג מכוון:** `milestoneIdsWriteBlock` בודק גם אם לאבן הדרך יש שיוך-רבעון לרבעון
**פעיל**. אחרת כל מה שגולגל קדימה היה הופך לקריאה בלבד ברגע שהרבעון שבו נולד עבר
לארכיון.

#### מחיקה: שתי דרגות מפורשות (סעיפים 10, 14, 15)

האפיון דורש מחיקה לוגית לכל מה שיש לו היסטוריה, אבל בפועל צריך גם דרך להיפטר
באמת מדאטה. לכן כל מחיקה נפתחת ב-`DeleteDialog` (`delete-dialog.tsx`) ולא
ב-`confirm` - כי יש **שתי** תשובות אפשריות ולא אחת, והדיאלוג מציג את המספרים
האמיתיים (שנטענים מהשרת) לפני שמאשרים:

- **ארכוב** - ברירת המחדל והאפשרות השמרנית: `deletedAt` על הרשומה, היא יורדת
  מהלוח ונשארת בהיסטוריה ובדוחות. מוצג רק כשיש מה לשמור.
- **מחיקה לצמיתות** - הרשומה, שיוכי התקופה שלה ויומן הפעולות שלה נמחקים באמת.
  **בלתי הפיך**, ולכן כשהמחיקה גוררת איתה דברים נוספים היא דורשת הקלדת המילה
  "מחיקה".

| ישות | ארכוב | מחיקה לצמיתות |
| --- | --- | --- |
| אבן דרך | `deletedAt` (בלי שיוך ובסטטוס `not_started` נמחקת פיזית ממילא) | הרשומה + שיוכיה + היומן שלה |
| סלע / תת-סלע | מחיקה לוגית מדורגת לכל התת-עץ (סלע ריק נמחק פיזית ממילא) | כל התת-עץ: סלעים, אבני דרך, שיוכים ויומן |
| רבעון | `status: "archived"` - קריאה בלבד, הכל נשאר | הרבעון, סלעיו, אבני הדרך שנולדו בו, כל השיוכים שנוגעים להם, סיכומי הישיבות הרבעוניות והיומן |
| נוהל | `status: "archived"` | מחיקת המסמך |

**מחיקת רבעון** (`deleteQuarterAction` + `DeleteQuarterButton`) היא הדרך היחידה
להיפטר באמת מכמות גדולה של דאטה, ולכן:

- `getQuarterDeletionSummary` מחזיר תחילה את הספירות המדויקות, והדיאלוג מציג אותן.
- אבני דרך שהגיעו לרבעון **מרבעון אחר לא נמחקות** - הבית שלהן במקום אחר, ורק
  ההתחייבות אליהן כאן יורדת. להפך: אבני דרך של הרבעון הנמחק שמשובצות גם ברבעון
  אחר **ייעלמו גם משם**, והדיאלוג מזהיר על כך במספרים.
- נמחקים גם שיוכים שנעשו בלוח של רבעון אחר, כדי שלא יישארו שיוכים יתומים.
- המחיקה רצה ב-`purgeDocs` בקבוצות של 450, כדי לא להיחסם במגבלת ה-batch.
- **רישום היומן על המחיקה נכתב אחרי הניקוי**, כך שהוא שורד אותו ומתעד מה נמחק,
  מתי ועל ידי מי.

**הסרה משיוך** מותרת רק לתקופה **פתוחה** - שיוך שתוצאתו ננעלה הוא אירוע עבר ואין
למחוק אותו (סעיף 16). מי שרוצה להיפטר ממנו מוחק את אבן הדרך או את הרבעון.

#### יומן פעילות (סעיף 16)

`logActivity` רושם לכל פעולה משמעותית מי, מתי ומה השתנה. כשיש `batch` הרישום נכנס
**לאותה טרנזקציה** של השינוי עצמו, כך שלא ייתכן שינוי שלא תועד; כשאין - הכתיבה
מומתנת ולא fire-and-forget. היומן אינו ניתן לעריכה דרך הממשק.

#### היסטוריה ומדדים (`/dashboard/duxus/history`, סעיף 11)

תוכנית מול ביצוע, עם סינון לפי תקופה, סלע, אחראי, סטטוס, מקור יצירה וטקסט חופשי.
לכל אבן דרך מוצגת שרשרת השיוכים שלה וצבע התוצאה של כל אחד. מעליה מדדי תקופה:
מספר התחייבויות, כמה נסגרו, אחוז השלמה בזמן, כמה נדחו, כמה נוספו תוך כדי וכמה
נפתחו מחדש. **המדדים נגזרים מהדאטה ואינם ניתנים לעריכה ידנית.** כל הדאטה נטען
פעם אחת בשרת (`loadHistory`) והסינון הוא צד-לקוח, ולכן מיידי.

#### נהלים (`/dashboard/duxus/procedures`, סעיף 12)

מאגר מסודר, נפרד לחלוטין מלוגיקת המשימות: כותרת, תיאור קצר, קטגוריה חופשית
(רשות), גרסה, מצב (טיוטה/בתוקף/הוחלף/בארכיון), אחראי, קובץ PDF/Word מצורף ותוכן
HTML עשיר (`rich-editor.tsx` עצמאי למודול). חיפוש לפי שם, קטגוריה וטקסט תיאורי;
נהלים בארכיון מוסתרים כברירת מחדל.

**העלאת גרסה חדשה אינה מוחקת גרסה קודמת:** "גרסה חדשה" יוצרת נוהל חדש עם
`supersedesId` שמצביע על הקודם, והקודם עובר ל-`superseded`. שני הכיוונים של
השרשרת מקושרים בעמוד הנוהל.

#### טעינה ורענון

`loadQuarterBoard` שולף את כל לוח הרבעון (רבעון, סלעים, אבני דרך, שיוכים, הגדרות)
ב**שליפה אחת**, וכל ארבעת המסכים נגזרים ממנה - ולכן אין מצב ששני מסכים יראו
מצב שונה (סעיף 15). `useBoard` (`use-board.ts`) מחזיק עותק מקומי לעדכון אופטימי
ומחזיר את המצב הקודם בכל כישלון.

**כל פעולת כתיבה קוראת ל-`revalidatePath("/dashboard/duxus", "layout")`** - ברמת
המודול כולו ולא ברמת הנתיב הספציפי, כדי שסימון "הושלם" במסך השבוע יופיע מיד גם
בחודש, ברבעון ובהיסטוריה.

### הדרכות (`/dashboard/tutorials`)
מאגר הדרכות עם עורך עשיר (`rich-editor.tsx`), קבצים מצורפים, תמונות והדפסה.

### חנות AI (`/shop` ציבורי + `/dashboard/shop` ניהול) — perm: shop

**`/shop`** — עמוד ציבורי (`app/shop/`, לא מוגן ב-middleware שמכסה רק `/dashboard/:path*`, ולכן נגיש
בלי התחברות) עם צ'אטבוט-אשף לבניית מחשב מותאם אישית:
1. הלקוח מתאר בטקסט חופשי למה הוא צריך מחשב (או בוחר תחום ישירות). זיהוי התחום
   (`detectUseCase`, `apps/web/lib/shop-recommender.ts`) הוא **מנוע חוקים מבוסס מילות מפתח -
   לא מחובר למודל שפה חיצוני**, כדי לא לדרוש מפתח API/עלות. תחומים: תורה, משרדי, גרפיקה, וידאו,
   גיימינג, תכנות, כללי.
2. שאלות המשך ממוקדות פר-תחום (`getFollowUpQuestions`) - כל תשובה נושאת `loadDelta` שמצטבר
   לרמת עומס 1-5 (`computeLoadLevel`).
3. `buildRecommendedOptions` בונה שלוש הצעות (מינימלי/מומלץ/מוגזם): אם יש פריט פעיל תואם
   ב-`n_shop_catalog` (`matchCatalog`, לפי תחום+דרגה+רמת עומס) - מוצג השם/מפרט/מחיר האמיתיים
   שלו; אחרת נופל חזרה למפרט גנרי לפי "סולם" מפרטים פר-תחום (קבוע בקוד, ללא מחיר סופי -
   "יימסר בהצעת מחיר").
4. הלקוח בוחר תצורה, מוסיף ציוד נלווה (מ-`n_shop_addons` הפעיל, או רשימת ברירת מחדל גנרית אם
   ריק), ומזין שם (רשות)/אימייל/טלפון (חובה).
5. `createShopLeadAction` (`app/shop/actions.ts`) שומר את כל השיחה (תשובות, ההמלצות שהוצגו,
   הבחירה, פרטי קשר) כ-`n_shop_leads` חדש עם `status: "new"` - **אינו** מבצע חיוב/הזמנה, רק
   פנייה שהצוות חוזר אליה טלפונית/במייל.
6. בו-זמנית נבנה ומורד ללקוח קובץ HTML עצמאי (`buildQuoteHtmlDocument`,
   `apps/web/lib/shop-quote.ts`) - הצעת מחיר ראשונית מעוצבת (לוגו/צבעי אולטרנט) שניתן לפתוח או
   להדפיס כ-PDF ישירות מהדפדפן; אין תלות בספריית PDF חיצונית.
   הדף עצמו מסומן `export const dynamic = "force-dynamic"` כדי לא להיבנות כדף סטטי בזמן
   `next build` (הוא קורא קטלוג/לוגו חיים מ-Firestore בכל בקשה).

**`/dashboard/shop`** — ניהול המודול (`shop-tabs.tsx`, שלושה טאבים):
- **לידים** (ברירת מחדל) — כל הפניות מ-`n_shop_leads`, מיון לפי תאריך; עדכון סטטוס
  (חדש/יצרנו קשר/נסגר) ומחיקה inline (`lead-row-actions.tsx`), ופירוט שיחה מלא (`<details>`).
  כפתור "פתיחת הצ'אטבוט הציבורי" לתצוגה מקדימה.
- **קטלוג מחשבים** (`/catalog`) — CRUD על `n_shop_catalog`: שם, דרגה, תחומי שימוש (בחירה
  מרובה), רמת עומס מינימלית, מעבד/זיכרון/אחסון/כרטיס מסך, מחיר (רשות - ריק = "יימסר בהצעת
  מחיר"), פעיל/לא פעיל. כל עוד ריק, `/shop` מציג מפרט גנרי בלבד.
- **ציוד נלווה** (`/addons`) — CRUD על `n_shop_addons`: שם, סוג (תיק/עכבר/מקלדת/דיסק און
  קי/אחר), מחיר (רשות), פעיל/לא פעיל.

כל הדף הציבורי משתמש באותו `logoUrl` מ-`n_label_settings/default` שמוצג גם בדשבורד (עם
וורדמארק טקסטואלי "אולטרנט" כברירת מחדל כשאין לוגו מוגדר) - אין מסלול לוגו נפרד לחנות ה-AI.

**הגשת הלוגו (`apps/web/lib/branding.ts` + `GET /api/branding/logo`)** — הלוגו שמור
כ-data URI בבסיס 64, ולכן **אף מסך לא משבץ אותו ישירות ב-HTML שלו**. `getLogoSrc()` מחזיר
כתובת קצרה `/api/branding/logo?v=<hash>` שהדפדפן מוריד פעם אחת ושומר בקאש; ה-hash הוא של
תוכן הלוגו, כך שלוגו חדש = כתובת חדשה והמסלול יכול להחזיר `immutable` בלי סכנת קאש ישן.
הקריאה ל-Firestore מקושרת (cache של דקה בתהליך), ו-`updateLabelSettingsAction` קורא
ל-`invalidateLogoCache()` כדי שלוגו שהוחלף יופיע מיד. המסלול ציבורי בכוונה (ה-middleware
מגן רק על `/dashboard/*`) כי הלוגו מוצג גם בעמוד ההתחברות וב-`/shop`. הדו"ח שנשלח במייל
ממשיך לקבל את ה-data URI הגולמי דרך `getStoredLogo().value` — `logoForEmail` צריך את
הבסיס-64 עצמו כדי לבנות את הצרופה עם `cid:`.

### בדיקת נתונים — שאריות מהעבר (`/dashboard/maintenance`) — owner בלבד

המערכת יושבת על ה-DB של `app.html` **בלי מיגרציה**, וכל מסך במודולים מסנן לפי הסניף
הנבחר. לכן רשומה שה-`branchId` שלה מצביע על סניף שנמחק, על סניף מסוג אחר או על מסמך
שכבר לא קיים **נופלת מכל המסכים** — אבל ממשיכה להיספר בדיוק במקומות שלא מסננים:
כרטיסי ההתראות בדף הבית ו-`loadMainLedger`. זה מייצר את המצב הגרוע ביותר בתוכנת ניהול —
**התראה על כסף בלי שום מסך שאפשר לראות או לתקן בו את המקור** — ולכן קיים המסך הזה.

`lib/leftovers.ts` (`loadLeftovers`) סורק וקובץ לארבע קבוצות, ולכל שורה: מה זה, **למה**
אף מסך לא מציג אותה, וכמה כסף מעורב (כולל אם הוא נספר בהנה"ח הראשית):

| קבוצה | מה נבדק |
| --- | --- |
| השכרות משרד שיתופי ללא סניף פעיל | `n_cw_clients` שה-`branchId` שלהן אינו סניף `coworking` חי. קישור למסך העמדות, ששם אפשר לשייך אותן |
| הוצאות קבועות משתנות בלי מסך | `n_recurring_var_expenses`: scope `rentals` (אין מסך שמציג אותו כלל), scope `computers`/`coworking` עם סניף שאינו חי, סניף שני במשרד השיתופי (המסך מציג רק את הראשון), ו-scope לא מוכר. scope `main` תמיד גלוי ולכן אינו שארית |
| הוצאות על סניף שלא קיים | `n_fixed_expenses` / `n_var_expenses` עם `branchId` חסר, מחוק או לא קיים. סנטינלי הוצאה משותפת (`shared-*`) אינם שארית |
| עמדות ללא סניף פעיל | `n_cw_stations` של סניף שאינו משרד שיתופי חי |

**מחיקה**: `deleteLeftoverAction` מוגבל לבעלים **ולרשימת קולקשנים סגורה**
(`LEFTOVER_COLLECTIONS`), כדי שהמסך לא יהפוך למחיקה כללית לפי מזהה. האישור בדפדפן חוזר
על הערת הכסף, כי מחיקה של שורה שנספרת בראשי היא החלטה חשבונאית ולא ניקיון. **אין כאן
שום מחיקה אוטומטית** — הדוח מציג, ההחלטה אנושית. הכניסה היא אריח "בדיקת נתונים" בדף
הבית, שמוצג לבעלים בלבד.

### מודולים רוחביים
- **משתמשים** (`/users`) — ניהול משתמשים והרשאות (owner).
- **סניפים** (`/branches`) — ניהול סניפים כללי.
- **תפעול** (`/operations`) — עדכון מלאי (V/X) ומשימות שבועיות/חודשיות.

---

## 9. אינטגרציות חיצוניות

- **Firebase / Firestore** — מסד הנתונים. גישת אדמין דרך `lib/firebase-admin.ts`
  (web) ו-`firebase.service.ts` (api). פרויקט: `ultranet-e94aa`.
- **Nedarim Plus** (`lib/nedarim.ts`) — סליקה ישראלית.
  `resolveNedarimCreds(branchId?, routeId?)`: אם ניתן `routeId` מפורש (מסלול
  ה-`collectionRouteId` שנשמר על לקוח השכרות מסוים) הוא המקור היחיד — נכשל
  (`null`) ולא נופל חזרה אם המסלול לא תקין, כדי לא לחייב טעות בטעות דרך עסק
  לא נכון. אחרת: קודם מסלול משויך לסניף (`branch.collectionRouteId`), אחרת
  מסלול גלובלי (`branchScope === null`) כברירת מחדל של הבעלים.
  `listNedarimRoutes()` מחזירה את כל המסלולים המחוברים (לבחירת עסק בממשק, ראו
  `defaultForNewCards` למטה). חיוב דרך `/api/rentals/charge` ו-`lib/collection-charge.ts`.
  שמירת טוקן בלבד, לא כרטיס.
- **Resend** — מותקן כתלות (`apps/web/package.json`) אך **לא בשימוש כרגע בשום
  מקום בקוד** - זמין לשימוש עתידי.
- **EmailJS** (`@emailjs/browser`) — שליחת קודי התחברות/אימות מכשיר מהלקוח
  (`login/email-code-form.tsx`, `verify-device/verify-device-form.tsx`). זו
  שיטת שליחת המיילים היחידה הפעילה כרגע במערכת.
- **EZcount** (`lib/ezcount.ts`) — הפקת קבלות (מסמך מסוג 400, ללא מע"מ) ושליחתן
  אוטומטית במייל ללקוח. `resolveEzcountCreds(branchId)` פותר creds (מפתח API +
  developer_email) מ-`n_collection_routes` לפי אותו דפוס resolve-per-branch
  של `resolveNedarimCreds` (מסלול ספציפי לסניף → מסלול גלובלי `branchScope
  === null`). `createEzcountReceipt(...)` שולח ל-`api.ezcount.co.il/api/createDoc`.
  מופעל אוטומטית אחרי חיוב אשראי מוצלח (`/api/rentals/charge`), וגם ידנית עבור
  תשלומי מזומן/העברה דרך `issueCashReceiptAction`.
- **מסלולי גבייה** (`n_collection_routes`) — הפשטה מעל ספקי סליקה
  (`nedarim_plus` / `tranzila` / `cardcom` / `payplus` / `meshulam` / manual)
  וספקי קבלות (`ezcount` / `icount` / `green_invoice`), כולל יעד הפקדה ועמלות.
  **חשוב:** מלבד `nedarim_plus` (סליקה בפועל) ו-`ezcount` (הפקת קבלות בפועל),
  שאר הספקים (גם לסליקה וגם לקבלות) עדיין placeholder בלבד - `chargeViaRoute`
  לא מבצע קריאת API אמיתית עבורם, רק רישום הנה"ח מדומה.
  שדה `defaultForNewCards` (boolean, רשות) מסמן מסלול נדרים פלוס יחיד כברירת
  מחדל לשיוך כרטיסים **חדשים** של לקוחות השכרות (וגם לחיובים חד-פעמיים ללא
  טוקן) — נערך דרך תיבת סימון בטופס המסלול (`/dashboard/accounting/routes`).
  משמש כדי לנתב הכנסות חדשות לעסק מסוים בלי לגעת בכרטיסים שכבר נשמרו: כרטיס
  קיים תמיד ממשיך להיגבות דרך המסלול שהוא נוצר תחתיו (`RentalClient.collectionRouteId`,
  ראו סעיף 6), בעוד שכל כרטיס/חיוב חדש נופל כברירת מחדל למסלול המסומן, עם
  אפשרות לבחור מסלול אחר בממשק בזמן השמירה/החיוב.

### PWA — התקנה כאפליקציה (טאבלט / מולטימדיה ברכב)

`apps/web` הוא Progressive Web App מלא. אין קוד native נפרד — אותו Next.js
app שרץ בדפדפן ניתן ל"התקנה" כאפליקציה עם אייקון ומסך standalone (בלי סרגל
דפדפן) על טאבלט, ועל כל מערכת מולטימדיה ברכב שמריצה Android אמיתי (כמו רוב
יחידות ה-head unit שמאפשרות התקנת APK חיצוני) — **דרך אותו APK עטיפה אחד**,
לא פיתוח נפרד.

- `apps/web/public/manifest.webmanifest` — שם/אייקונים/צבעי מותג/`display:
  standalone`. `start_url` הוא `/dashboard`.
- `apps/web/public/icons/` — סט אייקונים (192/512/512-maskable/apple-touch),
  פלייסהולדר בצבעי המותג (`teal`/`ink`) עד שיוחלף בלוגו אמיתי.
- `apps/web/public/sw.js` + `apps/web/app/pwa-register.tsx` — Service Worker
  ורישומו. אסטרטגיה: **network-first** לניווט/דאטה (כדי שכל דיפלוי יתעדכן
  מיידית כשיש חיבור), **cache-first** רק לנכסים סטטיים עם hash
  (`/_next/static/...`). ה-cache מנוקה בכל `activate` כך שאין דליפת גרסה ישנה.
  כשמזוהה Service Worker חדש (=דיפלוי חדש) והוא הופעל, הלקוח מרענן את העמוד
  אוטומטית.
- `apps/web/next.config.mjs` — `headers()` ל-`/sw.js` ול-`/manifest.webmanifest`
  עם `Cache-Control: no-cache` כדי שדפדפנים תמיד יבדקו גרסה עדכנית.
- **התקנה בפועל**: בדפדפן (Chrome/Edge) על הטאבלט → "התקן אפליקציה" /
  "הוסף למסך הבית". לחבילת APK אמיתית (להתקנה על מולטימדיה ברכב או פרסום
  ב-Play Store) יש לעטוף את ה-PWA ב-**TWA** (Trusted Web Activity, בכלי
  `@bubblewrap/cli`) שמצביע על הדומיין הפרודקשן החי — זו רק "מעטפת" תצוגה,
  אין קוד native ללוגיקה. יש פלייסהולדר ב-
  `apps/web/public/.well-known/assetlinks.json` שיש להשלים עם ה-SHA256
  fingerprint האמיתי של מפתח החתימה לפני build ה-TWA (ראה הערות בקובץ).
- **עדכונים**: כל עוד מתקינים דרך PWA/TWA (ולא build native עצמאי), כל
  דיפלוי ל-production משפיע אוטומטית על מה שמותקן בטאבלט/ברכב — אין צורך
  בהתקנה מחדש.

---

## 10. עקרונות מפתח והחלטות ארכיטקטורה

- **אין מיגרציית דאטה.** ה-DB נשאר Firestore בדיוק כמו ב-`app.html`. אין שינוי
  סכימה, אין העברת נתונים. `app.html` הישן ממשיך לרוץ במקביל עד שהמערכת החדשה
  תבשיל.
- **מקור אמת אחד לטיפוסים** — `packages/shared-types`. כל שינוי שדה/קולקשן
  מחייב סנכרון עם ה-DB ועם `app.html`.
- **הרשאות בצד שרת** — כל מודול נשען על `requireModuleAccess` / `requireOwner`;
  אין להסתמך על הסתרת UI בלבד.
- **בטיחות תשלומים** — אין לשמור נתוני כרטיס מלאים; רק ארבע ספרות אחרונות,
  תוקף וטוקן שער.
- **קוד-סניף `"all"`** — לבעלים; מסמן ראייה חוצת-סניפים.
- **שתי שאלות נפרדות על כל שקל** — "על מי הוא נופל בסניף" (`paidBy`/`owedBy`/`ownerPct`)
  ו"האם הוא נכנס לספר הראשי" (`countsToMain`). הראשונה היא חשבון מול שותף, השנייה חשבון של
  העסק מול עצמו. כשהשנייה נגזרה מהראשונה, כל מסך גזר אותה אחרת — ומכאן כל המנגנונים שנמחקו.
- **הספר הראשי הוא סכום, לא נגזרת** — `/dashboard/accounting` סוכם את השורות שסומנו, ולא
  מסיק דבר. אין מראות, אין ספרים מקבילים, ואין מה שיכול לסטות; ולכן גם אין צורך במסך שבודק
  בדיעבד למה שני מספרים לא הסתדרו.
- **`undefined` הוא `false`** — דגל חדש נפתח על דאטה קיים במצב הכי שמרני. שום סכום ישן לא
  מופיע בשורה התחתונה בלי שמישהו אמר במפורש שהוא שייך, בטופס שבו ההוצאה נרשמת.
- **"לא קרה" ו"לא נרשם" הם לא אותו דבר** — חודש בלי סכום בהוצאה קבועה משתנה, ותא ריק בטבלת
  הרווח-פר-מחשב, נשארים ריקים ולא אפס. אפס הוא תשובה; ריק הוא שאלה פתוחה, וזו כל התועלת
  של שני המסכים האלה.
- **סינון במקום מיגרציה** — כשמודל משתנה, הדאטה הישן נשאר במקומו ומוקרן/מסונן בזמן קריאה
  (`RETIRED_RATE_KEYS`, `isRetiredRate`, `lib/tx-data.ts`). זה הפיך, ולא נוגע בספרים אמיתיים
  של העסק.
- **היסטוריה לא נערכת, מוסיפים לה** (פרק טו׳) — מחשבים שנוספו הם תנועה חדשה בתאריך, הוצאה
  שנגמרה מקבלת תאריך סיום, מחיר שהשתנה פותח גרסה חדשה, וסניף שנסגר מקבל תאריך סגירה.
  **אם קיימת פעולה שמשנה מספר של חודש שכבר עבר — היא באג**, לא משנה כמה היא נוחה.
- **פריט אף פעם לא נמחק** — הוא רק משנה סטטוס. מחיקה שוברת את מאזן כלל 4 וגם מוחקת את הידיעה
  שהופסד שם כסף.
- **מנהל הסניף מזין עובדות, לעולם לא תנאים** (פרק יד׳) — כמה, על מה, מתי והקבלה הן עובדות
  שרק הוא יודע; מי נושא בעלות נגזר מ-`Branch.expensePolicy` ואינו שדה שהוא רואה.
  **הכלל הזה עדיין לא מיושם במסך שעובד** — ראו פרק יד׳ למטה.

---

## 11. מפת קבצים מרכזיים

| נתיב | תפקיד |
|------|-------|
| `packages/shared-types/src/index.ts` | כל טיפוסי הדומיין / סכימת Firestore |
| `apps/web/lib/perms.ts` | שערי הרשאה (`requireModuleAccess`, `requireOwner`) |
| `apps/web/lib/nav-items.ts` | הגדרת ניווט + סינון לפי הרשאה |
| `apps/web/lib/auth.ts` | NextAuth (3 providers, סנכרון JWT) |
| `apps/web/lib/firebase-admin.ts` | אתחול Firebase Admin (web) |
| `apps/web/lib/nedarim.ts` | פתרון creds ל-Nedarim Plus לפי סניף |
| `apps/web/lib/rental-pricing.ts` | לוגיקת תמחור השכרות |
| `apps/web/lib/collection-charge.ts` | לוגיקת חיוב/גבייה |
| `apps/web/lib/branch-accounting*.ts` | חישובי הנה"ח והתחשבנות סניפי השכרות |
| `apps/web/lib/branch-ledger.ts` | בניית היסטוריית התחשבנות מלאה פר-סניף עם יתרה מצטברת; מזין את טבלת ההעברות ב-`/dashboard/accounting/mobile`. פעולות הסימון עברו ל-`accounting/mobile/transfer-actions.ts`; `/dashboard/rentals/ledger` נשאר `redirect` בלבד |
| `apps/web/lib/branch-transfer-rows.ts` | שורת ההתחשבנות של סניף בחודש נתון (יתרה, צריך להעביר, הועבר, קבלה) — המקור המשותף לטבלת ההעברות המצומצמת ולטבלה הרחבה שנפתחת בחלון, כדי ששתיהן לא יראו שני סכומים לאותו סניף |
| `apps/web/lib/computer-room-accounting.ts` | חישובי "השקעה מול רווח" פר סניף חדר מחשבים. כולל `branchMonthlyIncomeRows()` — מה שכבר רשום לכל סניף בכל חודש, לטופס ההזנה החודשי. ההשקעה נקראת משכבת הנכסים כשקיימת שם, ונופלת ל-`Branch.setupCost` רק כשלא; כולל `payback` — כמה מההשקעה כבר חזרה, `setupBreakdown` — פירוט ההקמה לחלון שנפתח מהקובייה, ו-`monthlyFlow` — הוצאות והכנסות חודש-חודש לגרף (הוצאה קבועה נפרסת על כל חודש שהייתה פעילה בו, חד-פעמית נזקפת לחודש התאריך שלה, והקמה לא נכנסת) |
| `apps/web/app/dashboard/coworking/` | המסך המאוחד "סניפים ועמדות": כרטיס הסניף, ארבעה ריבועים 1–4, השכרה עם תאריך התחלה/סיום, וסימון תשלום חודשי שנרשם ישירות בהנה"ח הראשית. `station-actions.ts` הוא הבעלים היחיד של המנויים והתשלומים; `rental-history.tsx` הוא ההיסטוריה (ומשמש גם לסעיף "השכרות ללא סניף פעיל") |
| `apps/web/components/setup-cost-field.tsx` | שדה עלות ההקמה בטופס סניף (חדרי מחשבים **ומשרד שיתופי**): מציג את הסה"כ, וכפתור פותח חלון צד להוספת שורה לכל הוצאה (`Branch.setupItems`) ולסימון אם ההקמה נכנסת לראשי. הסה"כ תמיד מחושב מהשורות, לא מוקלד |
| `apps/web/lib/expense-shared-scope.ts` | סנטינלים `SHARED_RENTALS_BRANCH_ID`/`SHARED_COMPUTERS_BRANCH_ID`/`SHARED_COWORKING_BRANCH_ID` להוצאות משותפות לכל הסניפים |
| `apps/web/lib/multi-branch-expense.ts` | פיצול הוצאה חד-פעמית בין כמה סניפים לפי אחוז הבעלים (`splitMultiBranchExpense`). מודול טהור - גם טופס ה-client וגם החישוב בשרת מייבאים אותו |
| `apps/web/lib/branch-month-report.ts` | בניית הדו"ח החודשי לסניף ורינדורו ל-HTML עצמאי (טבלאות + inline styles, בשביל Gmail/Outlook) |
| `apps/web/lib/branch-report-recipients.ts` | לאיזו כתובת נשלח הדו"ח של כל סניף - `Branch.partnerEmail` ואז המשתמש התואם ב-`n_users` |
| `apps/web/lib/mailer.ts` | שליחת HTML במייל מהשרת דרך Resend, כולל תמונות inline (`cid:`); בלי `RESEND_API_KEY`/`REPORT_FROM_EMAIL` מחזיר שגיאה מוסברת במקום ליפול |
| `apps/web/lib/email-logo.ts` | הפיכת הלוגו השמור (data URI) ל-inline attachment עם `cid:` - כי Gmail/Outlook מסירים תמונות `data:` |
| `apps/web/lib/branch-report-send.ts` | בניית מייל הדו"ח (מקור אמת אחד לכל הנתיבים) ושליחה חודשית לכל הסניפים, כולל דילוג על מה שכבר נשלח (`reportSentAt`) |
| `apps/web/lib/tx.ts` | שכבה 1 — טהור: קבועי `nature`/`node`, נירמול פיצולים לשקל (`normalizeAllocations`), פריסת `recurring` בזמן קריאה, ובניית תנועה (`buildTransaction`). נצרך גם ע"י קומפוננטות לקוח |
| `apps/web/lib/tx-data.ts` | **מודל הקריאה המאוחד**: `n_tx` + הקרנה של שש הקולקשנים הישנות לאותה צורה, בלי מיגרציה. מזהה בבואות ומוציא אותן מהמודל, ומסווג העברות מהשותף כ-`transfer` ולא כהכנסה. מקרין גם שורת `revenue_share` אחת לכל (אדם, סניף, חודש) — האחוזים שאני מעביר, כיציאה תפעולית, אחרת הספר הראשי היה סופר כרווח כסף שמעולם לא נשאר בכיס |
| `apps/web/lib/assets.ts` | שכבה 2 — טהור: אילוץ החשבונית (`validatePurchase`), פריסת שורות לפריטים, השקעה לפי מיקום, פיצול תמורת מכירה לפי עלות (`splitSaleProceeds`), תוצאה הונית (`capitalResult`), **השקעה נכונה־לתאריך** (`locationsAtMonth`/`investmentAtMonth`/`investmentSeries`) ו-`paybackStatus` שהמדד הראשי שלו הוא צפי איזון בחודשים ולא אחוז. נצרך גם ע"י טופסי הלקוח |
| `apps/web/lib/assets-data.ts` | טעינת שכבת הנכסים ו-`moveItems()` — שאין לו פרמטר סכום, בכוונה |
| `apps/web/lib/history.ts` | היסטוריה לא נערכת, מוסיפים לה (פרק טו׳): `reviseRecurringAmount()` — סגירת תקופה ופתיחת גרסה חדשה במקום עריכת סכום · `endRecurring()` — תאריך סיום במקום מחיקה · `closeBranch()` — תאריך סגירה עסקי, עצירת ההוצאות החוזרות והחזרת הציוד למחסן, ב-batch אחד ובלי למחוק דבר · `reopenBranch()` |
| `apps/web/lib/expense-policy.ts` | מדיניות התשלום פר סניף (פרק יד׳): מאיזו קטגוריה נגזר `paidBy`, רשימת הקטגוריות הסגורה של מנהל הסניף, וההסבר שהמסך מציג במקום שדה. מודול טהור. **אין לו צרכן בקוד כרגע** — נשמר כהגדרה של פרק יד׳ עד שהכלל יוחל על `/rentals/expenses` |
| `apps/web/lib/expense-review.ts` | סימון אוטומטי של שורות חריגות שסניפים הזינו, לפי הממוצע של הסניף עצמו (`flagsForEntry`), ומיון רשימת הסקירה. **אין לו צרכן בקוד כרגע** — ראו פרק יד׳ |
| `apps/web/lib/counts-to-main.ts` | הכלל היחיד של ההנה"ח הראשית: קריאת `countsToMain` מרשומה ומ-`FormData`, `setupCostCountsToMain` לעלות הקמה (ברירת מחדל דלוקה), והתוויות שהצ'קבוקס מציג. מודול טהור — גם טפסי לקוח מייבאים ממנו |
| `apps/web/lib/main-ledger.ts` | הספר הראשי: איסוף כל השורות שסומנו `countsToMain` מששת מקורות ההוצאה ושני מקורות ההכנסה, בתוספת עלות ההקמה של כל סניף (`setupCountsToMain`), וסכימתן. `fixedExpenseAccrued` צובר הוצאה קבועה חודש-חודש מ-`startDate` |
| `apps/web/lib/main-fixed-expenses.ts` | הוצאה קבועה של העסק עצמו (`n_ah_fixed_expenses`): `mainFixedExpenseMonths` / `mainFixedExpenseAccrued` / `mainFixedMonthlyTotal` / `isMainFixedExpenseActive`. אותה סמנטיקה של `FixedExpense` (סכום חודשי מחודש ההתחלה ועד הסיום/היום, החודש הראשון במלואו) בלי `branchId` |
| `apps/web/lib/recurring-expenses.ts` | הוצאות קבועות משתנות: `frequencySegments` / `dueMonths` / `missingMonths` / `buildReminders` / `lastClosedMonth` — חודש בלי סכום הוא "עוד לא עודכן", לא אפס; ההתראה נדלקת רק אחרי שהחודש נסגר; ותדירות היא קו-זמן, לא ערך יחיד |
| `apps/web/lib/partner-payouts.ts` | היתרה המצטברת לכל מי שיש לו אחוז מהברוטו: מה שהצטבר פחות מה שנרשם ששולם (`n_partner_payouts`), על פני חלון חודשים |
| `apps/web/lib/revenue-shares.ts` | **המנוע היחיד של האחוזים שאני מעביר החוצה** — ההסדרים הפר-סניפיים (`BRANCH_REVENUE_SHARES`: אדם, אחוז, סניף, `startDate`), החישוב הפר-מחשבי, והפונקציות הטהורות ששלושת הצרכנים חולקים: `computeBranchFinancials` (ניכוי מהרווח), `partner-settlement.ts` (שורת החוב) ו-`tx-data.ts` (שורת יציאה בספר הראשי) |
| `apps/web/lib/partner-settlement.ts` | שכבת הטעינה בלבד: מביאה מ-Firestore ומריצה את `revenue-shares.ts` — `computeRevenueShareLines(month)` |
| `apps/web/lib/laptop-branch-tracking.ts` | טבלת הרווח-פר-מחשב של כל סניפי הניידים יחד (חודשים כעמודות). `computeSecretaryShare`/`SECRETARY_PCT` נמחקו — ראו `revenue-shares.ts` |
| `apps/web/lib/leftovers.ts` | סורק "שאריות מהעבר": רשומות שאף מסך לא מציג (סניף מחוק/לא קיים/scope בלי מסך), עם הסיבה והשפעת הכסף. מוצג ב-`/dashboard/maintenance` |
| `apps/web/lib/coworking.ts` | המשרד השיתופי: לוח התשלומים (`billableMonths` / `payDayOf` / `isActiveOn` / `clientStatus`) ומאזן ההוצאות-מול-הכנסות (`buildCoworkingLedger`). גם זיהוי השכרות ללא סניף פעיל (`orphan` / `orphanReason`) |
| `apps/web/components/counts-to-main-field.tsx` | הצ'קבוקס והתגית של `countsToMain` — אותה קוביה בכל טופס הוצאה/תשלום בעסק |
| `apps/web/components/recurring-expenses/` | כרטיס ההוצאות הקבועות המשתנות והפעולות שלו, משותף לחדרי מחשבים, למשרד השיתופי ולהנה"ח הראשית |
| `apps/web/components/expenses/branch-expense-table.tsx` | רשימת הסניפים במסכי ההוצאות כטבלה קומפקטית, כולל "סה"כ עד היום" ו"מזה לראשי" |
| `apps/web/app/dashboard/accounting/receipt-actions.ts` | הפקת קבלה ב-EZcount על שורת הכנסת ניידים ושליחתה במייל ללקוח |
| `apps/web/app/dashboard/accounting/data-table.tsx` | הטבלה המשותפת של ההנה"ח: חיפוש, בוררי סינון פר-עמודה, טווח תאריכים, סידור בלחיצה ועימוד. כל טבלה מגדירה רק עמודות (`value` למיון/סינון, `render` לתצוגה) |
| `apps/web/app/dashboard/accounting/ledger-workspace.tsx` | גוף הספר הראשי: שני כפתורי ההזנה, סרגל חמש הטבלאות (סגור כברירת מחדל) ו"סגירה" |
| `apps/web/app/dashboard/accounting/add-income-button.tsx` / `add-expense-button.tsx` / `modal.tsx` | חלונות ההזנה — הכנסה (4 סוגים) והוצאה (קבועה / קבועה משתנה / רכישה), כל אחד שולח לאותה Server Action שהטופס המקורי שלח |
| `apps/web/app/dashboard/accounting/purchases-table.tsx` / `recurring-purchases-table.tsx` / `fixed-expenses-table.tsx` / `recurring-update-table.tsx` | ארבע הטבלאות שהגיעו מ"הוצאות נוספות" כשהוא נמחק: רכישות חד-פעמיות, סיכום הרכישות החוזרות, ההוצאות הקבועות של העסק, ועדכון החודש של כל ההוצאות הקבועות המשתנות בכל המודולים |
| `apps/web/app/dashboard/accounting/main-fixed-expense-controls.tsx` / `recurring-row-controls.tsx` | "הפסקה"/"חידוש"/מחיקה בתוך שורת טבלה — אותן Server Actions של הכרטיסים שהיו ב"הוצאות נוספות", בגרסת לקוח |
| `apps/web/app/dashboard/accounting/extra-expenses/page.tsx` | `redirect` בלבד ל-`/dashboard/accounting` — הכתובת הישנה של "הוצאות נוספות" |
| `apps/web/vercel.json` | cron חודשי (`0 6 1 * *`) שמפעיל את `/api/rentals/monthly-reports` |
| `apps/web/lib/branch-expense-ledger.ts` | מנגנון הבבואות — **כבוי**. `createLinkedOwnerLedgerExpense` הוא no-op (הסיגנטורה נשמרה כדי ששמונת מקומות הקריאה ימשיכו לעבוד); המחיקה נשארה אמיתית, כי הוצאה ישנה עשויה עדיין לשאת בבואה |
| `apps/web/lib/branch-income-ledger.ts` | יצירה/עדכון/מחיקה של רשומת `n_ah_income` מקושרת מהעברת סניף שסומנה כבוצעה (`BranchTransfer.linkedAhIncomeId`) |
| `apps/web/lib/shop-recommender.ts` | מנוע ההמלצות של חנות ה-AI - זיהוי תחום שימוש מטקסט חופשי, שאלות המשך, ניקוד רמת עומס, מפרט גנרי פר-תחום, התאמה לקטלוג האמיתי (`n_shop_catalog`) |
| `apps/web/lib/shop-quote.ts` | בניית מסמך ה-HTML העצמאי של הצעת המחיר שהלקוח מוריד בסוף השיחה ב-`/shop` |
| `apps/web/lib/owner-name.ts` | שמות אמיתיים (לא "אני"/"השותף") לשדות "מי שילם"/"על מי החוב" במסכי הוצאות - `getOwnerName` (שם המשתמש עם `role: "owner"`) ו-`resolveSharedPartnerName` (שם השותף ל"סניף" המדומה המשותף, כשכל סניפי השותפות במודול מסכימים על אותו שותף) |
| `apps/web/lib/device-trust.ts` | אימות/אמון מכשיר |
| `apps/web/middleware.ts` | middleware של Next (הגנת נתיבים) |
| `apps/web/public/manifest.webmanifest` | הגדרת ה-PWA (אייקונים/צבעים/standalone) |
| `apps/web/public/sw.js` | Service Worker (network-first + cache נכסים סטטיים) |
| `apps/web/app/pwa-register.tsx` | רישום ה-Service Worker בצד לקוח |
| `apps/web/public/.well-known/assetlinks.json` | אימות דומיין ל-TWA (Android App Links) |
| `apps/api/src/branches/*` | מודול NestJS לדוגמה (branches) |
| `apps/api/src/firebase/*` | מודול Firebase ב-NestJS |

---

## 12. פרק יד׳ — מה מנהל הסניף מזין בעצמו

**הכלל:** מנהל הסניף מזין **עובדות**, לעולם לא **תנאים**. כמה, על מה, מתי והקבלה הן עובדות
שרק הוא יודע. מי שילם ועל מי החוב הם תנאי ההסכם — הם נגזרים מ-`Branch.expensePolicy`, ואסור
שיהיו שדה שהוא רואה. הנימוק המלא ב-`apps/web/lib/expense-policy.ts`.

### המצב היום

הכלל **מוגדר ולא מיושם**. מודול `/dashboard/rentals/my-expenses` מימש אותו במלואו
(08/2026), אבל הוא כתב ל-`n_tx` — ואף מסך הנה"ח באפליקציה לא קורא את `n_tx`:

- `lib/branch-accounting-data.ts` — המקור של `/rentals/accounting` ושל ההנה"ח הראשית —
  קורא `n_fixed_expenses` ו-`n_var_expenses` בלבד.
- `loadTransactionModel()` (`lib/tx-data.ts`), הפונקציה היחידה שקוראת `n_tx`, נצרכה
  רק ע"י `my-expenses` עצמו.
- `/dashboard/accounting/review` — רשימת הסקירה של הבעלים, שהמודול הפנה אליה — נמחקה
  בניקוי של 31/08/2026 ולא נבנתה מחדש.

התוצאה: כל הוצאה שמנהל סניף הזין דרך המסך ההוא נשמרה ב-Firestore ולא הופיעה בשום דוח,
בשום התחשבנות ובשום מסך של הבעלים — בעוד שהמסך הבטיח לו במפורש "נכנס להתחשבנות מיד".
לכן המודול הוסר (09/2026), ולמנהל הסניף נשארה לשונית הוצאות אחת בלבד.

**מה לא נגעו בו:** אף מסמך ב-`n_tx` לא נמחק ואין מיגרציה — בדיוק כמו בכל שאר המודל
("סינון במקום מיגרציה", סעיף 10). `lib/expense-policy.ts` ו-`lib/expense-review.ts`
נשארו בקוד ללא צרכן, כהגדרה של הכלל.

### הצעד הבא

להחיל את פרק יד׳ על המסך שכבר מחובר להנה"ח במקום לבנות מסך שני: ב-`/rentals/expenses/[id]`,
כשהמשתמש אינו הבעלים, להסתיר את `paidBy`/`owedBy` מטופסי ההוספה ולגזור אותם בצד השרת
(`expenses/actions.ts`) מ-`paidByForCategory(branch, category)` עם רשימת הקטגוריות הסגורה
מ-`BRANCH_EXPENSE_CATEGORIES`. כך היתרון של פרק יד׳ מגיע לספרים האמיתיים בלי מודול שני
ובלי קולקשן שני.
