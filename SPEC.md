# איפיון מערכת — ultranet-platform

> מסמך חי. יש לעדכן אותו בכל שינוי פיצ'ר, מודול, מודל נתונים או אינטגרציה
> (ראה כלל תחזוקת התיעוד ב-[`CLAUDE.md`](CLAUDE.md)).
>
> עודכן לאחרונה: 2026-08-31

## תוכן עניינים
1. [סקירה כללית](#1-סקירה-כללית)
2. [ארכיטקטורה](#2-ארכיטקטורה)
3. [סטאק טכנולוגי](#3-סטאק-טכנולוגי)
4. [הרצה והגדרת סביבה](#4-הרצה-והגדרת-סביבה)
5. [תפקידים והרשאות](#5-תפקידים-והרשאות)
6. [אימות (Authentication)](#6-אימות-authentication)
7. [מודל הנתונים (Firestore)](#7-מודל-הנתונים-firestore)
8. [מודולים ופיצ'רים](#8-מודולים-ופיצרים)
9. [אינטגרציות חיצוניות](#9-אינטגרציות-חיצוניות)
10. [עקרונות מפתח והחלטות ארכיטקטורה](#10-עקרונות-מפתח-והחלטות-ארכיטקטורה)
11. [מפת קבצים מרכזיים](#11-מפת-קבצים-מרכזיים)

---

## 1. סקירה כללית

**אולטרנט (ultranet)** היא מערכת ניהול מאוחדת לעסק המנהל מספר קווי פעילות בכמה
סניפים:

- **חדרי מחשבים** — כ-5 סניפים.
- **השכרות** — מחשבים ניידים וסטיקים סלולריים, כ-30 סניפים בשותפות.
- **משרד שיתופי (coworking)**.

**המערכת הזו היא הנהלת החשבונות של העסק, ולא יותר.** קווי הפעילות הם מה שהכסף
מתאר, לא מודולים בתוכנה: אין כאן ניהול מכשירים, קריאות שירות, מלאי תפעולי, לקוחות
או חנות. הכל צומצם למודל שלוש השכבות (סעיף 8) ולשני המסכים שמזינים אותו. הניהול
התפעולי היומיומי ממשיך לרוץ ב-`app.html` הישן, על אותו מסד נתונים בדיוק.

המערכת מרובת-סניפים ומרובת-משתמשים, עם הרשאות לפי תפקיד (owner / partner /
employee) ולפי מודול.

---

## 2. ארכיטקטורה

מונורפו מנוהל ב-**pnpm workspaces + Turborepo**:

```
ultranet-platform/
├── apps/
│   └── web/            Next.js 14 (App Router) — האפליקציה היחידה
├── packages/
│   └── shared-types/   טיפוסי TS משותפים = סכימת ה-Firestore
├── turbo.json          פייפליין build/dev/lint/typecheck
├── pnpm-workspace.yaml
└── tsconfig.base.json
```

- **`apps/web`** — הלקוח והשרת של המשתמש. משתמש ב-App Router: `page.tsx` הן
  Server Components, מוטציות דרך Server Actions (`actions.ts`), חלקים
  אינטראקטיביים בקומפוננטות `*-client.tsx`. גישה ל-Firestore דרך
  `firebase-admin` (צד שרת בלבד).
- **`packages/shared-types`** — מקור אמת יחיד לטיפוסי הדומיין; משקף שם-לשם את
  קולקשני ושדות ה-Firestore.

---

## 3. סטאק טכנולוגי

| שכבה | טכנולוגיה |
|------|-----------|
| שפה | TypeScript (strict), Node ≥ 20 |
| Frontend/SSR | Next.js 14 (App Router), React 18 |
| עיצוב | Tailwind CSS (RTL, ממשק בעברית) |
| אימות | NextAuth 4 (Credentials + Email-code + Google) |
| DB | Firebase / Firestore (`firebase-admin`) |
| מייל | Resend, EmailJS (`@emailjs/browser`) |
| Build | Turborepo, pnpm 9 |

---

## 4. הרצה והגדרת סביבה

```bash
pnpm install
cp apps/web/.env.example apps/web/.env.local
pnpm dev        # turbo run dev — מריץ web + api
```

משתני סביבה (`apps/web/.env.example`):

| משתנה | תיאור |
|-------|-------|
| `FIREBASE_PROJECT_ID` | `ultranet-e94aa` |
| `FIREBASE_CLIENT_EMAIL` | חשבון שירות Firebase Admin |
| `FIREBASE_PRIVATE_KEY` | מפתח פרטי של חשבון השירות |
| `NEXTAUTH_SECRET` | סוד NextAuth |
| `NEXTAUTH_URL` | כתובת הבסיס (localhost:3000 בפיתוח) |
| `GOOGLE_CLIENT_ID` / `GOOGLE_CLIENT_SECRET` | אופציונלי — כניסת Google |

| `RESEND_API_KEY` / `REPORT_FROM_EMAIL` | שליחת הדו"ח החודשי במייל |
| `REPORTS_CRON_SECRET` | סוד ה-cron של `/api/monthly-reports` (Vercel מספקת `CRON_SECRET` לבד) |

---

## 5. תפקידים והרשאות

מוגדר ב-`apps/web/lib/perms.ts` ו-`packages/shared-types` (`AppUser`).

**תפקידים (`UserRole`):** `owner` · `partner` · `employee`.

**מפתחות הרשאה (`PermKey`):** הטיפוס עדיין מכיל את כל המפתחות ההיסטוריים
(`branches` · `computers` · `rentals` · `coworking` · `accounting` · `tasks` · `charging` · `shop` · `duxus`),
כי הם קיימים במסמכי `n_users` הקיימים ואין מיגרציה. **שניים בלבד מהם עדיין שומרים על מסך**:
`accounting` ו-`rentals`, והם היחידים שניתנים לעריכה במסך המשתמשים. מפתח שאין לו מסך לא
נמחק ולא מאופס — `updateUserAction` כותב `perms` ב-`{ merge: true }`, כך שערך שנשמר בעבר נשאר
בדיוק כפי שהוא.

**כללי גישה:**
- `owner` — עובר תמיד את כל הבדיקות (branchId = `"all"`).
- `partner` / `employee` — עוברים רק אם `n_users.perms[key] === true`.
- `requireModuleAccess(key)` — שער כניסה למודול; מפנה ל-`/dashboard` בהיעדר
  הרשאה, ול-`/login` בהיעדר session.
- `requireOwner()` — למסכים ברמת בעלים בלבד (למשל הנהלת חשבונות מרכזית).
- `viewClientBranchIds` — שיתוף ראייה של לקוחות בין סניפים (cross-branch).
- `charging` — היסטורי: היה מתיר חיוב כרטיסים. אין לו כיום מסך.

הניווט (`lib/nav-items.ts`) מסנן פריטים לפי הרשאה עם `visibleFor(role, perms, item)`.

**גבול השכבה הוא גבול ההרשאה (הנה"ח):** במודל שלוש השכבות (סעיף 8) השותף לא "מסונן" משכבות
1 ו-2 — פשוט אין לו אליהן גישה. אין שדה להסתיר, אין מסך לחסום, ואין באג הרשאות שממתין לקרות:
כל מסכי שכבה 1 ו-2 (`/entries`, `/purchases`, `/inventory`, `/sales`, `/policies`, `/review`,
`/bottom-line`, `/integrity`) דורשים `requireOwner()`. המסך היחיד בשכבה 3 שמנהל סניף כותב אליו
הוא `/my-expenses`, והסניף שלו נלקח שם מה-session ולא מהטופס. החריגה היחידה, וההכרחית: השותף רואה **כמה** מחשבים וסטיקים יש אצלו,
כי הוא חייב לתפעל אותם — אבל לא כמה הם שווים. זהו כלל ברמת השדה: `Item.unitCost` נקרא רק
בהקשר של הבעלים, ולכן כרטיס ההשקעה בעמוד הסניף (`BranchPaybackCard`) וטבלת ההשקעה כלל לא
נטענים כשהצופה מוגבל (`restricted`).

---

## 6. אימות (Authentication)

מוגדר ב-`apps/web/lib/auth.ts` (NextAuth, אסטרטגיית JWT). שלושה providers:

1. **מייל + סיסמה** — נבדק מול `n_users` (השדה `pass`, plaintext legacy — תואם
   ל-`app.html`; יש להחליף לאימות תקין לפני חשיפה חיצונית).
2. **קוד באימייל** (`email-code`) — מייל חייב להופיע ב-`n_approved_emails`; קוד
   חד-פעמי נשמר ב-`n_login_codes` עם תפוגה, ונמחק אחרי שימוש.
3. **Google** — מצליח רק אם כתובת ה-Gmail כבר קיימת ב-`n_users`.

ה-JWT מסנכרן מחדש `role` / `branchId` / `perms` / `viewClientBranchIds`
מ-Firestore כל ~2 דקות, כך ששינויי הרשאה נכנסים לתוקף בלי צורך בכניסה מחדש.
**הערה חשובה למימוש**: `getServerSession()` מריץ את ה-callback אבל אין לו תשובה לכתוב
אליה, ולכן ה-`branchSyncedAt` המעודכן נזרק ולא נשמר בעוגייה — חלון ה-2 דקות שעל הטוקן
לעולם לא נסגר בזמן רינדור. לכן הקריאה ל-`n_users` עצמה מקושרת לפי אימייל באותו חלון
(`loadUserForSync` ב-`lib/auth.ts`); בלי זה כל קריאת session בצד שרת הייתה שאילתה נפרדת,
ובכל דף יש לפחות שתיים (layout ואז `requireModuleAccess`) ועוד אחת לכל Server Action.
פעולות עריכת משתמש קוראות ל-`invalidateUserSyncCache(email)` כדי ששינוי הרשאה ייכנס לתוקף
מיד ולא בסוף החלון. בנוסף קיים אימות מכשיר (`/verify-device`, `lib/device-trust.ts`).

---

## 7. מודל הנתונים (Firestore)

כל השמות בתחילית `n_`. הטיפוסים ב-`packages/shared-types/src/index.ts`.
**אין לשנות שמות שדות/קולקשנים ללא התאמה ל-`app.html` ולדאטה הקיים.**

> **הטבלה מתארת את ה-DB, לא את האתר.** חלק מהקולקשנים כאן שייכים למודולים שהוסרו מהאתר
> הזה (מכשירים, קריאות שירות, משימות, סלעים, חנות, לקוחות והשכרות). הם **לא נמחקו ולא
> יימחקו** — `app.html` ממשיך לקרוא ולכתוב אליהם, וההנה"ח קוראת מכמה מהם (`n_rentals`,
> `n_branch_income`) בהקרנה. נתיבי המסכים שמופיעים בעמודת התיאור הם היסטוריים, ומתועדים
> כדי שיהיה ברור מי יצר את הדאטה.

| קולקשן | טיפוס | תיאור |
|--------|-------|-------|
| `n_branches` | `Branch` | סניפים; סוג (`computers`/`rentals`/`coworking`), אחוזי בעלים/שותף, סניף-אב, מסלול גבייה, דגלי גבייה/קבלות, `phone` (רשות - טלפון ליצירת קשר), `openedAt` (רשות - תאריך פתיחת הסניף; ממנו ואילך מחושבות ההכנסות וההוצאות, ראו "תאריך פתיחה" בסעיף ההנה"ח), `notStarted` (רשות - סימון ידני "עדיין לא התחיל לפעול" שגובר על כל נתון שכבר הוזן), `rentalPricing` (רשות - מחירון ברירת המחדל של סניף השכרות, ראו `/pricing`), `deleted`/`deletedAt` (רשות - soft-delete טכני, ראו סעיף 10 `/branches`), `closedAt` (רשות - **תאריך הסגירה העסקי**, שאינו `deletedAt`: סניף שנסגר ב-15 ביולי וסומן כמחוק רק בספטמבר חייב להפסיק לצבור מיולי; ראו פרק טו׳ בסעיף 8), `expensePolicy` (רשות - מי משלם כל קטגוריה חוזרת בסניף; ממנו נגזר `paidBy` של כל הוצאה שמנהל הסניף מזין, ראו "מה מנהל הסניף מזין" בסעיף 8) |
| `n_users` | `AppUser` | משתמשים; תפקיד, סניף, `perms`, `viewClientBranchIds` |
| `n_fixed_expenses` | `FixedExpense` | הוצאות קבועות לסניף |
| `n_var_expenses` | `VariableExpense` | הוצאות משתנות (לפי חודש) |
| `n_branch_income` | `BranchIncome` | הכנסות סניף; שורות הכנסה ידניות owner-only בדף `/dashboard/rentals/expenses/[id]` (מסך שהוסר) - מתחשבנות כמו השכרה רגילה בחישוב ההשכרות (`computeBranchFinancials`), וגם שורות ידניות נפרדות בדשבורד "השקעה מול רווח" של חדרי מחשבים (שם לא מחושבות בשום מקום) - בשני המקרים לעולם לא נכתב ל-`n_ah_income`, לא משפיע על הנה"ח הראשית |
| `n_tasks` | `Task` | משימות (דחיפות, חזרתיות, בוצע) |
| `n_sub_locations` | `SubLocation` | תתי-מיקומים בסניף |
| `n_devices` | `Device` | מכשירים/מחשבים בחדרי מחשבים |
| `n_laptops` | `Laptop` | מחשבים ניידים להשכרה + תמחור (יום/שבוע/חודש, וריאנטים); `hasPartner`/`partnerName`/`partnerPct` - שותפות פר-מחשב (ראו סעיף 8); `itemId` (רשות) - הפריט ב-`n_items` שמייצג את המחשב הפיזי ונושא את עלותו האמיתית |
| `n_sticks` | `Stick` | סטיקים סלולריים + תמחור מדורג (`day1`/`day2`/`day3plus`) ומדרגות `weekPrice`/`monthPrice` (רשות) להשכרת סטיק בלבד; `linkedLaptopId` מקשר לסטיק שמשוייך למחשב ניתן להשכרה - מסונכרן אוטומטית מ-`n_laptops` (ראו סעיף 8, `/laptops`); `itemId` (רשות) - הפריט ב-`n_items` שמייצג את הסטיק הפיזי |
| `n_rental_clients` | `RentalClient` | לקוחות השכרה; פרטי כרטיס לא-רגישים, `gatewayToken`, `collectionRouteId` (העסק שהטוקן שויך אליו) |
| `n_rentals` | `Rental` | השכרות; פריט, תאריכים, מחיר מחושב/סופי, סטטוס, תשלום |
| `n_inventory` | `InventoryItem` | מלאי (כמות, מינימום) |
| `n_tickets` | `Ticket` | קריאות שירות למכשירים |
| `n_printers` | `Printer` | מדפסות (טונר, נייר, IP) |
| `n_cw_stations` | `CoworkingStation` | עמדות במשרד שיתופי |
| `n_cw_clients` | `CoworkingClient` | לקוחות משרד שיתופי + היסטוריית תשלומים |
| `n_ah_income` | `AccountingIncome` | הכנסות בהנה"ח מרכזית (בעלים); 3 סוגים בלבד - `laptops`/`credit`/`cash`, ראו סעיף 8 |
| `n_ah_expenses` | `AccountingExpense` | הוצאות בהנה"ח מרכזית |
| `n_collection_routes` | `CollectionRoute` | מסלולי גבייה (ספק תשלום, קבלות, יעד הפקדה, עמלות, `defaultForNewCards`) |
| `n_shop_catalog` | `ShopComputerConfig` | תצורות מחשב אמיתיות למכירה בחנות ה-AI (`/shop`) - שם, דרגה (`minimal`/`recommended`/`extreme`), תחומי שימוש (`useCases`), מפרט, מחיר; ממולא ידנית ע"י הבעלים ב-`/dashboard/shop/catalog` |
| `n_shop_addons` | `ShopAddon` | ציוד נלווה שניתן להוסיף להצעת המחיר בחנות ה-AI (תיק/עכבר/מקלדת/דיסק און קי/אחר) |
| `n_shop_leads` | `ShopLead` | פניות שהתקבלו מהצ'אטבוט הציבורי - תיאור, תשובות, ההמלצות שהוצגו, תצורה/ציוד נבחרים, פרטי קשר, סטטוס |
| `n_branch_transfers` | `BranchTransfer` | התחשבנות חודשית שותף↔בעלים; `transferredAmount` (רשות) - הסכום שבפועל נרשם כהועבר לאותו חודש, באותה מוסכמת סימן כמו `netToOwner` (חיובי = לבעלים, שלילי = מהבעלים) - מאפשר לרשום העברה חלקית/מאוחרת שלא תואמת בדיוק את הסכום המחושב. רשומות ישנות בלי השדה הזה (רק `transferred: true`) מטופלות כמסולקות במלואן, ראו `lib/branch-ledger.ts`. `linkedAhIncomeId` (רשות) - מזהה רשומת `n_ah_income` שנוצרה/מתעדכנת אוטומטית כש-`transferredAmount` חיובי, ראו `lib/branch-income-ledger.ts` בסעיף 9. `receiptIssued` (רשות) - סימון עצמאי "הוצאנו קבלה", לא קשור לסכום |
| `n_approved_emails` | — | מיילים מאושרים לכניסת קוד |
| `n_login_codes` | — | קודי כניסה חד-פעמיים עם תפוגה |
| `n_procedures` | `Procedure` | נהלים ברורים במודול "משימות ונהלים" (`/dashboard/duxus/procedures`) - כותרת, תוכן HTML עשיר (עורך זהה להדרכות), קטגוריה חופשית |
| `n_quarters` | `Quarter` | רבעוני העבודה (`/dashboard/duxus/rocks`). **מזהה המסמך הוא ה-`quarterKey`** שמופיע על `Rock`/`Milestone`: `label` (תווית חופשית, למשל "ראש חודש אלול - ראש חודש כסלו"), `status` (`active`/`archived` - מאורכב = קריאה בלבד, נאכף בשרת), `startDate`/`endDate` (רשות, לתצוגה), `order` (חותמת זמן למיון ציר הזמן), `activeMonthKey`/`activeWeekKey` (החודש/השבוע ה"פתוחים" בקומות העליונות של לוח העבודה; ריק = עוד לא נפתחו), `rolledFromKey` (הרבעון שממנו גולגל). מפתחות לועזיים ישנים ("2026-Q3") ממשיכים לעבוד בלי מיגרציה - מוצגים כרבעון פעיל וירטואלי, והמסמך נוצר בפועל רק כשמבצעים עליו פעולה |
| `n_rocks` | `Rock` | סלעים רבעוניים ותתי-סלעים במודל "סלעים ואבני דרך" (`/dashboard/duxus/rocks`) - `quarterKey` (מזהה מסמך ב-`n_quarters`), `parentRockId` (ריק = סלע עצמו, מוגדר = תת-סלע, עומק אחד), `status` (`active`/`done`/`dropped`, מתעדכן אוטומטית לפי אבני הדרך), `rolledFromId` (הסלע המקורי כששוכפל בגלגול רבעון), אחראי |
| `n_milestones` | `Milestone` | אבני הדרך (המשימות בפועל) תחת סלע/תת-סלע, וגם המשימות השוטפות - `quarterKey` מוכפל מהסלע-אב לשאילתת שוויון יחידה, `stage` (`backlog`/`month`/`week`) מסמן את הדלי הפעיל, `monthKey`/`weekKey` נשארים על הרשומה גם אחרי שקודמה הלאה (להיסטוריה), `carryOverCount` סופר העברות קדימה כשלא הושלמה בזמן, `source` (`rock` = נגזר מסלע, ברירת מחדל גם לדאטה ישן / `adhoc` = משימה שבועית-שוטפת עם `rockId` ריק), `rolledFromId` (אבן הדרך המקורית כששוכפלה בגלגול רבעון) |
| `n_rock_reviews` | `RockReview` | סיכום/לקחים לכל פגישת רבעון/חודש/שבוע - מזהה דטרמיניסטי `${period}_${periodKey}` (upsert, רשומה אחת לתקופה) |
| `n_cost_rates` | `CostRate` | תעריפון עלויות התפעול (`/dashboard/accounting/rates`) - מחשב/תיק/סטיק/סינון וגלישה: `unitCost`, `kind` (`once`/`monthly`), `owedBy` (ברירת מחדל של חלוקת החוב), `qtySource` (מאיפה נגזרת הכמות בסניף). כשהקולקשן ריק מוצגות ברירות המחדל מ-`lib/cost-rates.ts` בזיכרון בלבד עד ששומרים אותן במפורש. **שורות `kind: "once"` (ציוד) הוצאו מהתעריפון** ומסוננות ב-`isRetiredRate` — העלות האמיתית מגיעה מ-`n_items.unitCost`; ראו "מה יצא מהתעריפון" בסעיף 8 |
| `n_branch_cost_settings` | `BranchCostSetting` | דריסה של שורת תעריפון לסניף מסוים - מזהה דטרמיניסטי `${branchId}__${rateKey}` (upsert). כל שדה רשות: `qty`/`unitCost`/`owedBy`/`paidBy`/`enabled` - מה שלא מוגדר נופל חזרה לערך שבתעריפון |
| `n_multi_branch_expenses` | `MultiBranchExpense` | הוצאה חד-פעמית אחת שמתחלקת בין כמה סניפי השכרות (מסך שהוסר): `amount` (הסכום המלא), `ownerPct` (כמה אחוז ממנה על הבעלים), `branchIds` (בין מי מתחלק השאר, שווה בשווה), `paidBy` (מי שילם בפועל - משפיע על כיוון ההתחשבנות בלבד, לא על הפיצול), `date`/`month`, `linkedAhExpenseId` (רשות - רשומת `n_ah_expenses` של חלק הבעלים, נוצרת רק כשהבעלים שילם). הכללה של מודל `n_ad_areas` מפרסום-בלבד לכל הוצאה חד-פעמית; **לא** נשמר כ-N שורות `n_var_expenses` נפרדות כי `owedBy` יודע לבטא רק בעלים/שותף/50-50 ולא אחוז חופשי. נפרס לשורת הוצאה פר-סניף בזמן קריאה (`expandExpenseLines`, `lib/branch-accounting-data.ts`), בדיוק כמו `n_ad_areas` |
| `n_ad_areas` | `AdArea` | אזור פרסום - קמפיין אחד המשותף לכמה סניפים באותה עיר (`/dashboard/accounting/ads`): `monthlyCost`, `ownerPct` (חלק הבעלים בקמפיין), `branchIds`/`branchCount` (בין כמה סניפים מתחלק השאר), `startMonth`/`endMonth`, `paidBy`. שורת הפרסום האוטומטית היחידה בסניף (בתעריפון אין שורת פרסום) - אלא אם הסניף הזין פרסום ידנית |
| `n_tx` | `Transaction` | **שכבה 1 — כל שקל שזז, פעם אחת.** היעד של כל תנועה חדשה, ומחליף כמסלול הכתיבה את `n_ah_income`/`n_ah_expenses`/`n_var_expenses`/`n_fixed_expenses`/`n_multi_branch_expenses`/`n_ad_areas`. שדות: `amount` (תמיד הסכום המלא לפני חלוקה), `nature` (`operating`/`capital`/`transfer` — קובע לאיזו שכבה התנועה הולכת), `node` (`business` + `branchId`, כשהערכים `shared`/`hq` הם צמתי-אב לגיטימיים), `ownerShare` (חלק הבעלים **בשקלים**, ולכן אחוז חופשי לא צריך קולקשן משלו), `allocations[]` (פיצול בין סניפים; חייב להסתכם ל-`amount`), `recurring` (`from`/`to` — נפרס לשורות חודשיות בזמן קריאה; **שינוי סכום סוגר תקופה ופותח גרסה חדשה, לעולם לא עורך במקום**), `paidBy`, `purchaseId` (רק ב-`capital`), `doc`, `enteredBy` (מי הזין — מוגדר כשמנהל סניף הזין בעצמו), `reviewedAt`/`flags` (רשימת הסקירה של הבעלים, ראו פרק יד׳). **המסמכים הישנים לא עברו מיגרציה** — הם מוקרנים לאותה צורה בזמן קריאה, ראו `lib/tx-data.ts` |
| `n_purchases` | `Purchase` | **שכבה 2 — חשבונית הספק.** `date`/`supplier`/`invoiceNo`/`total`, `txId` (התנועה ההונית שנוצרה איתה), `lines[]` (`kind`/`label`/`qty`/`unitCost`). אילוץ שנאכף בכתיבה ונבדק שוב במסך בדיקת השלמות: `Σ(qty × unitCost) === total === tx.amount`. יצירת רכישה יוצרת תנועה הונית אחת ופריט לכל יחידה — ולעולם לא הוצאה של סניף |
| `n_items` | `Item` | **שכבה 2 — פריט פיזי בודד.** `kind` (`laptop`/`stick`/`bag`/`sim`/`other`), `unitCost` (**מה שהיחידה הזו באמת עלתה בחשבונית שלה** — השדה שכל המודל נשען עליו), `purchaseId`, `acquiredAt`, `location` (מזהה סניף · `warehouse` · `sold`; שדה חובה — פריט נמצא בדיוק במקום אחד), `status` (`active`/`repair`/`sold`/`writeoff`/`lost` — מצבים סופיים, **פריט אף פעם לא נמחק**), `serial`, `linkedLaptopId`/`linkedStickId`. **שדות היציאה** (פרק יג׳): `soldAt`, `soldPrice` (חלקו של הפריט בתמורה), `saleTxId`, ו-`lastBranchId` — **חובה בכל יציאה**: אחרי מכירה `location` כבר לא מצביע על סניף, ובלי לזכור מאיפה הפריט יצא אי אפשר לייחס את התמורה והחזר ההון של הסניף שגוי לנצח |
| `n_item_moves` | `ItemMove` | **שכבה 2 — פריט עבר ממקום למקום, ומקור האמת למיקום.** `itemId`/`from`/`to`/`date`/`reason` (`allocation`/`return`/`transfer`/`repair`/`writeoff`/`sale`/`branch_closed`/`initial`). **אין כאן שדה סכום, בכוונה, לעולם** — העלות נוסעת עם הפריט, ולכן משלוח לא יכול לייצר כסף. `Item.location` הוא **מטמון בלבד** של התנועה האחרונה: אסור לכתוב אליו בלי לכתוב תנועה מתאימה באותו batch, אחרת כל מספר היסטורי הופך לניחוש (פרק טו׳) |

> הערה: `RentalClient` שומר `cardLast4` (תצוגה בלבד), `cardExpiry` (MM/YY,
> לא רגיש) ו-`gatewayToken` — לעולם לא PAN מלא. שדה נוסף: `referralSource`
> (רשות, טקסט חופשי) — מאיפה הלקוח הגיע אלינו, למעקב שיווקי.
>
> **`collectionRouteId`** (רשות) — ה-`n_collection_routes` שהטוקן נוצר תחתיו, כלומר
> לאיזה עסק/מוסד נדרים פלוס הכרטיס השמור שייך. טוקן תקף רק אצל המוסד שיצר אותו ולכן
> אי אפשר "להעביר" אותו בין עסקים — רק ליצור טוקן חדש (הזנת כרטיס מחדש) תחת מוסד אחר.
> לקוחות שנשמרו לפני שהשדה הזה נוסף אין להם ערך (`undefined`) וממשיכים להיפתר לפי
> מסלול הסניף כמו קודם — כך שכרטיסים ישנים ממשיכים
> להיגבות אוטומטית דרך אותו עסק כמו תמיד. כרטיסים **חדשים** נשמרים כברירת מחדל תחת
> המסלול המסומן `defaultForNewCards: true`, עם אפשרות לבחור מסלול אחר מתוך רשימת
> המסלולים המחוברים בזמן שמירת הכרטיס (`client-card-section.tsx`,
> `complete-cards-queue.tsx`). בכל מסך חיוב (`token-charge-button.tsx`,
> `client-charge-section.tsx`, `active-rental-row.tsx`) מוצג באופן קבוע לאיזה עסק
> החיוב מתבצע ("שים לב: זה גובה לעסק X"), ובדף עריכת לקוח עם כרטיס ישן (לא במסלול
> ברירת המחדל) מוצגת הודעת אזהרה שמציעה להעביר את הלקוח (כלומר לבקש ממנו להזין כרטיס
> מחדש) לעסק הנוכחי כברירת מחדל.

---

## 8. מודולים ופיצ'רים

הבסיס תחת `apps/web/app/dashboard/`. פריטי הניווט (`lib/nav-items.ts`): בית · הנה"ח ·
העברות ודוחות · ההוצאות שלי (ולבעלים גם משתמשים והרשאות).

**מה נמחק, ולמה זה לא נגרע.** האתר נשא מודול לכל קו פעילות — חדרי מחשבים, השכרות,
משרד שיתופי, משימות ונהלים, חנות AI, הדרכות — ולכל אחד מהם היו טאבים משלו ומסכי הזנה
משלו לאותו שקל עצמו. זו בדיוק הבעיה שמודל שלוש השכבות פתר: מרגע שכל תנועה נכנסת פעם
אחת ל-`n_tx`, מסך הזנה מקביל הוא לא עוד דרך להזין — הוא דרך לספור פעמיים. לכן המודולים
האלה **הוסרו מהקוד**, לא הוסתרו. הקולקשנים שלהם ב-Firestore לא נגעו בהם ולא נמחקו
(`n_devices`, `n_tickets`, `n_tasks`, `n_rocks`, `n_shop_leads` ועוד), וגם `app.html` הישן
ממשיך לרוץ עליהם — כך שההיסטוריה שלמה וכל מודול שיוחזר בעתיד ימצא את הדאטה שלו במקום.

מה שנשאר הוא שלושה מסכים בלבד:

| מסך | נתיב | הרשאה | למה הוא כאן |
|---|---|---|---|
| הנה"ח | `/dashboard/accounting` | `accounting` (רובו `requireOwner`) | מודל שלוש השכבות עצמו |
| העברות ודוחות | `/dashboard/settlement` | `rentals` | כמה כל סניף צריך להעביר, ודו"ח חודשי |
| ההוצאות שלי | `/dashboard/my-expenses` | `rentals` | המסך היחיד בשכבה 3 שמנהל סניף כותב אליו |

---

### הנהלת חשבונות (`/dashboard/accounting`) — perm: accounting

**מבנה המודול (`accounting-tabs.tsx`) — שש לשוניות, לפי סדר השכבות:**
`רישום ותנועות` (שכבה 1) · `סקירה` ו-`השורה התחתונה` (שכבה 3) · `ציוד` (שכבה 2, שער ל-`purchases`
`inventory` `sales`) · `סניפים` (שער ל-`policies` `review` `routes`) · `בדיקת שלמות`.
`/dashboard/accounting` עצמו הוא `redirect` ל-`/overview`.

**מה נמחק מההנה"ח ולמה** — היו כאן חמש-עשרה לשוניות, ורובן היו דרכים שונות להזין את אותו שקל:

| נמחק | מה החליף אותו |
|---|---|
| תעריפון (`/rates`, `n_cost_rates`) | `n_items.unitCost` — עלות אמיתית מהחשבונית במקום הערכה שטוחה |
| פרסום משותף (`/ads`, `n_ad_areas`) | `allocations[]` + `ownerShare` על התנועה |
| שיוך לסניפים (`/attribute`) | תנועה מסווגת בזמן ההזנה — היא לא "ממתינה לשיוך" |
| ייבוא מאקסל (`/import`) | מסך אחד שיוצר כסף (כלל 1) |
| טופס ההוצאה הישן, מודאל הגבייה, טפסי ההוספה בעמוד הסניף | `רישום ותנועות` |
| מנגנון ההשתקה (`suppressed`) ועורך התעריפון פר-סניף | כשיש מקור אחד לכל עלות, אין מה להשתיק |

**הדאטה לא נמחק.** `lib/tx-data.ts` ממשיך לקרוא ולהקרין את `n_ad_areas`, `n_cost_rates` לא נדרש
יותר, וכל שאר הקולקשנים הישנים נקראים כרגיל — ולכן כל מספר היסטורי נשאר נכון. נמחקו המסכים,
לא הספרים. `/routes` (מסלולי גבייה) **נשאר** כי מודול ההשכרות תלוי בו, והוא נגיש מדף הסניפים.

**`/branches` — ניהול סניפים** (owner בלבד): הוספה, מחיקה רכה ושחזור; **סגירת סניף** עם תאריך
עסקי (פרק טו׳); וטבלת הצעות לתאריך פתיחה (`lib/branch-opening-proposals.ts`).

#### מודל שלוש השכבות (`lib/tx.ts`, `lib/assets.ts`, `lib/business-ledger.ts`)

כל שכבה עונה על שאלה אחת ויחידה. שכבות לא מתחברות זו לזו — לא כי זו כפילות, אלא כי חיבור
ביניהן חסר משמעות.

| שכבה | השאלה שהיא עונה עליה | הישות | מי רואה |
|---|---|---|---|
| 1 · כסף | איפה הכסף שלי עכשיו? | `n_tx` | הבעלים בלבד |
| 2 · נכסים | מה יש לי, איפה זה, וכמה זה עלה? | `n_purchases` · `n_items` · `n_item_moves` | הבעלים. שותף רואה כמות, לא ערך |
| 3 · רווחיות | כמה הרווחתי, ואיפה? | ספר הסניף (חישובי `branch-accounting*`) | הבעלים — הכל; שותף — רק הסניף שלו |

**החוק היסודי: רכישת ציוד היא לא הוצאה, היא המרה של כסף לנכס.** כשיוצאים 15,000 ₪ מהחשבון
לא נעשים עניים ב-15,000 ₪ — יש עכשיו פריטים ששווים 15,000 ₪. ההון לא זז, רק שינה צורה. לכן
תנועה הונית **נעצרת** בשכבה 2 והופכת לפריטים, והעלות נעה בין מחסן לסניפים בלי שנרשם ולו שקל
נוסף. אותם 15,000 ₪ עונים במקביל: יצאו מהחשבון 15,000 ₪ (שכבה 1), הושקעו ברמות 9,600 ₪
(שכבה 2), ונכנסו לרווחיות רמות **0 ₪** (שכבה 3).

**שמונת הכללים** (מיושמים בקוד, ושלושה מהם נבדקים אוטומטית ב-`/integrity`):

1. שקל נרשם פעם אחת, במסך אחד — `/entries` הוא המסך היחיד שיוצר כסף.
2. למסך ההקצאה לסניף אין שדה סכום. אין שדה, אין כפילות.
3. `Σ allocations === amount`, לשקל (`normalizeAllocations`; שארית עיגול לסניף הראשון).
4. המחסן הוא יעד לגיטימי, ולכן `Σ פריטים פעילים (סניפים + מחסן) + Σ פריטים שיצאו === Σ הרכש ההוני`.
   פריט שנמכר לא נעלם מהמאזן — הוא עובר לצד השני שלו (פרק יג׳).
5. פריט נמצא בדיוק במקום אחד — `location` הוא שדה חובה, וגם `warehouse` הוא מיקום.
6. שכבות לא מתחברות — אין בשום מסך "סה"כ" שמחבר תזרים עם מחזור.
7. תנועה הונית לעולם לא נכנסת לספר התפעולי של הסניף ולא מתחלקת עם שותף.
8. העברה היא לא הכנסה ולא הוצאה — היא סילוק חוב על הכנסה שכבר רשומה בספר הסניף.

**`nature` — השדה שקובע לאיזו שכבה התנועה הולכת:**

| הסוג | דוגמאות | לאן | מתחלק עם השותף? |
|---|---|---|---|
| `operating` | אינטרנט, סינון, פרסום, שכירות, הכנסות | שכבה 3 | כן, לפי `ownerShare` |
| `capital` | מחשבים, סטיקים, תיקים, הקמת חדר מחשבים | שכבה 2 | **לא. לעולם** |
| `transfer` | ההעברה החודשית מהשותף, העברה בין חשבונות | שכבה 1 בלבד | לא רלוונטי |

**עץ מרכזי הרווח:** כל תנועה נתלית על צומת אחת בדיוק (`node`), וכל צומת מעליה היא סכום כל מה
שמתחתיה — ולכן סכימת ענף לא יכולה לספור שקל פעמיים. הצמתים `shared` (כל היחידה) ו-`hq` (מטה)
הם צמתי-אב לגיטימיים: **תייג בצומת הנמוכה ביותר שאתה באמת יודע**, וצומת-אב שמחזיקה סכומים
אמיתיים היא סימן לבריאות ולא לרשלנות. מסך בדיקת השלמות מציף תנועות שתלויות על `hq` מעל 30 יום,
למקרה שזו שכחה ולא הוצאת מטה.

#### מודל הקריאה המאוחד — למה אין מיגרציה (`lib/tx-data.ts`)

שבעת מקורות ההוצאה הפכו לרשימה אחת **בלי שאף מסמך זז**. `loadTransactionModel()` קורא את `n_tx`
ומקרין לאותה צורה גם את `n_ah_income`, `n_ah_expenses`, `n_var_expenses`, `n_fixed_expenses`,
`n_multi_branch_expenses`, `n_ad_areas`, `n_branch_income` ו-`Branch.setupCost`. זו בדיוק
הטכניקה של `RETIRED_RATE_KEYS` ("סינון במקום מיגרציה"), מוחלת על קולקשנים שלמים: כתיבה חדשה
הולכת ל-`n_tx`, והדאטה הישן ממשיך לעבוד במקומו ונקרא זהה.

מה שזה מחק:

- **הבבואות.** ספר התזרים הוא מעכשיו **שאילתה** (`buildFlow`/`flowTotals` —
  `paidBy === "owner"`, סכימת `ownerShare`) ולא קולקשן שצריך לסנכרן. בבואה היא עותק של שורה
  שנוצר רק כדי לראות אותה מזווית שנייה, ושאילתה עושה את זה בלי עותק.
  `createLinkedOwnerLedgerExpense` הוא no-op (הסיגנטורה נשמרה כדי ששמונת מקומות הקריאה ימשיכו
  לעבוד ופשוט יפסיקו לייצר עותקים); בבואות שנשארו ב-Firestore מזוהות לפי
  `linkedAhExpenseId`, **מוסרות מהמודל** כדי שלא ייספרו, וניתנות למחיקה מ-`/integrity`.
- **`ownerLedgerExpenseAmount` ו-`lib/owner-expense-burden.ts`** — נמחקו. הכלל שהם קידדו נאמר
  עכשיו פעם אחת, כסינון על `paidBy` וסכימה של `ownerShare`.
- **`loadComputerRoomSetupCostTotal`** — נמחקה. עלות הקמה היא תנועה הונית עם תאריך, ולכן היא
  מגיעה למודל לבד ומופיעה במזכר ההוני במקום להיות מוסיפה על סכום ההוצאות ביד.
- **הוצאה רב-סניפית ואזור פרסום** — שני מימושים לאותו רעיון. `allocations[]` + `ownerShare`
  מבטאים את שניהם.
- **הוצאות קבועות כקולקשן נפרד** — `recurring` על התנועה מייצר את השורות החודשיות בזמן קריאה.

**מה מוקרן מאיפה, וכללי אי-הכפילות שבתוך ההקרנה** — ברגע ששני הספרים נקראים ממודל אחד, החפיפה
ביניהם מפסיקה להיות "לא מחברים אותם" והופכת להחלטת סיווג מפורשת:

- **`n_rentals`** (הוחזרה **וגם** שולמה) מוקרנת כהכנסה תפעולית של סניף ההשכרות. זה המקור
  הגדול ביותר בספר הסניף, והוא לא היה קיים באף ספר — בלעדיו המחזור והשורה התחתונה היו מציגים
  עסק עם עלויות וכמעט בלי הכנסות. `paidBy` נקבע לפי `isCollectedByOwner` (מי מחזיק בפועל את
  המזומן), ולכן ספר התזרים סופר רק את מה שבאמת הגיע לבעלים.
- **`n_ah_income` מסוג `laptops` עם `branchId`** מסווגת `transfer` ולא הכנסה — גם כשנוצרה
  אוטומטית מסימון העברה וגם כשהוקלדה ידנית לפניה. ההשכרות שהיא משלמת עליהן כבר רשומות בספר
  הסניף (מהשורה הקודמת); לספור אותה גם כהכנסה זו בדיוק הכפילות שכלל 8 מונע.
- **`n_ah_income` מסוג `cash`** (קופת חדר מחשבים) **נשארת הכנסה תפעולית** — זה המקור היחיד
  להכנסות החדר, אין לו רישום אחר בשום מקום.
- **`n_branch_income` של חדר מחשבים מדולגת** — לפי הגדרת הטיפוס היא מעקב סטטוס בלבד ולא
  משתתפת בשום חישוב שם, ולכן הקרנה שלה הייתה מכפילה את הכנסת הקופה. אצל סניף השכרות היא כן
  מוקרנת, כי שם היא נספרת כהכנסה אמיתית בדיוק כמו השכרה.
- **הכנסה על צומת `hq`** (למשל "אשראי מהעסק") מוצגת ב-`/bottom-line` כשורה נפרדת ו**לא**
  כחלק מהמחזור: המטה לא מייצר הכנסות משלו. אם בפועל זה הכסף שמגיע עבור הכנסות שכבר רשומות
  בסניף — הסיווג הנכון הוא `transfer`, וכך היא מפסיקה להיספר כהכנסה בכלל. המסך אומר את זה
  במפורש כדי שההחלטה תהיה גלויה ולא מובלעת.
- **אזור פרסום בלי `startMonth`** ("פעיל מאז ומעולם") נחסם לחודש המתועד המוקדם ביותר במודל.
  במסכים חלוניים זה מעולם לא הורגש, אבל סכום "מתחילת הדרך" היה מכפיל את העלות החודשית בכל
  החודשים מאז שנת 2000.
- **הכנסות המשרד השיתופי** (`n_cw_clients.payments`) עדיין לא מוקרנות למודל — המחזור של
  היחידה הזו מוצג במודול שלה בלבד. זו הרחבה ידועה שנותרה פתוחה.

#### תנועות ידניות: שלושה ספרים (`lib/accounting-entries.ts`)

`entryCollection(kind, book)` בורר את הקולקשן. `tx` הוא היעד של כל שורה חדשה; שני הספרים
הישנים נשארים קריאים ועריכים כדי שהדאטה הקיים ימשיך לעבוד במקומו:

| | `ledger` (הנה"ח אישית) | `branch` (ספר הסניף) | `tx` (מאוחד) |
|---|---|---|---|
| `income` | `n_ah_income` | `n_branch_income` | `n_tx` |
| `expense` | `n_ah_expenses` | `n_var_expenses` | `n_tx` |

שורה חיה **בדיוק באחד מהם**. בספרים הישנים שיוך לסניף הוא **העברה** (יצירה ביעד + מחיקת המקור
באותו batch), אף פעם לא העתקה. שורת `tx` לעולם לא עוברת בין ספרים כי יש רק ספר אחד — תיוג
מחדש לסניף אחר הוא שינוי של `node`, לא של קולקשן, וזה בדיוק מה שאיחוד הצירים קנה.

- **`lib/accounting-entries.ts`** — טיפוסים, `entryCollection`, `isPendingAttribution` והבנאים
  של כל צורת יעד. נקי מ-`firebase-admin` בכוונה: קומפוננטות הלקוח מייבאות ממנו. תנועת `tx`
  לעולם לא "ממתינה לשיוך" — היא מסווגת ברגע ההזנה.
- **`lib/accounting-entries-data.ts`** — `loadMovements()`: קורא את כל חמש הקולקשנים ומשטח
  אותן ל-`MovementEntry[]`.
- **`accounting/entry-actions.ts`** — `deleteEntryAction(kind, book, id)` ו-
  `updateEntryAction(kind, book, id, formData)`, owner בלבד. עריכת תנועת `tx` משנה את השורה
  במקום, ומשנה את `ownerShare` ואת `allocations` ביחס לסכום החדש כדי שהפיצול לא יסטה ממנו.
- **`accounting/entry-list.tsx` + `edit-entry-modal.tsx`** — הרשימה עם עריכה ומחיקה לכל שורה.

**`mirror`** — שורת `n_ah_expenses` שהיא רק בבואה של הוצאת סניף. המנגנון כבוי (ראו למעלה);
השורות שנותרו מסומנות, לא נספרות, וניתנות לניקוי ממסך בדיקת השלמות.

**מסך `/entries`** מציג את הממתינות לשיוך פתוחות, ואת מה שכבר שויך מקופל מאחורי שורת סיכום.
כרטיסי הסיכום למעלה נגזרים מהמודל המאוחד (`flowTotals`), ולכן הם כוללים מעצמם גם את חלק הבעלים
בהוצאות הקבועות ובהוצאות הסניפים — בלי שום תוספת ידנית שאפשר לשכוח או לחשב אחרת במסך אחר.
ההשקעה בציוד מוצגת בכרטיס נפרד, כי היא הון ולא הוצאה.

**מסך `/attribute`** (`attributeEntriesAction`) מקבל הכנסות והוצאות יחד. הסניף מנוחש מהשורה —
קודם `branchId` שכבר רשום עליה (הכנסת "ניידים"/"מזומן"), אחרת מהטקסט. שורה שמכסה כמה סניפים
("כל סניפי חדרי המחשבים") מתפצלת שווה בשווה, והשארית מהעיגול הולכת לסניף הראשון כך שהחלקים
תמיד מסתכמים בחזרה לסכום המקורי. הכנסה שעוברת לסניף נכתבת עם `collectedByOwner: true` — היא
יצאה מהספר של הבעלים, כלומר המזומן כבר בידיו, וזה מה שהתחשבנות השותף קוראת.

#### יציאת ציוד — מכירה, גריטה ואבדן (`/sales`, פרק יג׳)

מכירה היא תמונת הראי של רכישה: רכישה ממירה כסף לנכס, מכירה ממירה נכס לכסף. אותה שכבה, אותו
מנגנון, כיוון הפוך — ולכן אין כאן מודל שני ללמוד.

**האקסיומה:** תמורה ממכירת ציוד היא **לא הכנסה, היא החזר הון**. התנועה שנוצרת היא
`nature: "capital"`, `direction: "in"` — היא לא נכנסת למחזור, לא מתחלקת עם השותף ולא נוגעת
ברווחיות של אף סניף, מאותה סיבה בדיוק שהרכישה לא הייתה הוצאה (כלל 7).

מה שקורה בשמירה אחת (`recordExitAction`):
- לכל פריט: `status` סופי, `location: "sold"`, `soldAt`, `soldPrice`, `lastBranchId` ותנועת מלאי
- תנועה הונית נכנסת אחת על סכום התמורה, תלויה על הסניף שממנו יצאו הפריטים (או `shared` כשיצאו מכמה)
- התמורה מתחלקת בין הפריטים **ביחס לעלות שלהם** (`splitSaleProceeds`), לא שווה בשווה: מכירת
  מחשב של 1,500 ₪ עם תיק של 100 ₪ תמורת 1,000 ₪ לא מימשה 500 ₪ על התיק

**גריטה ואבדן** הם אותו מסך עם תמורה 0 ובלי תנועה נכנסת; ההפסד ההוני הוא מלוא `unitCost`.

**פריט אף פעם לא נמחק** — הוא רק משנה סטטוס. מחיקה הייתה שוברת את המאזן של כלל 4, וגם מוחקת את
הידיעה שהופסד שם כסף. מסך המלאי כבר לא מאפשר לסמן "נמכר"/"אבד" ישירות: יציאה דורשת תאריך, סיבה,
תמורה וסניף, וכולם נשמרים רק כאן.

שלוש הנוסחאות (`lib/assets.ts`): `investmentAtMonth` (כמה הון יושב בסניף), `capitalReturnedFromBranch`
(כמה הון חזר ממנו דרך מכירה, לפי `lastBranchId`), ו-`capitalResult` (רווח/הפסד הוני — מספר של
**כל העסק**, לא של סניף: הציוד הוא ההון של הבעלים, וספר הסניף מעולם לא נשא את העלות).

**אזהרת קריאה:** מכירת ציוד מעלה את אחוז ההחזר של הסניף (אותו רווח, פחות הון) — אבל זה לא בהכרח
שיפור, כי נשארו פחות מחשבים והרווח העתידי יהיה נמוך יותר. לכן הכרטיס מציג את שניהם, והמדד הראשי
הוא צפי האיזון ולא האחוז (ראו פרק טו׳).

**מתי מכירה מפסיקה להיות הונית:** אם הפריט עבד אצלך לפני שנמכר — הוני. אם נקנה במיוחד כדי
להימכר, באופן שיטתי — זו יחידה עסקית חדשה שצריכה צומת משלה בעץ, מחזור משלה ועלות מכר משלה.

#### מה מנהל הסניף מזין בעצמו (`/my-expenses`, פרק יד׳)

**הכלל: מנהל הסניף מזין עובדות, לעולם לא תנאים.** כמה, על מה, מתי והקבלה — עובדות, והוא היחיד
שיודע אותן. מי נושא בעלות — תנאי, שנגזר מההסכם ולא אמור להיות שדה שהוא רואה.

| מה הוא מקליד | מה נגזר אוטומטית |
|---|---|
| תיאור · סכום · תאריך · קטגוריה מרשימה סגורה · קבלה | `paidBy` מ-`Branch.expensePolicy` · `ownerShare` מאחוזי השותפות · `node` — הסניף שלו, מה-session ולא מהטופס · `nature: "operating"` |

**`Branch.expensePolicy`** (`lib/expense-policy.ts`) מחזיק מי משלם כל קטגוריה חוזרת
(אינטרנט · סינון · פרסום · שכירות · חשמל · הדפסות). עד עכשיו ההחלטה הזו נעשתה מחדש בכל שורת
הוצאה, ולכן יצאה לא־עקבית ואי אפשר היה להשוות בין סניפים. מסך **`/policies`** מציג את כל
ההסכמים בטבלה אחת; שינוי הסכם הוא שדה אחד, ולא תיקון של עשרות שורות אחורה — שורות שכבר נרשמו
שומרות על החלוקה שנרשמו בה.

**הוצאה חוזרת** מוזנת פעם אחת, ביום שהקו הותקן, עם `recurring.from`. מכאן והלאה השורה מופיעה לבד
בכל חודש. אף אחד לא מזין כלום כל חודש — זו כל הנקודה.

**רשימת סקירה, לא שער אישור** (`/review`, `lib/expense-review.ts`): ההוצאה נכנסת ונספרת מיד, בלי
חיכוך למנהל; אצל הבעלים היא מופיעה עם תיבת סימון אחת. אישור חוסם היה אומר שהמנהל מחכה לבעלים,
וזו בדיוק החיכוך שהמודל בא למנוע. שורה מסומנת אוטומטית כש:

| הטריגר | למה זה שווה בדיקה |
|---|---|
| פי 2 מהממוצע של **הסניף עצמו** באותה קטגוריה ב-3 החודשים האחרונים | קפיצה אמיתית או טעות הקלדה |
| קטגוריה שלא הייתה בסניף הזה מעולם | הוצאה חדשה שאולי לא סוכמה |
| אין קבלה וסכום מעל הסף (`RECEIPT_REQUIRED_ABOVE`) | גם רואה החשבון ישאל על זה |
| נרשמה לחודש שכבר נסגרה עליו העברה | משנה יתרה שכבר סוכמה |

הממוצע נמדד מול הסניף עצמו ולא מול כל הסניפים: אחרת סניף ששכר הדירה שלו גבוה לגיטימית היה מסומן
בכל חודש, ורשימה שצועקת זאב היא רשימה שאף אחד לא פותח. הדגלים מחושבים **פעם אחת בזמן ההזנה**
ונשמרים על השורה, כך שהסיבה שהיא הוצפה לא משתנה בשקט כשהממוצע זז.

**הלולאה כבר סגורה** ואין לבנות אותה מחדש: הקיזוז מול ההעברה החודשית (`computeMonthlySettlement`),
הדוח שהמנהל מקבל ב-1 לחודש (`branch-month-report.ts`) והיתרה שמתגלגלת (`branch-ledger.ts`) עובדים
נכון כבר היום.

#### כל מספר נכון לתאריך (פרק טו׳)

**האקסיומה השלישית: היסטוריה לא נערכת. מוסיפים לה.** מחשבים שנוספו — תנועת מלאי חדשה. הוצאה
שנגמרה — תאריך סיום. מחיר שהשתנה — גרסה חדשה. סניף שנסגר — תאריך סגירה. אותה תשובה, ארבע פעמים.

**בדיקת האיכות:** אם קיימת במערכת פעולה כלשהי שמשנה מספר של חודש שכבר עבר — היא באג, לא משנה כמה
היא נוחה.

**1 · הקצאה מתוארכת.** `investmentAtMonth` / `investmentSeries` (`lib/assets.ts`) משחזרות את
תנועות המלאי עד לתאריך הנדרש, במקום לצלם את המצב של היום. בלי זה, סניף שנפתח עם 5 מחשבים וקיבל
עוד 2 השבוע ייראה רטרואקטיבית כאילו היו בו 7 מחשבים מההתחלה. **`n_item_moves` הוא מקור האמת
למיקום; `Item.location` הוא מטמון** — בדיקת שלמות משווה ביניהם, כי זה הכשל היחיד במודל שאין לו
שום סימפטום גלוי.

**המלכודת שנפתרה:** ההשקעה היא קו מדרגות, ולכן אחוז ההחזר יורד ביום שמזינים ציוד נוסף (32% → 23%)
בלי שקרה שום דבר רע. לכן:
- **המדד הראשי הוא צפי האיזון בחודשים**, לא האחוז — הוא נגזר מהקצב בפועל ואי אפשר לקרוא אותו
  כהתדרדרות
- כרטיס הסניף מציג שורת **"הון נוסף"** עם החודש והסכום, כדי שהקפיצה תוסבר ולא תישאר חידה
- אחרי הזנת הון, הצפי מסומן **"טרם התייצב"** עד שיעבור חודש מלא

**2 · הוצאה שנגמרת ומחיר שמשתנה** (`lib/history.ts`). `endRecurring` נותן `recurring.to` —
**אסור למחוק** הוצאה חוזרת כדי לעצור אותה, כי מחיקה מוחקת גם חודשים שכבר נסגרה עליהם התחשבנות.
`reviseRecurringAmount` **סוגר תקופה ופותח גרסה חדשה** במקום לערוך את הסכום:

| הדרך | התוצאה |
|---|---|
| לערוך את `amount` ל-310 | מרץ–אוגוסט נקראים מחדש כ-310. 6 × 50 = 300 ₪ נכתבו מחדש בשקט, והדוחות שנשלחו לשותף כבר לא תואמים |
| לסגור ולפתוח גרסה | `{from:03, to:08, 260}` + `{from:09, 310}`. העבר קפוא, השינוי חל מספטמבר, הדוחות הישנים נכונים לנצח |

שורה חוזרת היא **סדרה של תקופות תוקף**, לא סכום — וזה גם נותן היסטוריית מחירים לכל ספק בכל סניף
בחינם.

**3 · סניף שנסגר.** `Branch.closedAt` הוא תאריך **עסקי**, ואינו `deletedAt` הטכני.
`closeBranch` עושה הכל ב-batch אחד: כותב את התאריך · נותן `to` לכל הוצאה חוזרת של הסניף (אחרת
הוא יצבור אינטרנט לנצח) · מחזיר כל פריט למחסן עם תנועת מלאי מתוארכת ו-`reason: "branch_closed"`
(אחרת ההון נשאר חנוט בסניף מת) · **משאיר את היתרה פתוחה** — סגירת סניף היא לא מחיקת חוב ·
**לא מוחק כלום**. `monthStatus` מקבל `after_close`: חודשים אחרי הסגירה מחושבים כמו חודשים לפני
הפתיחה — כלום. פתיחה מחדש היא `closedAt = null` ותנועות מלאי חדשות; שום דבר מהעבר לא צריך
להשתנות, כי שום דבר מהעבר לא נמחק.

#### מסך הסקירה (`/overview`) — שני ספרים שלעולם לא נסכמים
זהו כלל אי-הכפילות של המודול, ו-`lib/accounting-overview.ts` בנוי סביבו:
1. **תזרים** (היה: "שלי") — כמה כסף באמת עבר דרך הידיים והחשבון של הבעלים. **נגזר** מהמודל
   המאוחד (`buildMyLedger` מעל `UnifiedTx`) ולא נקרא יותר ישירות מ-`n_ah_income`/`n_ah_expenses`,
   ולכן הוא כולל מעצמו גם את חלק הבעלים בהוצאות הסניפים ובהוצאות הקבועות — בלי בבואות ובלי
   תיקונים שמתווספים בכל מסך אחרת.
2. **מחזור** (היה: "הסניפים") — הספר התפעולי של כל סניף ב-100%: מה נכנס ומה עולה להפעיל אותו,
   בלי קשר לכיס שדרכו זה עבר. מפוצל ל-**ניידים** (`rentals`) ול-**חדרי מחשבים** (`computers`).

**למה השמות שונו:** שני השמות הישנים נשמעו כמו "הכנסות", ולכן המוח מנסה לחבר אותם ומרגיש
כפילות שלא קיימת. בשמות החדשים הסיבה שאסור לחבר אותם נמצאת בשמות עצמם — אותו שקל עונה כאן על
שתי שאלות שונות, וחיבורן לא עונה על אף אחת מהן. השמות מוגדרים כקבועים ב-`lib/business-ledger.ts`
(`FLOW_LABEL`/`TURNOVER_LABEL`).

כסף שעבר מסניף לבעלים מופיע בתזרים כשהוא זז, וממשיך להיות הכנסת הסניף במחזור. כל מסך מציג
אותם בעמודות נפרדות. שדה `category` (רשות) מזין את פירוט התזרים; רשימות הקטגוריות ב-
`lib/accounting-categories.ts`. **ציוד לא מופיע באף אחד משני הספרים** — הוא מוצג בכרטיס נפרד
("השקעה בציוד (הוני)") וב-`/bottom-line` מתחת לשורה התחתונה, כי הוא הון ולא הוצאה.

**בתוך ספר הסניפים, לכל עלות מקור אחד בדיוק:** הוצאה שהוזנה ידנית לסניף
(`n_fixed_expenses`/`n_var_expenses`) תמיד מנצחת, ושורת התעריפון המקבילה מושמטת לאותו
סניף/חודש ומדווחת ב-`suppressed` (מוצגת במפורש בעמוד הסניף) כדי ששום דבר לא ייעלם בשקט.
הזיהוי לפי מילות מפתח ב-`expenseCoversRate` (`lib/cost-rates.ts`).

**תעריפון (`/rates`, `n_cost_rates`):** מחשב רגיל/מחשב גרפיקה/תיק/סטיק/סינון וגלישה. הכמות בכל
סניף נגזרת אוטומטית לפי `qtySource` (מספר המחשבים/הסטיקים/הסימים בסניף, או קבוע לחודש),
מוכפלת ב-`unitCost` ומתחלקת בין הבעלים לשותף לפי `owedBy` — עם דריסה פר-סניף ב-
`n_branch_cost_settings`. **בתעריפון נשארו רק עלויות חוזרות (`kind: "monthly"`)**: שורות הציוד
הוצאו ממנו, וטבלת ההשקעה בעמוד הסניף מחושבת מ-`n_items.unitCost` — העלות האמיתית של הפריטים
שנמצאים פיזית בסניף.
**מחשב רגיל מול מחשב גרפיקה:** אין ב-`n_laptops` שדה שמסמן מחשב כמחשב גרפיקה, ולכן `computer_graphics`
הוא `qtySource: "manual"` — הכמות מוזנת פר-סניף בטבלת "התאמת התעריפון לסניף" בעמוד הסניף
(`branch-cost-settings.tsx` → `n_branch_cost_settings`). שורת "מחשב רגיל" מחסרת אוטומטית את
הכמות הזו מסך המחשבים בסניף כדי שאף מחשב לא ייספר פעמיים; שורת "תיק למחשב" ממשיכה לפי כל
המחשבים. קטגוריה עם `unitCost: 0` לא מייצרת שורה כלל עד שמוזנת לה עלות.
`seedDefaultRatesAction` יוצרת רק קטגוריות שחסרות ולעולם לא דורסת קטגוריה קיימת — כך קטגוריית
ברירת מחדל חדשה מגיעה גם לתעריפון שנשמר לפני שהיא נוספה.

**מה יצא מהתעריפון — עלויות הציוד (`isRetiredRate` ב-`lib/cost-rates.ts`):** שורת
`kind: "once"` היא **הערכה** — מחיר שטוח אחד לכל מחשב בעסק, שנספר בחודש שבו המחשב נוסף. לכן
חשבונית אמיתית של 15,000 ₪ לא יכלה להתאזן מול מה שהסניפים הציגו: שני מספרים בלי שום קשר
מתמטי ביניהם. העלות האמיתית מגיעה עכשיו משכבת הנכסים, שבה כל יחידה נושאת את מה שהיא עלתה
בחשבונית שלה ויושבת בדיוק במקום אחד. הוצאו **בסינון ולא במיגרציה**, בדיוק כמו שני הבאים:
תעריפון שנשמר לפני השינוי ממשיך להחזיק את המסמכים, והם פשוט מפסיקים לייצר שורות עלות.
`seedDefaultRatesAction` לא זורעת אותם, ומסך התעריפון לא מציע להוסיף אותם חזרה.

**מה יצא מהתעריפון — פרסום והדפסת התקנון (`RETIRED_RATE_KEYS` ב-`lib/cost-rates.ts`):**
הסכום של שניהם שונה בכל חודש, ולכן מחיר יחידה שטוח היה תמיד גבוה או נמוך מדי. הם הוסרו
מ-`DEFAULT_COST_RATES`, ו-`loadCostRates` מסננת את המפתחות `ads`/`print` גם אם הם עדיין
קיימים כמסמכים ב-`n_cost_rates` מתעריפון שנשמר לפני השינוי — כך שהם מפסיקים לייצר שורות עלות
בלי מיגרציה ובלי מחיקת דאטה. מכאן והלאה כל סניף מזין אותם בעצמו כהוצאה חד-פעמית (או קבועה)
בחודש הרלוונטי; נוספה הקטגוריה "הדפסות ותקנונים" לרשימות ההוצאות של הסניף ולספר "שלי".
המפתח `ads` נשאר ב-`MATCH_TOKENS` — לא כשורת תעריפון אלא כדי שאזור פרסום משותף לא יחייב סניף
שכבר הזין פרסום ידנית.

**פרסום משותף (`/ads`, `n_ad_areas`, `lib/ad-areas.ts`):** קמפיין אחד לעיר שמתחלק בין כמה
סניפים. הבעלים נושא ב-`ownerPct`% מהקמפיין והשאר מתחלק בין הסניפים; בספר של כל סניף זה נכנס
כשורה אחת בגובה `monthlyCost / branchCount`. זו שורת הפרסום האוטומטית היחידה שיש (בתעריפון אין
יותר שורת "פרסום") — אלא אם לסניף כבר יש הוצאת פרסום ידנית באותו חודש, ואז היא מנצחת כרגיל
(הזיהוי לפי `ADS_RATE_MATCH` ב-`lib/ad-areas.ts`, ולא לפי שורת תעריפון שמורה).

**תאריך פתיחה של סניף (`n_branches.openedAt`) — מתי בכלל נפתח הספר:** לכל סניף יש תאריך פתיחה
וסימון "עדיין לא התחיל לפעול" (`notStarted`), שנקבעים במסך **ניהול סניפים**
(`/dashboard/accounting/branches`) — טבלה אחת של כל הסניפים עם מה שהמערכת מחשבת לכל אחד היום,
שדה תאריך, סימון "עדיין לא התחיל לפעול" ושמירה פר-שורה
(`saveBranchStatusAction`). אותם שני שדות קיימים גם בכרטיס הסניף
(`/dashboard/accounting/branches/[id]`), ומוצגים בכל מקום שבו הסניף
מופיע. `computeBranchActivity` (`lib/accounting-overview.ts`) קובע לכל סניף `startMonth` לפי
הסדר: `notStarted` (גובר על הכל → אין `startMonth` כלל) → `openedAt` → `founded` (נתונים ישנים)
→ החודש הראשון שיש בו הכנסה אמיתית (השכרה שהוחזרה או שורת הכנסה — **לא** הוצאה); המקור נשמר
ב-`startSource` ומוצג במסך ניהול הסניפים. **תאריך פתיחה שהוזן פותח את ספר הסניף בפועל**: מאותו
חודש הסניף עובר לספר שלו ומקבל שורות תעריפון והתחשבנות, גם לפני שנרשמה בו הכנסה ראשונה — זו
בדיוק המשמעות של השדה. רק סניף **בלי** תאריך פתיחה ממתין להכנסה הראשונה. לכל חודש נגזר `status`:

| `status` | מתי | מה מחושב |
| --- | --- | --- |
| `active` | החודש ≥ `startMonth` | הכל כרגיל |
| `before_open` | החודש < תאריך הפתיחה | **כלום** — אפס הכנסות, אפס הוצאות, אפס העברה |
| `not_started` | `notStarted` מסומן, **או** שאין תאריך פתיחה וגם לא נרשמה בסניף שום הכנסה | **כלום** — הסניף מסומן "לא התחיל השכרות" |

זהו תיקון לכשל אמיתי: לפני כן סניף בלי שום נתון נחשב "פתוח מאז ומעולם", וקיבל שורות תעריפון
חודשיות (פרסום, סינון וגלישה) שיצרו דרישת העברה מהשותף על סניף שעדיין לא התחיל לעבוד.
`BranchActivity.statusLabel` מזין את התגית בטבלת הסניפים, בכרטיסי הסניפים וברשימת סניפי
ההשכרות; סניף פתוח שעדיין אין בו אף השכרה/הכנסה מסומן גם הוא ("לא התחיל השכרות") אך הוצאותיו
כן נספרות מתאריך הפתיחה. ברגע שמוזנים לקוחות והשכרות — או שנקבע תאריך פתיחה — החישוב מתחיל
אוטומטית מאותו חודש, בלי שום פעולה נוספת.

**מי רשאי לשנות:** תאריך הפתיחה והסימון "עדיין לא התחיל לפעול" הם **owner בלבד** בכל מסלול —
`saveBranchStatusAction` ופעולות ה-CRUD של הסניפים דורשות `role === "owner"`, ומסכי עריכת
הסניף (`/dashboard/accounting/branches/*`) חסומים לשותפים לחלוטין.
שותף שנכנס לכרטיס הסניף שלו (`/dashboard/accounting/overview/[branchId]`) רואה את תאריך הפתיחה **לקריאה בלבד**,
עם ציון מפורש ששינוי שלו הוא בידי הבעלים. הסיבה: התאריך קובע מאיזה חודש הסניף מחויב ומתחשבן,
ולכן הוא החלטה של הבעלים ולא של בעל הסניף.

**התחשבנות חודשית:** בעמוד הסניף מוצג "להעביר לבעלים ב-1 לחודש" =
`incomeShareToOwner` + סך `netToOwner` של שורות ההוצאה (`expenseNetToOwnerFromShares`,
`lib/branch-accounting.ts`), כלומר חלק הבעלים בהכנסות בתוספת/בניכוי הקיזוז לפי מי שילם בפועל.
שני הרכיבים נשמרים ב-`BranchMonth.transferIncomePart` ו-`expenseNet`, ושמות שורות ההוצאה
שמזיזות כסף בפועל ב-`transferDrivers` — כדי שכל מסך יוכל לענות "על סמך מה הסכום הזה" בלי לפתוח
את טבלת העלויות המלאה (`transferBasis` ב-`overview/ui.tsx`).

**הרשאות:** את מסך הסקירה המלא רואה **הבעלים בלבד**. מנהל סניף/שותף עם הרשאת `accounting`
מנותב אוטומטית ל-`/overview/[branchId]` של הסניף שלו בלבד, ושם מוסתרים ספר "שלי", סיכומי
העסק, שאר הסניפים, טבלת ההשקעה, התעריפון ומסלולי הגבייה; עמודות "חלקי" מוצגות לו כ"חלקך"
ומראות את חלקו שלו. ניסיון לגשת ל-`[branchId]` של סניף אחר מבצע `redirect` ל-`/dashboard`.

**הכנסות (`n_ah_income`) - 3 סוגים בלבד, אלה היחידים שמתחשבנים בהנה"ח הראשית ובדף הבית:**
טופס ההוספה ב-`/dashboard/accounting` (וגם הטופס המקוצר ב-`/dashboard/settlement`)
בנוי סביב שדה `type`:
1. **`laptops`** (ניידים) - תאריך + בחירת סניף מתוך סניפי `rentals` (`branchId`) + סכום.
   `business` נקבע אוטומטית ל-`rentals`.
2. **`credit`** (אשראי מהעסק) - תאריך (ברירת מחדל ל-10 בחודש הנוכחי) + סכום, ללא סניף.
   `business` = `general`.
3. **`cash`** (מזומן) - תאריך + בחירת קופה מתוך סניפי `computers` (`branchId` = איזו קופה
   נלקח ממנה המזומן) + סכום. `business` נקבע אוטומטית ל-`computers`.

**חשוב:** סימון השכרה כ"שולם" (`markRentalPaidAction`, מסך שהוסר — הסימון נעשה כיום ב-`app.html`)
וסימון השכרה כ"הוחזר" (`markReturnedAction`, אותו קובץ) **לא** כותבים ל-`n_ah_income` -
זו בכוונה בדיקת-ספרים פנימית של מודול ההשכרות בלבד (`n_rentals`), עצמאית לגמרי מההנה"ח
הראשית. פעם היה שם קוד ישן שכתב אוטומטית ל-`n_ah_income` (וב-`markReturnedAction` אפילו חייב
אוטומטית דרך `chargeViaRoute` בכל החזרה עם מסלול גבייה, בלי שום פעולה מפורשת של הבעלים) - זה
הוסר, כי זה סתר את הכלל של 3 הסוגים למעלה: הכנסת ניידים מגיעה להנה"ח הראשית *רק* כשהבעלים
מקליד אותה ידנית שם (סוג `laptops`), לעולם לא אוטומטית מתוך מודול ההשכרות.

**הוצאות — חלק הבעלים נגזר, לא נכתב:**
הוצאת סניף (ניידים וחדרי מחשבים, כולל ה"סניפים" המשותפים) מגיעה לספר התזרים של הבעלים, אבל לא
תמיד ולא בסכום המלא. **הכלל לא השתנה; המימוש כן.** פעם הוא הופעל בכל מקום שבו נכתבה הוצאה,
ע"י יצירת שורת "בבואה" ב-`n_ah_expenses` — ולכן הוצאה קבועה, שחוזרת כל חודש, לא יכלה להיות
מיוצגת כך כלל, ומודול נפרד חישב אותה בזמן אמת ונוסף על הסכום ביד בכל מסך. היום הוא **סינון
אחד** במקום אחד (`buildFlow`/`flowTotals`, `lib/business-ledger.ts`):

1. **`paidBy: "owner"`** — רק כסף שבאמת יצא מהחשבון של הבעלים נכנס לתזרים. הוצאה שהשותף שילם
   לא נספרת כאן **גם אם חלק/כל החוב על הבעלים** — לא היתה יציאת מזומן; החלק שלו מתקזז מול
   ההעברה החודשית מהשותף (`expenseNetToOwner`/`computeMonthlySettlement`), ולא נגבה בנפרד.
2. מתוך אלה נסכם **`ownerShare`** — חלק הבעלים בשקלים. לתנועות מהקולקשנים הישנים הוא נגזר
   מ-`owedBy` בזמן ההקרנה (`ownerExpenseBurden`: `"partner"` → 0, `"shared"` → חצי,
   `"owner"`/ברירת מחדל → הסכום המלא); לתנועה חדשה הוא נשמר ישירות כמספר, ולכן אחוז חופשי
   (30% על הבעלים) לא דורש קולקשן משלו.

מה שנובע מכך:

- **הוצאות חד-פעמיות ורב-סניפיות** — לא נכתבת להן יותר בבואה. הן נספרות בתזרים כי המודל
  קורא אותן ישירות, לא כי מישהו העתיק אותן.
- **הוצאות קבועות** — נספרות באותה דרך בדיוק, לכל חודש שבו הן פעילות (`recurring`, נפרס בזמן
  קריאה). אין יותר מודול נפרד שמוסיף אותן מעל הסכום.
- **עלות הקמת סניפי חדרי מחשבים** — **הפסיקה להיספר כהוצאה**. הקמת חדר היא רכישת ציוד, כלומר
  תנועה **הונית**: היא מופיעה במלואה במזכר ההוני מתחת לשורה התחתונה, ולא בתוך הרווח התפעולי
  שבו מעולם לא היה מקומה (כלל 7). ההמרה לרכישה אמיתית עם פריט נעשית מ-`/purchases`.
- **`ownerExpenseBurden(amount, owedBy)`** (בלי תלות ב-`paidBy`) עדיין קיימת ומשמשת את
  חישובי הרווחיות של הסניף (`computeBranchFinancials`, `lib/branch-accounting.ts`) — שם
  רוצים את העלות הכלכלית האמיתית של הסניף בלי קשר למי פיזית שילם.

ערכי `type` הישנים (`fixed`/`variable`) נשארים בטיפוס לתאימות לאחור בלבד (רשומות ישנות) -
לא נוצרות יותר על ידי ה-UI. שדה `branchId` (אופציונלי) נוסף ל-`AccountingIncome` עבור
`laptops`/`cash`. **חשוב:** ההכנסות הפנימיות של מודול הניידים ומודול חדרי המחשבים (מעקב
השקעה מול רווח פר סניף, ראו `/dashboard/settlement`) הן נפרדות לגמרי מ-`n_ah_income`
ואינן מתחשבנות כאן.
יצירה/עריכה/מחיקה של מסלול - owner בלבד (`createCollectionRouteAction` /
`updateCollectionRouteAction` / `deleteCollectionRouteAction`, `/routes/[id]`
לעריכה). שדות הסוד (apiKey/apiSecret/receiptsApiKey/receiptsApiSecret) לא
מוצגים מחדש בטופס העריכה - משאירים ריק כדי לא לשנות אותם.
חישובי התחשבנות ב-`lib/branch-accounting.ts` / `branch-accounting-data.ts`.


### העברות ודוחות (`/dashboard/settlement`) — perm: rentals

המסך שנשאר ממודול ההשכרות: **כמה כל סניף צריך להעביר**, וההתחשבנות החודשית מולו.
היה `/dashboard/settlement`, והוא הועבר כמו שהוא (כולל ספר הסניף שתחתיו,
`/settlement/ledger`, ותצוגת הדו"ח `/settlement/report-preview`) כדי שלא יאבד עם המודול
שסביבו. הלוגיקה לא השתנתה — רק הנתיב.

- **`/accounting`** — הנה"ח ברמת סניף השכרות. **partner/employee** (לא owner) רואים תצוגת
  סניף אישית - `BranchAccountingView` עם סימון העברות (`mark-transferred-button.tsx`,
  `n_branch_transfers`) - בדיוק כמו קודם, ללא שינוי.

  **owner** רואה עמוד מאוחד (מיזוג של מה שהיה קודם שני עמודים נפרדים - "הנה\"ח" וה-tab
  הישן "חישוב הנה\"ח" ב-`/ledger` - הוסר מ-`rentals-tabs.tsx`, `/ledger` היום עושה `redirect`
  ל-`/accounting`):
  0. **בורר חודש + הפקת דו"ח** (שורה מעל הטבלה): מימין `MonthPicker` (`month-picker.tsx`) -
     `<select>` של 24 החודשים האחרונים עד החודש הנוכחי, מנווט ל-`?month=YYYY-MM`; ברירת המחדל
     היא החודש הנוכחי, וכל ערך פגום/עתידי נופל בחזרה אליו (הליג'ר נבנה רק עד היום). החודש
     הנבחר מזרים את **כל** העמוד - הטבלה, כרטיס שותפי המחשבים, ה-drill-down וכל קישורי
     ההיסטוריה שומרים עליו. משמאל `ReportButtons` (`report-buttons.tsx`) - ראו "דו"ח חודשי
     במייל" למטה.
  1. **`UnifiedBranchesTable`** (`unified-branches-table.tsx`) - טבלה אחת מסוגננת כמו גיליון
     אקסל, **שורה אחת לכל סניף** (סניפי-בת מסומנים ↳ ומקובצים מתחת לסניף-האב), לחודש הנבחר.
     העמודות נקראות מימין לשמאל כמו דף חשבון: סניף · **יתרה מחודש קודם**
     (`LedgerMonthRow.openingBalance`) · **הוצאות** (`settlementExpenseThisMonth`) · **הכנסות**
     (`grossIncomeThisMonth` - סה"כ ברוטו, לפני חלוקה) · **צריך להעביר**
     (`LedgerMonthRow.netToOwner` - החודש בלבד) · **כולל חודש קודם** (`LedgerMonthRow.totalDue`
     - השורה התחתונה) · **הועבר** (`TransferMarkCell` → `recordBranchTransferAction` הקיים
     מ-`ledger/actions.ts`; סימון התיבה קובע סכום מלא, ואפשר לערוך ידנית להעברה חלקית) ·
     **הוצאנו קבלה** (`ReceiptCheckbox`). כל עמודות הכסף צבועות באותה מוסכמה: ירוק/+ = הסניף
     צריך להעביר לבעלים, אדום/− = הבעלים צריך להעביר לסניף. בתחתית שורת **סה"כ** לכל העמודות
     המספריות. חודש שקודם לתחילת הפעילות של הסניף פשוט אין לו שורת ליג'ר, ואז כל נתוני
     ההתחשבנות שלו אפס (לא נופל).

     **מה נספר ב"הוצאות":** `settlementExpenseThisMonth` (חדש ב-`BranchFinancials`) = **סה"כ
     שהוצא** על שורות שיש עליהן התחשבנות בלבד, כלומר `expenseNetToOwner !== 0` - הוצאות
     משותפות, והוצאות שצד אחד שילם עבור השני (כולל שורות של הוצאה רב-סניפית). הוצאה שהבעלים
     גם שילם וגם כולה עליו **לא** נספרת - זה חשבון מול שותפים, לא דו"ח רווח והפסד. הפירוט
     עצמו זמין ב-`settlementExpenseLinesForMonth`.

     העמודות הישנות "חודש"/"הוצאות שלי"/"הוצאות שלו" הוסרו מהטבלה. השדות `myExpenseThisMonth`/
     `hisExpenseThisMonth` נשארו ב-`BranchFinancials` ואינם בשימוש בתצוגה כרגע.
  2. **`OwnerBranchesOverview`** (`owner-overview.tsx`, `"use client"`), כותרת **"מעקב
     התקדמות הסניפים"** (שונתה מ"סניפים - מבט על") - לכל סניף (מקובץ אב/בת): "הוצאתי עד היום"
     (`ownerInvestedToDate` - עלות מלאה על הוצאות `owedBy: owner`/ברירת מחדל, חצי על `shared`,
     0 על `partner` - `ownerExpenseBurden`), "הכנסתי עד היום" (`ownerEarnedToDate`), "מאזן".
     מתחת: טבלת Excel-מיני (`ComputerProfitTable`, ראו למטה). מעל, שורת **סימון סניפים** -
     checkbox לכל סניף (כולל תתי-סניף, מסומן ↳) שמצמצמת את התצוגה לסניף אחד או יותר שנבחרו
     (במצב "הכל" - ברירת המחדל - מוצגת הקבצת אב/בת עם סה"כ משותף כמו קודם; עם סינון פעיל
     מוצגת רשימה שטוחה של הסניפים שנבחרו בלבד, ללא ההקבצה). לחיצה על שם סניף (בכל מצב) מובילה
     ל-`?branchId=<id>#branch-history` - גם פותחת את תיבת ה-drill-down (ראו סעיף 4 למטה) וגם
     גוללת ומצמצמת את קטע ההיסטוריה (סעיף 3, בתחתית העמוד) לסניף הזה בלבד - "לחיצה אחת = הכל
     על הסניף הזה".
  3. **"היסטוריה מלאה" (`id="branch-history"`, בתחתית ממש של העמוד** - אחרי טבלאות
     ההכנסות/הוצאות של ההנה"ח הראשית, לא ליד הטבלה המאוחדת) - `<details>` מתקפל (פתוח אוטומטית
     כשמסוננת לסניף בודד), מכיל בדיוק את מה שהיה בעמוד `/ledger` הישן: `BalanceSummaryCard` +
     `BranchLedgerTable` פר-סניף (הועברו ל-`ledger/branch-ledger-table.tsx`) - **כל ההיסטוריה**
     מהחודש הראשון שיש בו פעילות ועד הנוכחי, עם `LedgerRowEditor` לעריכת "הועבר בפועל" גם
     בחודשים עברו. **כולל סניפים שנמחקו** (ראו "מחיקת סניף" למטה) - זו הסיבה שהיא בתחתית לגמרי:
     קטע ארכיוני/היסטורי, נפרד מהניהול השוטף למעלה. ללא `?branchId=` מוצגת היסטוריית כל
     הסניפים (פעילים + שנמחקו); עם `?branchId=` מוצגת רק היסטוריית הסניף הנבחר, עם קישור "הצג
     היסטוריה של כל הסניפים" לחזרה. ראו תיאור מפורט למטה.
  4. drill-down לסניף נבחר (`?branchId=`) - `BranchAccountingView` כמו קודם. הסניף מחופש
     בין **כל** סניפי ההשכרות (כולל שנמחקו) כדי שקישור מההיסטוריה יעבוד גם לסניף שנמחק.
  5. כרטיס **"שותפי מחשבים - להעברה"**: לכל מחשב עם `hasPartner` (ראו `/laptops` לעיל) מסכם,
     עבור החודש הנוכחי, את כל ההשכרות (של המחשב ושל הסטיק המשוייך אליו, אם יש) שהוחזרו באותו
     חודש, ומציג את הסכום שיש להעביר לשותף לפי `partnerPct` שלו (`computePartnerSettlement`,
     `apps/web/lib/partner-settlement.ts`) - מקובץ לפי שותף+סניף, כי לסניף יכולים להיות כמה
     שותפים על מחשבים שונים. זה נפרד לגמרי מהתחשבנות שותף-סניף הקיימת
     (`Branch.partnerPct`/`myPct`, `computeBranchFinancials`) - מיועד לשותפות על מחשבים
     ספציפיים בתוך סניף שהוא "שלי" (`Branch.isMine`), לא על סניף שלם.

  **דו"ח חודשי במייל לסניף** (`ReportButtons` מעל הטבלה, owner בלבד):
  - **"תצוגה מקדימה"** → `/dashboard/settlement/report-preview?branchId=&month=` —
    מרנדר ב-`<iframe srcDoc>` בדיוק את ה-HTML שיישלח (מסמך עצמאי עם ה-`<body>` שלו, ולכן
    ב-iframe ולא inline). **עובד בלי שום הגדרת מייל** - זו הדרך לאשר את העיצוב לפני שליחה.
  - **"שלח מייל לבדיקה"** → דיאלוג עם **שני בוררים בלתי-תלויים**: *לאיזה מייל לשלוח* (רשימת
    הסניפים עם כתובותיהם + הכתובת של הבעלים) ו*של איזה סניף הדו"ח*. ההפרדה מכוונת: כך אפשר
    לשלוח לעצמך את הדו"ח של כל סניף לבדיקה לפני ששותף מקבל משהו. → `sendBranchReportAction`
    (`report-actions.ts`, owner בלבד) שמחזיר `{ ok, message }` במקום לזרוק, כי "המייל עוד לא
    מוגדר" הוא מצב תקין שצריך להציג במלואו. מייל בדיקה **לא** מסמן `reportSentAt`, כדי שלא
    ימנע שליחה אמיתית לאותו סניף אחר כך.
  - **"שלח לכל הסניפים"** → `sendMonthlyReportsAction` → `sendMonthlyReports`
    (`lib/branch-report-send.ts`). הדיאלוג מציג **לפני** השליחה בדיוק לאילו סניפים וכתובות
    זה ילך, מי יידלג בגלל חוסר כתובת, ואזהרה שאי אפשר לבטל מייל שנשלח; אחריה מוצגת תוצאה
    פר-סניף. סניף שכבר קיבל את הדו"ח לאותו חודש מדולג (`reportSentAt`), כך שאפשר להריץ שוב
    בבטחה. סניפים `deleted`/`notStarted` לא נכללים.
  - **שליחה אוטומטית ב-1 לחודש**: `POST /api/monthly-reports`
    (`apps/web/app/api/monthly-reports/route.ts`), מיועד ל-cron - עונה גם ל-GET וגם ל-POST, כי Vercel Cron שולחת GET בלבד ונתיב POST-only פשוט לא היה נורה אף פעם. אין כאן session,
    ולכן הנתיב מוגן ב-`REPORTS_CRON_SECRET` או ב-`CRON_SECRET` של Vercel (`Authorization: Bearer <secret>`, ש-Vercel שולחת לבד) ו**מסרב לפעול
    בכלל** כשהמשתנה לא מוגדר. ברירת המחדל היא **החודש הקודם** - "מריצים ב-1 לחודש" פירושו
    לדווח על החודש שנסגר, לא על זה שהתחיל הבוקר; אפשר לעקוף עם `?month=YYYY-MM`. מחזיר
    `{ ok, month, sent, skipped, failed[] }`.
  - תוכן הדו"ח (`apps/web/lib/branch-month-report.ts`): לוגו מ-`n_label_settings/default`
    (אותו מקור ככותרת הדשבורד), שם סניף וחודש, מספר השכרות + סה"כ הכנסות, **פירוט** שורות
    ההוצאה המשותפות (תיאור / מי שילם / על חשבון מי / סכום - בשמות אמיתיים, לא "אני"/"השותף",
    דרך `getOwnerName`/`partnerName`), יתרה קודמת, התחשבנות החודש, סה"כ, ושורה תחתונה מנוסחת
    מנקודת מבט השותף ("עליך להעביר X" / "מגיע לך X" / "מאוזן"). ה-HTML מבוסס טבלאות עם עיצוב
    inline בלבד — Gmail/Outlook מתעלמים מ-`<style>` ומ-flex/grid.
  - **הלוגו במייל (`apps/web/lib/email-logo.ts`) — חשוב:** הלוגו נשמר ב-
    `n_label_settings/default.logoUrl` כ-**data URI בבסיס 64** (זה מה שמסך העלאת הלוגו
    ב-`app.html` מייצר, דרך `FileReader.readAsDataURL`). דפדפן
    מרנדר את זה מצוין, אבל **Gmail ו-Outlook מסירים תמונות `data:`** - כלומר דו"ח שנשלח היה
    מאבד את הלוגו בשקט. הפתרון הוא **inline attachment עם `cid:`**: `logoForEmail()` מפרק את
    ה-data URI ל-base64 + mime, ה-HTML מפנה ל-`src="cid:ultranet-logo"`, והתמונה נוסעת בתוך
    ההודעה דרך `Attachment.contentId` של Resend. לוגו `https://` מועבר כמו שהוא ללא צורך
    בכל זה; `svg` מוחזר כ-null בכוונה (חסום כמעט בכל לקוחות המייל, ועדיף fallback לטקסט
    "אולטרנט" מאשר תמונה שבורה). **התצוגה המקדימה בדפדפן** ממשיכה להשתמש ב-data URI ישירות.
  - שליחה בפועל: `apps/web/lib/mailer.ts` (Resend, שכבר היה dependency), דורש **רק** `RESEND_API_KEY`. `REPORT_FROM_EMAIL` הוא רשות: בלעדיו השליחה נופלת לכתובת הבדיקה של Resend (`onboarding@resend.dev`), שמגיעה רק לכתובת שאיתה נרשמת שם - `mailerSandboxMode()` מחזיר `true` וה-UI אומר את זה במפורש (רמז כחול בדיאלוג הבדיקה, אזהרה אדומה ב"שלח לכל הסניפים", כי שם זה פשוט ייכשל).
    בלי `RESEND_API_KEY` בכלל, `mailerConfigError()` מחזיר הודעה בעברית שאומרת בדיוק מה חסר
    ואיפה להגדיר אותו, והדיאלוג מציג אותה מראש. **מכוון שזה נפרד
    מ-EmailJS** שבמסכי ההתחברות: אלה רצים בדפדפן מול תבנית טקסט קבועה (קוד בן 6 ספרות) שלא
    יכולה לשאת מסמך HTML מעוצב או attachment.
  - כתובת לכל סניף (`lib/branch-report-recipients.ts`): `Branch.partnerEmail`, ובהיעדרה
    המשתמש ב-`n_users` שה-`branchId` שלו תואם. סניף בלי אף כתובת מדווח ככזה ולא מושמט בשקט.
  - **בנייה אחת לכל הנתיבים** (`lib/branch-report-send.ts`): התצוגה המקדימה, מייל הבדיקה,
    "שלח לכל הסניפים" והשליחה האוטומטית כולם עוברים דרך `buildBranchReportEmail()`, כדי
    שהתוכן לא יוכל להתפצל ביניהם.

  **חישוב חלקו של כל צד בהוצאה — `ownerShare`:** כל שורת הוצאה
  (`DatedExpenseLine`, `lib/branch-accounting-data.ts`) נושאת שדה `ownerShare` = חלק הבעלים
  בשקלים. לשורה רגילה הוא פשוט `ownerExpenseBurden(amount, owedBy)`, ולשורה של הוצאה
  רב-סניפית הוא `perBranchOwnerShare` (אחוז חופשי ש-`owedBy` לא יודע לבטא). **כל** חישובי
  ההוצאות קוראים ממנו: נטל הבעלים = `ownerShare`, נטל השותף = `amount - ownerShare`, ונטו
  לבעלים = `expenseNetToOwnerFromShares(amount, ownerShare, paidBy)`. לשורות רגילות זה זהה
  לחלוטין ל-`expenseNetToOwner(amount, paidBy, owedBy)` הישן (ראו הערת התיעוד על
  `expenseNetToOwnerFromShares` ב-`lib/branch-accounting.ts`) - כלומר המעבר לא שינה אף מספר
  היסטורי, הוא רק פתח את המודל לפיצול באחוזים.

  **חשוב:** גם `computeBranchFinancials` (דרך `buildIncomeLines`,
  `apps/web/lib/branch-accounting-data.ts`) וגם `computePartnerSettlement` סופרים רק השכרות
  שהוחזרו **וגם** מסומנות `paid: true` - השכרה יכולה להיות `status: "returned"` אבל עדיין
  `paid: false` (מופיעה ברשימת "לא שולם"/חובות ב-`/manage`), ואז היא **לא** נספרת בשום חישוב
  הנה"ח - כי אף אחד עוד לא באמת מחזיק את הכסף, הלקוח עדיין חייב אותו. בלי הבדיקה הזו, השכרה
  שהוחזרה אך לא נגבתה עדיין היתה מנפחת את היתרה המוצגת כאילו הסניף/השותף מחזיק כסף שצריך
  להעביר לבעלים.

  **"היסטוריה מלאה" (לשעבר `/ledger`, היום בתחתית `/accounting`) - לכל סניף השכרות** (כולל
  סניפי `isMine` בלי שותף - גם הם יכולים להחזיק כסף שצריך להגיע לבעלים - **וכולל סניפים
  שנמחקו**) מציג: כרטיס סיכום עליון עם היתרה הנוכחית
  (`buildBranchLedger`, `apps/web/lib/branch-ledger.ts`) - חיובי (ירוק) = הסניף/השותף צריך
  להעביר לבעלים, שלילי (כתום) = הבעלים צריך להעביר לסניף/לשותף, ואפס = מאוזן; ומתחתיו טבלה
  מסוגננת כמו גיליון אקסל (`<table>` עם גבולות לכל תא, כותרת מוצללת, שורות לסירוגין) עם כל
  ההיסטוריה מהחודש הראשון שיש בו פעילות (השכרה שהוחזרה/הוצאה/הכנסה ידנית) ועד החודש הנוכחי -
  חודש, נטו לחודש (מחושב מ-`computeBranchFinancials(...).settlementNetToOwner`), יתרה שהועברה
  מהחודש הקודם, סה&quot;כ לתשלום, הסכום שנרשם כהועבר בפועל (ניתן לעריכה inline בכל שורה - גם
  עבר, לתיקון/העברה חלקית - `LedgerRowEditor` → `recordBranchTransferAction`, `ledger/actions.ts`),
  יתרה נותרת (מועברת לשורה הבאה כ"יתרה מחודש קודם") וסטטוס (מאוזן/חוב אליי/חוב ממני). היתרה
  הנותרת מצטברת רציפות מהחודש הראשון עד הנוכחי - כך שחוב שלא שולם ממשיך "לגלול" קדימה עד
  שנרשמת עבורו העברה. `TransferMarkCell` בטבלה המאוחדת למעלה וה-`markTransferredAction` הישן
  ב-`BranchAccountingView` שניהם כותבים לאותה רשומת `n_branch_transfers` (כולל `transferredAmount`)
  כדי שהיא תופיע נכון גם כאן.

  **הכנסה אוטומטית מהעברה שסומנה:** כל שלושת נתיבי הכתיבה ל-`n_branch_transfers`
  (`TransferMarkCell`/`recordBranchTransferAction` ב-`ledger/actions.ts`, `MarkTransferredButton`/
  `markTransferredAction` ב-`accounting/branch-actions.ts`, ו-`LedgerRowEditor` שמשתמש באותו
  `recordBranchTransferAction`) קוראים ל-`syncBranchTransferIncome`
  (`apps/web/lib/branch-income-ledger.ts`, מבנה מקביל ל-`branch-expense-ledger.ts` הקיים
  להוצאות) - **כש-`transferredAmount` חיובי** (השותף/הסניף שילם לבעלים), נוצרת/מתעדכנת אוטומטית
  רשומת `n_ah_income` (`type: "laptops"`, `business: "rentals"`, `branchId`, `amount`,
  `date`/`month` = **תחילת חודש ההעברה עצמו** - לא "היום" - כדי שהעברה מאוחרת/רטרואקטיבית
  תיספר בחודש הנכון ולא בחודש שבו סומנה) - כך שהיא מופיעה גם בהנה"ח הראשית
  (`/dashboard/accounting`) וגם פר-סניף (`/dashboard/settlement`), בלי שהבעלים יצטרך
  להקליד אותה שנית באופן ידני. ה-id של הרשומה נשמר בשדה `BranchTransfer.linkedAhIncomeId`
  ומתעדכן/נמחק (`FieldValue.delete()`) יחד עם שינוי/איפוס הסכום - **לא** נוצרות רשומות כפולות.
  סכום שלילי או אפס (הבעלים חייב לסניף/לשותף, או שום דבר לא סומן) לא יוצר הכנסה, ומוחק קישור
  קיים אם היה. זה שינוי מכוון מההתנהגות המקורית (ראו הערה למטה על `n_ah_income` - "לעולם לא
  נכתבת אוטומטית" - שהתייחסה **רק** להכנסת ניידים דרך מודול ההשכרות עצמו/`n_rentals`, לא
  להעברה שהבעלים בעצמו מסמן שקיבל בפועל).

  **"הוצאנו קבלה"** - תא סימון עצמאי נוסף בטבלה המאוחדת (עמודה אחרונה, `ReceiptCheckbox` →
  `setBranchTransferReceiptAction`, `ledger/actions.ts`) - `BranchTransfer.receiptIssued`, לא
  קשור לסכום/הכנסה, רק מסמן שהוצאה קבלה על ההעברה.

  **יעד רווח פר-מחשב = 150 ₪ + מע&quot;מ (`VAT_RATE = 0.18`, `PROFIT_PER_COMPUTER_TARGET = 177`,
  `apps/web/lib/branch-accounting.ts`)** - שונה מ-150 ₪ קבוע. מוצג כטבלה (`ComputerProfitTable`,
  `accounting/computer-profit-table.tsx`) - **חודשים כעמודות** (לא שורות) כדי שהרצועה תתפוס
  מעט מקום אנכי ואפשר יהיה לעבור מהר על כמה סניפים/חודשים ברצף - שורת "רווח/מחשב" (סכום +
  ✓/− צבוע) ושורת "מחשבים" מתחת, ועמודת תווית קבועה (`sticky right-0`) כשגוללים אופקית. מוצג
  הן ב-`owner-overview.tsx` (קומפקטי) והן ב-`branch-view.tsx` (drill-down לסניף בודד).
  **חשוב:** `computersActiveInMonth` יכולה להחזיר `0` (סניף בלי אף מחשב רשום עדיין) -
  `buildComputerProfitTrend` **לא** מחליף את זה ב-1 (זה היה באג שהראה "1 מחשב" לסניף בלי אף
  מחשב אמיתי) - עם 0 מחשבים `profitPerComputer` הוא 0 ו-`isHealthy` תמיד `false`.

### מודולים רוחביים
- **משתמשים והרשאות** (`/dashboard/users`) — owner בלבד. שתי תיבות הרשאה בלבד (ראו סעיף 5).
- **ניהול הסניפים עצמם** יושב בתוך ההנה"ח (`/dashboard/accounting/branches`), כי סניף הוא
  ישות של המודל החשבונאי (מדיניות תשלום, סגירה עסקית, כרטיס רווחיות) ולא מסך ניהול נפרד.

---

## 9. אינטגרציות חיצוניות

- **Firebase / Firestore** — מסד הנתונים. גישת אדמין דרך `lib/firebase-admin.ts`.
  פרויקט: `ultranet-e94aa`.
- **EmailJS** (`@emailjs/browser`) — שליחת קודי התחברות/אימות מכשיר מהלקוח
  (`login/email-code-form.tsx`, `verify-device/verify-device-form.tsx`).
- **Resend** (`lib/mailer.ts`) — שליחת הדו"ח החודשי לסניפים מהשרת, כולל תמונות
  inline (`cid:`). בלי `RESEND_API_KEY`/`REPORT_FROM_EMAIL` מחזיר שגיאה מוסברת במקום ליפול.
  הדו"ח נשלח אוטומטית ב-1 לחודש דרך `/api/monthly-reports` (Vercel Cron, מוגן
  ב-`REPORTS_CRON_SECRET`/`CRON_SECRET`; ללא סוד מוגדר הנתיב מסרב לרוץ).
- **מסלולי גבייה** (`n_collection_routes`, `/dashboard/accounting/routes`) — רישום המסלולים
  ועמלותיהם, כדי שהכנסה תיזקף בניכוי העמלה הנכונה (`lib/collection-charge.ts`).

  **הסליקה עצמה יצאה מהמערכת הזו.** `lib/nedarim.ts`, `lib/ezcount.ts` והנתיב
  `/api/rentals/charge` נמחקו יחד עם מודול ההשכרות: חיוב כרטיס והפקת קבלה הם פעולה
  תפעולית מול לקוח, לא רישום חשבונאי, והם ממשיכים להתבצע ב-`app.html`. מה שנשאר כאן הוא
  רק **התוצאה** — `chargeViaRoute` רושם את ההכנסה נטו אחרי עמלת המסלול. הקולקשן
  `n_collection_routes` לא נגעו בו, כך שהמסלולים שהוגדרו קודם קיימים ומשמשים את שני הצדדים.
  **לעולם לא נשמר PAN מלא** — רק `cardLast4`/`cardExpiry` או טוקן מהשער.

### PWA — התקנה כאפליקציה (טאבלט / מולטימדיה ברכב)

`apps/web` הוא Progressive Web App מלא. אין קוד native נפרד — אותו Next.js
app שרץ בדפדפן ניתן ל"התקנה" כאפליקציה עם אייקון ומסך standalone (בלי סרגל
דפדפן) על טאבלט, ועל כל מערכת מולטימדיה ברכב שמריצה Android אמיתי (כמו רוב
יחידות ה-head unit שמאפשרות התקנת APK חיצוני) — **דרך אותו APK עטיפה אחד**,
לא פיתוח נפרד.

- `apps/web/public/manifest.webmanifest` — שם/אייקונים/צבעי מותג/`display:
  standalone`. `start_url` הוא `/dashboard`.
- `apps/web/public/icons/` — סט אייקונים (192/512/512-maskable/apple-touch),
  פלייסהולדר בצבעי המותג (`teal`/`ink`) עד שיוחלף בלוגו אמיתי.
- `apps/web/public/sw.js` + `apps/web/app/pwa-register.tsx` — Service Worker
  ורישומו. אסטרטגיה: **network-first** לניווט/דאטה (כדי שכל דיפלוי יתעדכן
  מיידית כשיש חיבור), **cache-first** רק לנכסים סטטיים עם hash
  (`/_next/static/...`). ה-cache מנוקה בכל `activate` כך שאין דליפת גרסה ישנה.
  כשמזוהה Service Worker חדש (=דיפלוי חדש) והוא הופעל, הלקוח מרענן את העמוד
  אוטומטית.
- `apps/web/next.config.mjs` — `headers()` ל-`/sw.js` ול-`/manifest.webmanifest`
  עם `Cache-Control: no-cache` כדי שדפדפנים תמיד יבדקו גרסה עדכנית.
- **התקנה בפועל**: בדפדפן (Chrome/Edge) על הטאבלט → "התקן אפליקציה" /
  "הוסף למסך הבית". לחבילת APK אמיתית (להתקנה על מולטימדיה ברכב או פרסום
  ב-Play Store) יש לעטוף את ה-PWA ב-**TWA** (Trusted Web Activity, בכלי
  `@bubblewrap/cli`) שמצביע על הדומיין הפרודקשן החי — זו רק "מעטפת" תצוגה,
  אין קוד native ללוגיקה. יש פלייסהולדר ב-
  `apps/web/public/.well-known/assetlinks.json` שיש להשלים עם ה-SHA256
  fingerprint האמיתי של מפתח החתימה לפני build ה-TWA (ראה הערות בקובץ).
- **עדכונים**: כל עוד מתקינים דרך PWA/TWA (ולא build native עצמאי), כל
  דיפלוי ל-production משפיע אוטומטית על מה שמותקן בטאבלט/ברכב — אין צורך
  בהתקנה מחדש.

---

## 10. עקרונות מפתח והחלטות ארכיטקטורה

- **אין מיגרציית דאטה.** ה-DB נשאר Firestore בדיוק כמו ב-`app.html`. אין שינוי
  סכימה, אין העברת נתונים. `app.html` הישן ממשיך לרוץ במקביל עד שהמערכת החדשה
  תבשיל.
- **מקור אמת אחד לטיפוסים** — `packages/shared-types`. כל שינוי שדה/קולקשן
  מחייב סנכרון עם ה-DB ועם `app.html`.
- **הרשאות בצד שרת** — כל מודול נשען על `requireModuleAccess` / `requireOwner`;
  אין להסתמך על הסתרת UI בלבד.
- **בטיחות תשלומים** — אין לשמור נתוני כרטיס מלאים; רק ארבע ספרות אחרונות,
  תוקף וטוקן שער.
- **קוד-סניף `"all"`** — לבעלים; מסמן ראייה חוצת-סניפים.
- **שלוש שכבות, ולא ספר אחד גדול** (סעיף 8) — כסף, נכסים, רווחיות. כל שכבה עונה על שאלה אחת,
  והן לא מתחברות זו לזו: זה לא כפילות שנמנעת, זה חיבור חסר משמעות.
- **רכישת ציוד היא לא הוצאה** — היא המרה של כסף לנכס. תנועה הונית נעצרת בשכבת הנכסים,
  והעלות נוסעת עם הפריטים בין המחסן לסניפים בלי שנרשם ולו שקל אחד נוסף. זו הסיבה שאותו שקל
  יכול להיות יציאה מהחשבון, השקעה בסניף, ואפס עלות לרווחיות אותו סניף — בו-זמנית.
- **שקל נרשם פעם אחת, במסך אחד** — `/dashboard/accounting/entries` הוא המסך היחיד שיוצר כסף;
  כל השאר מסווגים, מפצלים ומציגים. אם משהו במערכת דורש להקליד סכום פעם שנייה — זה לא צורך
  אמיתי, זו שכבה חסרה.
- **סינון במקום מיגרציה** — כשמודל משתנה, הדאטה הישן נשאר במקומו ומוקרן/מסונן בזמן קריאה
  (`RETIRED_RATE_KEYS`, `isRetiredRate`, `lib/tx-data.ts`). זה הפיך, ולא נוגע בספרים אמיתיים
  של העסק.
- **היסטוריה לא נערכת, מוסיפים לה** (פרק טו׳) — מחשבים שנוספו הם תנועה חדשה בתאריך, הוצאה
  שנגמרה מקבלת תאריך סיום, מחיר שהשתנה פותח גרסה חדשה, וסניף שנסגר מקבל תאריך סגירה.
  **אם קיימת פעולה שמשנה מספר של חודש שכבר עבר — היא באג**, לא משנה כמה היא נוחה.
- **פריט אף פעם לא נמחק** — הוא רק משנה סטטוס. מחיקה שוברת את מאזן כלל 4 וגם מוחקת את הידיעה
  שהופסד שם כסף.
- **מנהל הסניף מזין עובדות, לעולם לא תנאים** (פרק יד׳) — כמה, על מה, מתי והקבלה הן עובדות
  שרק הוא יודע; מי נושא בעלות נגזר מ-`Branch.expensePolicy` ואינו שדה שהוא רואה.

---

## 11. מפת קבצים מרכזיים

| נתיב | תפקיד |
|------|-------|
| `packages/shared-types/src/index.ts` | כל טיפוסי הדומיין / סכימת Firestore |
| `apps/web/lib/perms.ts` | שערי הרשאה (`requireModuleAccess`, `requireOwner`) |
| `apps/web/lib/nav-items.ts` | הגדרת ניווט + סינון לפי הרשאה |
| `apps/web/lib/auth.ts` | NextAuth (3 providers, סנכרון JWT) |
| `apps/web/lib/firebase-admin.ts` | אתחול Firebase Admin (web) |
| `apps/web/lib/rental-pricing.ts` | לוגיקת תמחור השכרות — נשארה כי `n_rentals` מוקרן להכנסות במודל |
| `apps/web/lib/collection-charge.ts` | רישום ההכנסה נטו אחרי עמלת המסלול (לא סליקה — ראו סעיף 9) |
| `apps/web/lib/branch-accounting*.ts` | חישובי הנה"ח והתחשבנות סניפי השכרות |
| `apps/web/lib/branch-ledger.ts` | בניית היסטוריית התחשבנות מלאה פר-סניף עם יתרה מצטברת (מוצג תחת `/dashboard/settlement`, owner בלבד) |
| `apps/web/lib/expense-shared-scope.ts` | סנטינלים `SHARED_RENTALS_BRANCH_ID`/`SHARED_COMPUTERS_BRANCH_ID` להוצאות משותפות לכל הסניפים |
| `apps/web/lib/multi-branch-expense.ts` | פיצול הוצאה חד-פעמית בין כמה סניפים לפי אחוז הבעלים (`splitMultiBranchExpense`). מודול טהור - גם טופס ה-client וגם החישוב בשרת מייבאים אותו |
| `apps/web/lib/branch-month-report.ts` | בניית הדו"ח החודשי לסניף ורינדורו ל-HTML עצמאי (טבלאות + inline styles, בשביל Gmail/Outlook) |
| `apps/web/lib/branch-report-recipients.ts` | לאיזו כתובת נשלח הדו"ח של כל סניף - `Branch.partnerEmail` ואז המשתמש התואם ב-`n_users` |
| `apps/web/lib/mailer.ts` | שליחת HTML במייל מהשרת דרך Resend, כולל תמונות inline (`cid:`); בלי `RESEND_API_KEY`/`REPORT_FROM_EMAIL` מחזיר שגיאה מוסברת במקום ליפול |
| `apps/web/lib/email-logo.ts` | הפיכת הלוגו השמור (data URI) ל-inline attachment עם `cid:` - כי Gmail/Outlook מסירים תמונות `data:` |
| `apps/web/lib/branch-report-send.ts` | בניית מייל הדו"ח (מקור אמת אחד לכל הנתיבים) ושליחה חודשית לכל הסניפים, כולל דילוג על מה שכבר נשלח (`reportSentAt`) |
| `apps/web/lib/accounting-entries.ts` | אוצר המילים של תנועה ידנית: `EntryKind`/`EntryBook`, `entryCollection`, `isPendingAttribution` והבנאים של כל צורת יעד. נקי מ-`firebase-admin` — נצרך גם ע"י קומפוננטות לקוח |
| `apps/web/lib/accounting-entries-data.ts` | `loadMovements()` — קריאת כל חמש קולקשני התנועות (כולל `n_tx`) והשטחתן ל-`MovementEntry[]` |
| `apps/web/lib/tx.ts` | שכבה 1 — טהור: קבועי `nature`/`node`, נירמול פיצולים לשקל (`normalizeAllocations`), פריסת `recurring` בזמן קריאה, ובניית תנועה (`buildTransaction`). נצרך גם ע"י קומפוננטות לקוח |
| `apps/web/lib/tx-data.ts` | **מודל הקריאה המאוחד**: `n_tx` + הקרנה של שש הקולקשנים הישנות לאותה צורה, בלי מיגרציה. מזהה בבואות ומוציא אותן מהמודל, ומסווג העברות מהשותף כ-`transfer` ולא כהכנסה |
| `apps/web/lib/assets.ts` | שכבה 2 — טהור: אילוץ החשבונית (`validatePurchase`), פריסת שורות לפריטים, השקעה לפי מיקום, פיצול תמורת מכירה לפי עלות (`splitSaleProceeds`), תוצאה הונית (`capitalResult`), **השקעה נכונה־לתאריך** (`locationsAtMonth`/`investmentAtMonth`/`investmentSeries`) ו-`paybackStatus` שהמדד הראשי שלו הוא צפי איזון בחודשים ולא אחוז. נצרך גם ע"י טופסי הלקוח |
| `apps/web/lib/assets-data.ts` | טעינת שכבת הנכסים ו-`moveItems()` — שאין לו פרמטר סכום, בכוונה |
| `apps/web/lib/business-ledger.ts` | הספרים כפי שהם נקראים: תזרים (`buildFlow`/`flowTotals`/`flowSnapshot` — שאילתה, לא קולקשן), מחזור, עץ מרכזי הרווח (`totalsByNode`), השורה התחתונה עם המזכר ההוני (`buildBottomLine`) וכרטיס הסניף (`loadBranchCard`) |
| `apps/web/lib/integrity.ts` | בדיקות כללים 3/4/5, המאזן ההוני (כולל פריטים שיצאו), התאמת `Item.location` לתנועה האחרונה, שלמות רישום היציאות, פריטים יתומים ובבואות שנותרו + `deleteLeftoverMirrors()` |
| `apps/web/lib/history.ts` | היסטוריה לא נערכת, מוסיפים לה (פרק טו׳): `reviseRecurringAmount()` — סגירת תקופה ופתיחת גרסה חדשה במקום עריכת סכום · `endRecurring()` — תאריך סיום במקום מחיקה · `closeBranch()` — תאריך סגירה עסקי, עצירת ההוצאות החוזרות והחזרת הציוד למחסן, ב-batch אחד ובלי למחוק דבר · `reopenBranch()` |
| `apps/web/lib/expense-policy.ts` | מדיניות התשלום פר סניף (פרק יד׳): מאיזו קטגוריה נגזר `paidBy`, רשימת הקטגוריות הסגורה של מנהל הסניף, וההסבר שהמסך מציג במקום שדה. מודול טהור — נצרך גם ע"י טופס הלקוח |
| `apps/web/lib/expense-review.ts` | סימון אוטומטי של שורות חריגות שסניפים הזינו, לפי הממוצע של הסניף עצמו (`flagsForEntry`), ומיון רשימת הסקירה |
| `apps/web/lib/branch-expense-ledger.ts` | מנגנון הבבואות — **כבוי**. `createLinkedOwnerLedgerExpense` הוא no-op; המחיקה נשארה אמיתית, כי הוצאה ישנה עשויה עדיין לשאת בבואה |
| `apps/web/lib/branch-income-ledger.ts` | יצירה/עדכון/מחיקה של רשומת `n_ah_income` מקושרת מהעברת סניף שסומנה כבוצעה (`BranchTransfer.linkedAhIncomeId`) |
| `apps/web/lib/owner-name.ts` | שמות אמיתיים (לא "אני"/"השותף") לשדות "מי שילם"/"על מי החוב" במסכי הוצאות - `getOwnerName` (שם המשתמש עם `role: "owner"`) ו-`resolveSharedPartnerName` (שם השותף ל"סניף" המדומה המשותף, כשכל סניפי השותפות במודול מסכימים על אותו שותף) |
| `apps/web/lib/device-trust.ts` | אימות/אמון מכשיר |
| `apps/web/app/api/monthly-reports/route.ts` | Vercel Cron — שליחת הדו"ח החודשי לכל הסניפים ב-1 לחודש |
| `apps/web/middleware.ts` | middleware של Next (הגנת נתיבים) |
| `apps/web/public/manifest.webmanifest` | הגדרת ה-PWA (אייקונים/צבעים/standalone) |
| `apps/web/public/sw.js` | Service Worker (network-first + cache נכסים סטטיים) |
| `apps/web/app/pwa-register.tsx` | רישום ה-Service Worker בצד לקוח |
| `apps/web/public/.well-known/assetlinks.json` | אימות דומיין ל-TWA (Android App Links) |
