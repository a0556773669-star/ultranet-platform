"use client";

export function ReturnButton() {
  return (
    <button
      type="submit"
      className="rounded-lg border border-card-border bg-white px-3 py-1.5 text-xs font-semibold text-muted transition hover:border-teal hover:text-teal"
    >
      סימון כהוחזר
    </button>
  );
}
